import fs from 'node:fs/promises';
import path from 'node:path';
import {SCREENS} from './screens.mjs';
const ROOT=path.resolve(import.meta.dirname,'../../..'),DIR=path.join(ROOT,'docs/design/audit/live-evidence');
const files=(await fs.readdir(DIR)).filter(x=>/^(chromium|webkit)-S-\d+\.json$/.test(x));
const data=await Promise.all(files.map(async f=>JSON.parse(await fs.readFile(path.join(DIR,f)))));
const supplement=JSON.parse(await fs.readFile(path.join(DIR,'supplement.json')).catch(()=>'{"forms":[]}'));
const assets=JSON.parse(await fs.readFile(path.join(DIR,'assets.json')));
if(data.length!==SCREENS.length*2||data.some(d=>d.harnessRevision!==2||d.records.length!==8||d.records.some(r=>r.error||!r.screenshot)))throw Error('Evidence matrix is incomplete; finish or repair the live run before reporting.');
const rows=data.flatMap(d=>d.records.map(r=>({...r,id:d.id,engine:d.engine,revision:d.harnessRevision})));
const rules=['ACCESS-008','TOUCH-003','INPUT-010','INPUT-006 / ACCESS-003','ACTION-007 / ACTION-008','LAYOUT-013 / LAYOUT-014','DATA-003/004 · LAYOUT-003','FEEDBACK-011'];
const forms=new Set(['S-22','S-33','S-36','S-45','S-53','S-63','S-76','S-79','S-86','S-88']);
function statuses(r){
 if(r.gap||r.error||!r.dom)return rules.map(()=>'U');
 const d=r.dom;
 const wrapFailure=d.wrap.some(x=>(x.path.includes('order-link')&&x.lines>1)||(x.path.includes('att-label')&&x.brokenWords.length));
 const noteEnter=supplement.forms?.find(x=>x.tab==='notes')?.enter;
 return [
  !r.axe?'U':r.axe.violations.length?'FAIL':r.axe.incomplete.length?'U':r.axe.passes.length?'PASS':'NA',
  !r.touch?'U':r.touch.failures?'FAIL':r.touch.checked?'PASS':'NA',
  d.inputs.some(x=>x.mismatch)?'FAIL':d.inputs.length?'PASS':'NA',
  d.inaccessible.length||r.keyboard?.status==='FAIL'?'FAIL':r.keyboard?.status||'U',
  !forms.has(r.id)?'NA':r.id==='S-22'&&noteEnter&&!noteEnter.textChanged&&!noteEnter.guarded?'FAIL':'U',
  d.overflow>1?'FAIL':d.clipping.length?'U':'PASS',
  wrapFailure?'FAIL':d.wrap.length?'PASS':'NA',
  d.colorOnly.length?'FAIL':d.statuses.some(x=>!x.text||/^\d+$/.test(x.text))?'U':d.statuses.length?'PASS':'NA'
 ];
}
function agg(a){return a.includes('FAIL')?'FAIL':a.includes('U')?'U':a.includes('PASS')?'PASS':'NA';}
const cell=r=>statuses(r);const results=SCREENS.map(s=>{const rr=rows.filter(r=>r.id===s.id);return {...s,rows:rr,valid:rr.filter(r=>!r.gap&&!r.error).length,status:rules.map((_,i)=>agg(rr.map(r=>cell(r)[i]))),gaps:[...new Set(rr.map(r=>r.gap||r.error).filter(Boolean))]};});
const shot=(id,variant='390x844-light',engine='chromium')=>`[${id} ${variant}](live-evidence/screenshots/${engine}/${variant}/${id}.png)`;
const ok='Verified-OK',bad='Confirmed-issue',unknown='Still-unverifiable';
// One row for each of the original 21 grouped unverifiable items (18 remain in the current master).
const resolutions=[
 ['TOUCH-003',bad,'Rendered hit regions include native selects, JS click targets, labels and pseudo-element extensions. Remaining failures are detailed per screen; an enlarged hit area is not failed solely for its smaller glyph.', 'S-76','IMP-011'],
 ['FEEDBACK-012 / ACCESS-008',bad,'Axe color-contrast failures remain where listed. A zero-violation run with incomplete nodes is U, not a pass. Production includes contrast changes beyond origin/main; old fixture ratios are not reused.', 'S-33','IMP-010 / IMP-012; proposed VA-001'],
 ['INPUT-022',unknown,'Placeholder appearance is measured in the supplemental modal probes; disabled-state distinction and every dynamic field are not fully covered.', 'S-45','IMP-025'],
 ['ACTION-002 (disabled clause)',unknown,'Only naturally rendered disabled states were inspected. The audit never enters committing/busy states, so it cannot certify all nine classes in every disabled condition.', 'S-36','IMP-004 / IMP-011'],
 ['DATA-006',bad,'The parsed inventory DOM contains one-row tbody groups, resetting nth-child striping. Expanded detail rows receive the even-row treatment; primary inventory rows do not alternate consistently.', 'S-11','IMP-049'],
 ['DATA-004 (trace/catalogue collision clause)',unknown,'Visible strings and wrapping are recorded, but no complete real-catalogue collision test was possible in empty scheduler data. The separate SO identifier failure is confirmed below.', 'S-68','IMP-021'],
 ['SEARCH-007',ok,'Browser window.find located live Shipping and Receiving text while expanded (2 and 1 matches), none while collapsed, and found it again after expansion. See find.json and find-S-17/18 screenshots. This settles the sampled expanded-content desktop clause; native toolbar UI and mobile Find were not exercised.', 'S-17','IMP-052'],
 ['ERROR-010',supplement.drag?.clicks>0&&supplement.drag?.selection?bad:unknown,'Dragging to select a live lot code produced selected text and also fired its click handler, opening the lot panel. This establishes a failure in one actual inventory link context; other contexts are not generalized.', 'S-11','IMP-048'],
 ['LAYOUT-014',unknown,'Both CSS viewport orientations are captured. Actual OS rotation/backgrounding is not exercised; any geometry failure is recorded separately in the screen matrix.', 'S-03','IMP-027 / IMP-066'],
 ['ERROR-002',unknown,'Unsaved local draft values are compared across viewport resize in the supplemental probes. App switching, reload, and recovery after a real failed commit are not tested.', 'S-22','IMP-008'],
 ['INPUT-010 (soft keyboard)',unknown,'Every rendered input is inventoried by type/inputmode and declared content. Desktop Chromium/WebKit cannot show or certify iOS/Android soft keyboards.', 'S-45','IMP-006'],
 ['FEEDBACK-002',unknown,'Live reads and status codes are logged, but serialized audit requests are not representative latency measurements. No committing operation was timed.', 'S-39','IMP-005'],
 ['FEEDBACK-003',unknown,'No hangs or API failures were deliberately induced. Initial connection-pool errors occurred; that observation does not establish stall timing for every workflow.', 'S-07','IMP-005'],
 ['FEEDBACK-011 (print / monochrome clause)',unknown,'Print CSS is captured, but physical monochrome output was not examined. Color-only overdue weekday signals on the Orders screen are independently confirmed below.', 'S-71','IMP-015 / IMP-065'],
 ['CHART-005',unknown,'DOM and screenshots do not establish what VoiceOver/NVDA announces for the graph. No screen-reader session was run.', 'S-68','IMP-045 / IMP-046'],
 ['CHART-006',unknown,'Small viewport chart rendering is captured; floor-device label legibility and glanceability require device/user verification.', 'S-58','IMP-027 / IMP-045'],
 ['ICON-007',unknown,'Optical alignment remains a visual-design judgement across the actual device/font population; no universal pass is inferred from geometry.', 'S-02','IMP-058'],
 ['ICON-002 / ICON-005',unknown,'These macOS engines are only one OS font environment; cross-platform emoji consistency and vector-icon substitution are not verified.', 'S-73','IMP-058'],
 ['ACCESS-001 (largest OS text size)',unknown,'CSS viewport sizes are not the largest OS text setting. This run does not replace the required real-device text-size test.', 'S-03','IMP-009 / IMP-062'],
 ['DATA-012',supplement.comparedDates>0&&!supplement.dateDifferences?.length?ok:unknown,`Compared ${supplement.comparedDates||0} matching live order dates in America/New_York and Pacific/Kiritimati. ${supplement.dateDifferences?.length||0} differences. This settles sampled order date/weekday timezone stability only; ER dates and human preferences remain outside that result.`, 'S-25','IMP-050'],
 ['OTHER-006',unknown,'No floor device, floor lighting, gloves, Spanish-label session or real-user workflow observation was available.', 'S-01','Process check']
];
const counts={};for(const [,v] of resolutions)counts[v]=(counts[v]||0)+1;
const allCounts=Object.fromEntries(rules.map((rule,i)=>[rule,Object.fromEntries(['PASS','FAIL','U','NA'].map(k=>[k,results.filter(s=>s.id!=='S-92'&&s.status[i]===k).length]))]));
let md=`# 06 — Read-only production browser check\n\n**Date:** 2026-09-08 · **Target:** ${'https://cns-factory-ledger.netlify.app'} · **Branch:** \`test/visual-audit\`, based on \`origin/main\` at \`feefa17\`.\n\nThis report supersedes the previous local-fixture results in this file. It audits production with Playwright Chromium and WebKit. [Standards](../FL-Design-Standards-MASTER.md), [inventory](00-screen-inventory.md), and [master improvements](IMPROVEMENTS-MASTER.md) were read; the master list and all shipped assets are unchanged. No cache-bust or changelog entry is applicable.\n\n## Scope, provenance and limits\n\nThe matrix includes all 91 inventory IDs plus **S-92**, an audit-only ID for the later Needs Attention strip. Each has eight requested viewport/theme combinations per engine: **390×844**, **844×390** (phone landscape), **768×1024**, and **1280×800**, each light/dark. DPR is 1; these are CSS viewport tests in desktop engines, not physical phone emulation. Print-only S-71/S-91 use print media at the same dimensions. A surface without a light palette remains its native palette; requesting light does not create one.\n\nThere are **${rows.length} matrix records**, **${rows.filter(r=>r.screenshot).length} screenshot paths**, and **${rows.filter(r=>!r.gap&&!r.error).length} successfully reached/measured screen variants**. ${rows.filter(r=>r.gap||r.error).length} records are unavailable-state context or harness errors; these are explicitly U. **A context screenshot is not a claim that an inaccessible dialog, populated table, or error state was captured.** See the coverage table. Every screenshot and raw result is committed under [live-evidence](live-evidence/).\n\nThe initial smoke/concurrent attempts encountered production HTTP 500 responses with “connection pool exhausted.” They were stopped. The retained run reuses loaded pages and serializes API reads across engines, forwarding genuine responses unchanged; it uses no fixture responses or data cache. Preliminary captures were superseded by the final harness revision. API timing in this arrangement is not a performance benchmark.\n\nProduction differs from the branch baseline in **${assets.files.filter(x=>!x.matches).length}/${assets.files.length}** sampled assets. [Asset hashes](live-evidence/assets.json) identify the observed deployment; they do not prove a single immutable deployment for the entire audit window. No production change is attributed to this branch. The application loaded without a login, using its existing configured authentication. Credentials, headers, request bodies, query strings, and browser storage were not written to evidence.\n\n[Safety observations](live-evidence/safety-observations.json) retain two discovery POST-preview requests blocked before transmission, and explain the guarded implicit Allocate activation generated by Enter. These are not successful submissions. S-39 is recaptured as an unavailable preview under this GET-only policy.\n\nThe harness never deliberately clicks Save, Post, Receive, Pack, Ship, Void, or any other commit control. It blocks non-GET/HEAD/OPTIONS requests, mutating paths and form submission before application handlers; native dialogs are dismissed. Modal drafts are cancelled. The scheduler uses a disposable context with its default local state; no sample orders, imports, baseline, or user scenario is created. [Guard test](../../../tests/visual/live/safety.test.mjs) verifies that the forbidden controls and network writes are intercepted on a synthetic data document.\n\n## Unverifiable-item resolutions\n\nThe current master has renamed its former “Unverifiable — needs browser check” section to “Settled by the browser check” and “Still unverifiable.” The following preserves **all 21 original grouped items**, including the three previously settled by fixtures. Verdicts apply to the stated clause, not an untested whole rule. Counts: **${counts[ok]||0} Verified-OK · ${counts[bad]||0} Confirmed-issue · ${counts[unknown]||0} Still-unverifiable**.\n\n| Rule / original item | Verdict | Live evidence and remaining boundary | Screenshot | Existing entry |\n|---|---|---|---|---|\n`;
for(const [rule,verdict,note,id,imp] of resolutions)md+=`| ${rule} | **${verdict}** | ${note} | ${shot(id)} | ${imp} |\n`;
md+=`\n## Six phone-review items added today\n\nThese are existing IMP-068–073, not new proposed entries. “Confirmed” below is limited to the observed facts, not every causal claim in the earlier phone review.\n\n| Item | Verdict | Observation | Rules | Screenshot |\n|---|---|---|---|---|\n| 1 · IMP-068 | Confirmed-issue | Attention labels fragment within words at 390 px. Per-word range rectangles record the breaks. | LAYOUT-003 · ACCESS-001 | ${shot('S-92')} |\n| 2 · IMP-069 | Confirmed-issue | Three months remain in the phone header, with 8 px day digits and an internal horizontal scroller. The controls and text occupy substantial entry-screen height; the run does not claim the entire attention strip is always below the fold. | NAV-001 · ACCESS-006 · TOUCH-003 | ${shot('S-02')} |\n| 3 · IMP-070 | Confirmed-issue | SO identifiers wrap at hyphens into three lines in the phone table. | DATA-003 · DATA-004 | ${shot('S-25')} |\n| 4 · IMP-071 | Confirmed-issue | Card title and hint remain in a horizontal flex row at phone width, squeezing the available reading columns. | LAYOUT-003 · INPUT-005 | ${shot('S-92')} |\n| 5 · IMP-072 | Confirmed-issue | The overdue date/weekday contains no “overdue” text or warning icon; its extra status is conveyed by red text alone. | FEEDBACK-011 | ${shot('S-25')} |\n| 6 · IMP-073 | Confirmed-issue | The header presents a numeric health badge without a visible Health label. Its tooltip is not an always-visible explanation. | NOTIFY-010 | ${shot('S-03')} |\n\n## Per-screen rule results\n\n**PASS** means the measured clause passed in all observed applicable variants; **FAIL** means at least one observed failure; **U** means incomplete/unavailable/manual verification remains; **NA** means no applicable element. A FAIL takes precedence over U. These are **91-screen aggregate counts**, excluding supplemental S-92. The complete engine/viewport breakdown is in [matrix.csv](live-evidence/matrix.csv).\n\n| Rule / measured clause | PASS | FAIL | U | NA |\n|---|---:|---:|---:|---:|\n`;
for(const rule of rules){const c=allCounts[rule];md+=`| ${rule} | ${c.PASS} | ${c.FAIL} | ${c.U} | ${c.NA} |\n`;}
md+=`\nACCESS-008 uses axe-core color-contrast, retaining incomplete nodes as U. TOUCH-003 measures every collected interactive hit region, including native selects, JS click targets, SVG pointer nodes, generated hit-area extensions and wrapping checkbox labels; it does not certify neighboring-target overlap. INPUT-010 here tests **declared type/inputmode intent**, not a soft keyboard. INPUT-006/ACCESS-003 tests field reachability and focus escape plus known non-tabbable click targets, not successful committing completion. ACTION-007/008 records safe Enter behavior; valid submission and destructive defaults are not activated. LAYOUT-013/014 here tests document overflow and clipping candidates; intended scroll containers are not automatically failures, and OS rotation remains U. Wrapping tests specifically identify the phone-review word/identifier breaks. FEEDBACK-011 PASS is limited to inspected visible textual status carriers; an unlabeled/pictorial carrier remains U.\n\n| Screen | Contrast ACCESS-008 | Hit TOUCH-003 | Input INPUT-010 | Keyboard INPUT-006/ACCESS-003 | Enter ACTION-007/008 | Layout LAYOUT-013/014 | Wrap DATA-003/004, LAYOUT-003 | Status FEEDBACK-011 |\n|---|---|---|---|---|---|---|---|---|\n`;
for(const s of results)md+=`| [${s.id}](live-evidence/chromium-${s.id}.json) ${s.name} | ${s.status.join(' | ')} |\n`;
md+=`\n## Coverage and state limitations\n\nEach row links its 390 px light viewport. Every engine/size/theme screenshot is indexed by the CSV and its screen JSON. “16/16” means both engines reached the selected state at all eight variants; it does not mean every datum, sub-state, validation condition, or row was tested. Scheduler populated-order/baseline/import-dependent states were not fabricated.\n\n| Screen | Measured variants / 16 | Context or limitation | Screenshot |\n|---|---:|---|---|\n`;
for(const s of results)md+=`| ${s.id} | ${s.valid}/16 | ${s.gaps.length?s.gaps.join('; ').replaceAll('|','/'):s.page==='scheduler'?'Default empty scenario only; no user orders or imported sample data.':'Selected live state; no synthetic error/empty responses.'} | ${shot(s.id)} |\n`;
md+=`\n## Form and dialog keyboard evidence\n\nThe screen JSON records the focused element sequence, whether it left the dialog, unreachable fields, and safe Enter observations per input. Selects, checkboxes, file pickers and buttons receive no Enter activation. Screens unavailable under the read-only policy remain U. Supplemental probes enter a disposable Note title and ER quantity, compare viewport-resize preservation, and cancel.\n\n| Form/dialog | Observed Tab result | Safe Enter observation | Screenshot |\n|---|---|---|---|\n`;
for(const s of results.filter(s=>forms.has(s.id))){const k=s.rows.filter(r=>r.keyboard).map(r=>r.keyboard);const enters=k.flatMap(k=>k.enter||[]);md+=`| ${s.id} ${s.name} | ${s.status[3]}; ${k.filter(x=>x.escaped).length} variants escaped the modal | ${enters.length} field probes; ${enters.filter(x=>x.guarded).length} guarded attempts; ${enters.filter(x=>x.newline).length} value/newline changes. Valid submit not executed. | ${shot(s.id,'1280x800-dark')} |\n`;}
md+=`\n[Supplemental evidence](live-evidence/supplement.json) includes placeholder contrast, unsaved draft resize preservation, two timezone date comparisons, and a real pointer-drag observation. A positive result on one sample is not generalized to every screen.\n\n## New proposed findings\n\n### VA-001 — Keep primary-button hover text above AA in dark mode\n\n**Status:** proposed — not yet in master list\n\n**Rules:** ACCESS-008 (Critical, Hard rule) · ACTION-002\n**Screens:** S-33 (observed) · **Importance:** Critical · **Effort:** S\n\nWith an editable live order open at 1280×800 in dark mode, hovering **Save Header** changes its background to \`rgb(96,165,250)\` (\`#60a5fa\`) while its 13 px normal-weight text remains white. Axe measures **2.54:1**, below the 4.5:1 target. The button was hovered, never clicked. The [dedicated hover screenshot](live-evidence/hover-S-33-dark.png) and [measurement](live-evidence/hover.json) capture the settled hover state.\n\nIMP-010 covers the normal primary fill/text token split, but does not name this hover-fill contrast failure. Production has changed the normal dark fill while retaining the failing hover fill, so verifying the resting state does not close this case. This is an additional state-specific proposal related to IMP-010, not a claim that its prior normal-state findings are new. Scope is the observed button; other users of the hover token need checking.\n\n**Fix (proposed only):** choose a hover fill that keeps white text at ≥4.5:1, and verify resting, hover, focus and pressed states in both themes. No implementation was changed.\n\n### VA-002 — Make the first Activity collapse click update the panel\n\n**Status:** proposed — not yet in master list\n\n**Rules:** ACTION-002\n**Screens:** S-17, S-18 · **Importance:** Medium · **Effort:** S\n\nBoth Activity panels initially render expanded. The first header click leaves the expanded class and visible searchable contents unchanged; a second click collapses the panel. The [recorded before/after classes and find counts](live-evidence/find.json), [Shipping screenshots](live-evidence/find-S-17-expanded.png), and [Receiving screenshots](live-evidence/find-S-18-expanded.png) document the live state. This is distinct from IMP-052’s find-within-page discoverability issue.\n\n**Fix (proposed only):** initialize the expansion model from the rendered initial state, so the first activation collapses and updates its visible state. No implementation was changed.\n\n**Other findings:** observed failures map to existing IMP-009–011, IMP-015, IMP-021, IMP-025, IMP-027, IMP-031, IMP-033, IMP-049, IMP-065/066 and IMP-068–073. They are not duplicated as new entries.\n\n\n## Reproduction and verification\n\nFrom \`tests/visual\`: \`npm ci\`, \`npm run browsers\`, \`npm run audit:live\`; then \`node live/supplement.mjs\`, \`node live/hover.mjs\`, \`node live/find.mjs\`, and \`node live/report.mjs\`. The existing root fixture command writes a different report and must not be used to reproduce these results. [Runner notes](../../../tests/visual/live/README.md). The pinned package lock and browser versions in each result identify the tools.\n`;
await fs.writeFile(path.join(ROOT,'docs/design/audit/06-browser-check.md'),md);
const csv=[['screen','engine','variant','screenshot','gap','error',...rules],...rows.map(r=>[r.id,r.engine,r.variant,r.screenshot||'',r.gap||'',r.error||'',...statuses(r)])].map(row=>row.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\n')+'\n';
await fs.writeFile(path.join(DIR,'matrix.csv'),csv);
await fs.writeFile(path.join(DIR,'summary.json'),JSON.stringify({counts,phoneItems:{[bad]:6},newProposed:2,records:rows.length,screenshotPaths:rows.filter(r=>r.screenshot).length,reached:rows.filter(r=>!r.gap&&!r.error).length,ruleCounts:allCounts},null,2));
console.log({counts,records:rows.length,reached:rows.filter(r=>!r.gap&&!r.error).length,allCounts});
