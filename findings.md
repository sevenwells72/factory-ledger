# Findings

- Adapted finished-good filter from `product_type = 'finished'` to live schema column `type = 'finished'`.
- Included live `products.brand` in the read-only query to apply the requested Sunshine 10 oz pouch case-pack lookup.
- Wrote GPT knowledge outputs under existing repo GPT directory: `/Users/cns/Documents/factory-ledger/docs/gpt`.
