# Factory Ledger Changelog — Regression Guard

Every fix is logged here so future sessions know what breaks if a change is reverted.

## Change History

| # | Date | Area | What Changed | Problem It Solved | Breaks If Reverted | Migration/File |
| 135 | 2026-09-11 | Sales-order **health v2 — time-aware tiers** and the **Ready to Ship** relabel of the Readiness dimension (branch `feat/so-health-v2`, based on `main` at `e8a8aa2`) — **NOT DEPLOYED** (code + docs only; no migration, no `dashboard/` change) | `compute_so_health()` rewritten from v1 to v2 and relabelled **"v2 — time-aware. Shape is the contract."**; the `{level, reasons, info}` shape and every caller are untouched, and it still issues no SQL of its own. Evaluated on OPEN orders only. **critical** = a stock shortage on a non-cancelled line AND `ship_by <= today + SO_HEALTH_CRITICAL_DAYS` (a past `ship_by` is inside that window by definition). **warning** = a shortage further out than that window, or with no ship date at all; OR overdue (`ship_by < today`, `fulfillment != 'shipped'`) with NO shortage; OR Ready to Ship unset with `ship_by <= today + 2`. **info** = `unallocated_need_lb > 0`, never affecting the level. **quiet** = nothing applies, or the order is closed/cancelled (no reasons and no info at all). Highest tier wins and every applicable reason is listed. Reasons now carry specifics and the date: `Short 735 lb on 2 lines — ships in 3 days`, `Short 500 lb — ships in 14 days`, `12 days overdue — stock on hand`, `Not Ready to Ship — ships tomorrow`; the time half comes from one helper, `_so_ship_phrase()`. Shortages are aggregated into ONE reason (v1 emitted one per line) and `on N lines` is omitted when N is 1. New env var **`SO_HEALTH_CRITICAL_DAYS`, default 5**, read on every call like `ALLOCATIONS_ENFORCED`; a non-integer or negative value falls back to 5 rather than raising. The Ready-to-Ship window is a separate constant `SO_HEALTH_READY_DAYS = 2`, deliberately NOT tied to the env var. `info` now reports unallocated pounds whether or not `ALLOCATIONS_ENFORCED` is set (v1 suppressed the observation entirely while the flag was off); the `; allocations not enforced` note is appended only when it is off. **Docs:** design standards bumped 1.1 → 1.2 with Appendix E — Readiness is now written **"Ready to Ship — the floor's flag, currently stored as Factory Ready"** in the STATUS-001 rule line, the STATUS-001 worked example and the STATUS-012 Factory Ledger line; rule IDs unchanged, nothing added, renumbered or removed. Same relabel in `docs/design/so-state-model-findings.md` (owner ruling 5), plus a new **Health v2** section carrying the tier table, the env var and the reason wording, and a note on ruling 4 recording that the shape survived the rewrite. **Tests:** 152 in `tests/test_sales_order_state_model.py` (was 116) — a 24-row tier matrix driven straight against `compute_so_health()` with a pinned `today`, covering every tier row and BOTH sides of each boundary (`today + 5` critical vs `today + 6` warning; `today` critical; `today - 1` critical; `today + 2` Not-Ready warning vs `today + 3` quiet), multi-reason orders, closed/cancelled quiet, and the env var widened / narrowed / defaulted / fed garbage. The existing shape tests are kept. Mutation-verified and reverted: changing the critical-window `<=` to `<` fails `shortage-exactly-the-boundary` and the narrowed-env-var test and nothing else. Suite 758 passed, 0 failed (was 722). | v1 made ANY stock shortage critical regardless of when the order ships. The same 500 lb short is a different fact at three days out than at three months, so the red tier said nothing and the Sales Orders board read as a list of orders rather than a queue of work — the operator still had to open every red row to find out which ones were actually this week's. Separately, "Factory Ready" is a storage detail (`sales_order_flags.ready`, migration 037) that had leaked into the operator's vocabulary in the written standards, while the counts endpoint already used the floor's own words (`ready_to_ship`, owner ruling 5). | Restoring "any shortage is critical" puts back the flat red board this exists to fix. Changing the critical-window comparison from `<=` to `<` silently moves the boundary a day in and is caught by exactly two tests — keep them. Tying the Ready-to-Ship window to `SO_HEALTH_CRITICAL_DAYS` makes retuning the SUPPLY window silently move the FLOOR's window too; they are separate on purpose. Making `_so_health_critical_days()` raise on a bad value turns an env-var typo into a 500 on the Sales Orders read path. Reading the env var once at import instead of per call breaks the documented retune-without-deploy property and every test that sets it. Dropping the `not short_lines` guard on the overdue reason prints the overdue fact twice on a short order, which reads as two problems. Re-gating `info` on `_allocations_enforced()` hides unallocated pounds exactly when allocations ARE enforced and the gap matters most. Emitting one shortage reason per line puts back the wall of text that buried the total and the date. The `{level, reasons, info}` shape is owner ruling 4 and is the contract — the tier rules are explicitly NOT; changing the shape breaks callers, changing tiers does not. Reverting the doc relabel re-splits the vocabulary between `ready_to_ship` in the API and "Factory Ready" in the standards; STATUS-001 and STATUS-012 IDs are permanent and must never be renumbered. | `main.py`, `tests/test_sales_order_state_model.py`, `docs/design/FL-Design-Standards-MASTER.md`, `docs/design/so-state-model-findings.md` |
| 134 | 2026-09-10 | Dashboard asset cache-buster assertion in `tests/test_recent_ledger.py` (branch `chore/post-051-cleanup`, based on `main` at `c4873b6`) — **NOT DEPLOYED** (tests only; no runtime file touched) | `test_dashboard_activity_renders_occurred_entered_lag_and_backfill_badge` hardcoded `dashboard.css?v=40`, `shell-layout.css?v=3` and `dashboard.js?v=56`. It now reads each version out of `dashboard/index.html` with a regex and asserts the invariant that actually matters: every asset the Activity feed depends on is referenced EXACTLY ONCE from index.html, with a numeric `?v=` cache-buster, and the file it names exists on disk. | index.html had moved to v=42 / v=5 / v=59, so all three assertions had rotted — this was the known pre-existing `test_recent_ledger` failure the suite carried through rows 128–131, failing for a reason that had nothing to do with the Activity feed. A version bump is routine; a test that has to be edited for every bump gets ignored, and then it catches nothing. | Re-pinning a literal version number puts the rot straight back. Dropping the `exactly once` check lets index.html reference one asset at two versions, where whichever tag the browser reads last wins and one of them serves stale bytes. Dropping the on-disk existence check lets a renamed or deleted dashboard asset ship as a 404 that no test notices. | `tests/test_recent_ledger.py` |
| 133 | 2026-09-10 | `sales_order_allocations.released_by` attribution on the cancel and ship paths (branch `chore/post-051-cleanup`, based on `main` at `c4873b6`; follow-up 1 of `docs/design/so-state-model-findings.md` § "`_operator_id()` is a no-op placeholder") — **NOT DEPLOYED** | The three call sites the findings doc names now attribute releases with `caller_source_tag(request)` instead of `_operator_id(_)`: `cancel_order_line` (its `_expire_auto_fifo_allocations` + `_release_active_allocations` pair) and `ship_order` (both `_sales_order_ship_plan` calls). The third — order cancel via `PATCH /sales/orders/{id}/status` — was already correct as of PR #40 and is now covered by a test so it stays that way. `ship_order`, `ship_order_preview`, `commit_ship_order` and `cancel_order_line` gain a `request: Request` parameter, the convention `close_sales_order` and `release_sales_order_allocation` already use. Existing rows are NOT backfilled. 8 new tests in `tests/test_released_by_attribution.py`. | The same column held two incompatible vocabularies depending on which code path released the row. `_operator_id()` returns the constant `'legacy-shared-key'` on 100% of calls — `verify_api_key` → `_authorize_api_key` returns a bare `True`, which is neither a dict nor a str — so master key and dashboard key were indistinguishable downstream, while the manual release endpoint on the same table wrote `'dashboard'` or NULL. `'legacy-shared-key'` is also explicitly banned by three existing assertions in `tests/test_sales_order_allocations.py` and `tests/test_expected_receipts.py`. | Feeding these paths `_operator_id()` again re-splits the column and re-introduces a value the suite bans. NULL on a master-key call is the CORRECT answer, not a gap: per-user attribution is FR-15 and does not exist — a placeholder that looks like an identity is worse than an honest absence, and `state_changed_by` carries the same warning in row 131. `_operator_id()` and `verify_api_key` are deliberately UNTOUCHED — seven other handlers (`ship`, `pack`, `void_transaction`, `correct_transaction`, `reassign_lot`, `merge_lots`, `update_order_line`) still call `_operator_id` and still write the placeholder; retiring it is a separate change. `PATCH .../lines/{line_id}/cancel` is NOT on the dashboard allowlist (only `.../update` is), so the master key's NULL is its only reachable case — adding that route to the allowlist to make a `'dashboard'` case testable would be a security change, not a test fix. | `main.py`, `tests/test_released_by_attribution.py`, `tests/test_sales_order_state_model.py`, `tests/test_ship_order_service_line.py` |
| 132 | 2026-09-10 | Run-once guard on the in-app startup migrations (branch `chore/post-051-cleanup`, based on `main` at `c4873b6`) — **NOT DEPLOYED** | Every DDL-bearing startup block in `main.py` is now gated on a durable marker row in `migration_markers` — the table migration 051 introduced, deliberately generic, already present in prod. One marker per block, named for the migration it belongs to: `startup_migration_label_type`, `_004`, `_005`, `_006`, `_008`, `_010`, `_011`, `_012`. First boot with no marker runs the DDL and inserts the marker IN THE SAME TRANSACTION; later boots read one row and issue no DDL. Every one of those transactions — including the one that creates `migration_markers` — opens with `pg_advisory_xact_lock(STARTUP_MIGRATION_LOCK_KEY)` (`216448987635338620`, the first 8 bytes of sha256 of `factory_ledger.startup_migrations`, documented next to the helper), taken BEFORE the marker SELECT, so the whole check-DDL-insert sequence is atomic against a concurrently booting instance rather than merely idempotent; the lock is transaction-scoped and releases on commit or rollback. If the marker SELECT or INSERT raises for ANY reason — permission, RLS, missing table — that block's transaction is rolled back, a warning naming the block and the error is logged, and the block is re-run with `gated=False` in a fresh transaction, so a misconfigured `migration_markers` degrades to the pre-guard every-boot behaviour and never to silently-unapplied schema. The migration sequence moved out of `startup()` into `_run_startup_migrations()` so tests can drive it against a recording connection — the SQL inside is unchanged statement for statement, verified mechanically. 16 tests in `tests/test_startup_migrations.py`, all in a throwaway schema on their own connection — including a real two-connection concurrency test (two threads, two connections, released on a barrier against a database with no markers and no `migration_markers` table; every DDL statement must appear exactly once across BOTH statement logs) and three marker-denied tests that run the sequence as a non-superuser role which owns every table the blocks touch but not `migration_markers` (SELECT-denied, INSERT-denied, and RLS-enabled-with-no-policies). | `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` is a no-op in EFFECT but not in COST: it still takes ACCESS EXCLUSIVE on the target for the duration of the statement, on every process start. Several processes starting at once — a deploy, an autoscale, or a test suite standing up TestClients — queue those locks behind each other and behind any open transaction. That is the harness fragility recorded in `docs/design/so-state-model-findings.md`, and it is what wedged the suite. | Removing the marker gate puts ACCESS EXCLUSIVE back on every boot. Gating the **Migration 007 status sweep**, the **051 cutover reconciliation**, or **Migration 009** breaks them: those are data fixes, not DDL — they take no ACCESS EXCLUSIVE, they are idempotent by their own WHERE clause, and they must keep running every boot to catch rows that arrive later (a marker would freeze an order that lands as `status='new'` tomorrow). Writing the marker BEFORE the block, or in a separate transaction, makes a crashed migration permanently skipped — it is written only on success, so a failed block is retried. Removing the advisory lock, or taking it AFTER the marker SELECT, reopens the race it closes: two instances both read "no marker" inside the same window and both run the whole sequence, so every ALTER TABLE takes ACCESS EXCLUSIVE twice — the exact cost the guard exists to remove. Changing `STARTUP_MIGRATION_LOCK_KEY` is the same bug with no visible symptom, because advisory locks are keyed by value alone and two instances on different keys do not exclude each other. `ON CONFLICT DO NOTHING` and the `IF NOT EXISTS` clauses stay as the second line of defence for the ungated fallback path, which takes no lock. Removing the ungated fallback — either the one for a `migration_markers` that cannot be established at all, or the per-block retry when its SELECT or INSERT raises — turns an unreadable or unwritable bookkeeping table into silently-never-applied schema. The RLS case is the one to keep in mind: with RLS enabled and no policies, a NON-OWNING role gets an empty SELECT (indistinguishable from "not applied yet") and a refused INSERT, so the refused write is what catches it. The app role must OWN `migration_markers` — an owner bypasses RLS — or the guard is silently forfeited and the DDL runs every boot with a warning. Asserting the second boot from the resulting SCHEMA instead of a statement log proves nothing — it cannot tell "the ALTER was skipped" from "the ALTER ran and was a no-op", and the no-op is the expensive case. | `main.py`, `tests/test_startup_migrations.py` |
| 131 | 2026-09-10 | Orthogonal sales-order state model — migration 051, three administrative exits, derived Fulfillment, tiered Health, counts endpoint (branch `feat/so-state-model`, based on `main` at `750601e`; discovery + owner rulings: `docs/design/so-state-model-findings.md`) — **DEPLOYED 2026-09-10 ~22:50 UTC**: owner-approved PR #40 squash-merged as `c4873b6` at 2026-09-10 22:42:30 UTC; Railway auto-deployed `main` (`main.py` is inside `railway.json`'s `watchPatterns`). Railway deployment id to be added — the Railway CLI is installed (4.30.3) but neither authenticated nor linked to a project in this environment, so the id could not be read. **Migration 051 APPLIED TO PROD 2026-09-10 22:30 UTC** (`migration_markers` row present, backfill counts below) — **deploy checklist step 2 STILL OUTSTANDING: `tests/schema/schema.sql` has not been re-dumped since 051 was applied (it is still the post-050 dump and carries none of 051's columns); `scripts/dump_prod_schema.sh` needs the prod connection string**; Codex cross-review of PR #40 (2 blockers, 8 should-fix) a second review (5 should-fix, 2 nits) a third (1 P1, 3 P2, 1 P3) and a fourth (tests + docs only) all addressed in place on the same branch, migration 051 edited rather than superseded because it was, at the time, unapplied everywhere except the local test DB — it is now applied to prod and is FROZEN; any further schema change needs a new migration | Adds a stored administrative `state` (open \| closed \| cancelled) to `sales_orders` alongside — never replacing — the operational `status`, with `state_reason` (CHECK-partitioned per state: closed takes shipped_recorded \| shipped_not_recorded \| short_closed, cancelled takes customer_cancelled \| cns_declined \| duplicate \| superseded \| other), `state_note`, `state_changed_at`, `state_changed_by`, `related_so_id` (FK to sales_orders), `status_before_exit`, and an index on (state). Backfill maps legacy status → state with the LEDGER as tiebreaker (effective_status='posted' over non-cancelled, non-service lines), so a shipped order whose shipment was voided backfills `shipped_not_recorded`, not `shipped_recorded`. **Production backfill counts (051 applied 2026-09-10 22:30 UTC):** status=confirmed → state=open: 20; status=shipped → state=closed reason=shipped_recorded: 190; status=shipped → state=closed reason=shipped_not_recorded: 6; status=cancelled → state=cancelled reason=other: 62; unmapped rows left untouched: 0; 278 rows backfilled in total. No prod row carried status new / in_production / ready / partial_ship / invoiced, so those mappings moved zero rows. Three dashboard-key-allowlisted endpoints — POST /sales/orders/{id}/close \| /cancel \| /reopen — follow the existing preview/commit body-mode pattern; cancel is refused with 409 ORDER_ALREADY_SHIPPED unless derived fulfillment is `unshipped`, pointing the caller at close/short_closed. Close and cancel release the order's active reservations through the existing audited `_release_active_allocations()` path (reasons `order_closed` / `order_cancelled`), after the same lock + auto-FIFO-expire ordering the legacy cancel path uses. They write NO ledger postings and emit NO trace events (proved with emission forced on). Read side gains `state`/`state_reason`/`state_note`/`state_changed_at`/`state_changed_by`/`related_so_id` plus derived `fulfillment` (unshipped \| partial \| shipped) and `health` ({level, reasons, info}; closed/cancelled ⇒ quiet) on BOTH GET /sales/orders items and the detail response, additively — no existing field removed. New `state=` filter on the list; new GET /sales/orders/counts → {open, ready_to_ship, overdue, shipped, closed, cancelled}, declared before /sales/orders/{order_id} so 'counts' is not resolved as an order number. Fulfillment and health reuse SALES_ORDER_READINESS_SQL — zero new SQL. **Cross-review pass:** state is authoritative — every write that ships or allocates now requires `state='open'` checked UNDER the order row lock, with a normative lock order of sales_orders row (`SELECT … FOR UPDATE`) first and product/lot locks second; `ship_order` commit previously read the order unlocked and took product locks inside the ship plans. Exit release is order-scoped and no longer triggers product-wide auto-FIFO expiry. `state_reason` CHECK carries `IS NOT NULL` per terminal branch plus a trailing `IS TRUE` (a bare `IN` list evaluates to NULL for a NULL column and a CHECK admits NULL). Backfill idempotency moved to a new durable `migration_markers(name PK, applied_at)` table. `changed_by` is stripped, never truncated, 400 over 120 chars; `released_by` always from `caller_source_tag(request)`. `related_so_id` must exist, may not be self, and is refused for reasons that cannot use it — identically in preview and commit. Health info reports `unallocated_need_lb` whenever > 0. `attribution_note` returned in preview too. Legacy `PATCH /sales/orders/{id}/status` routes `cancelled` through the cancel logic (unshipped guard included) and `invoiced` through close (mirroring `'invoiced'`, NOT close's usual `'shipped'`); new/confirmed/in_production/ready are unchanged and never touch state. Idempotent startup sweep reconciles legacy `status='cancelled'` → `state='cancelled'`. **Review-2 pass:** every `sales_orders`/`sales_order_lines` row lock is `FOR NO KEY UPDATE`, NOT `FOR UPDATE` — verified first that nothing deletes or rekeys either table (no DELETE exists; key columns id/order_number/id are never UPDATEd; `order_number` is written only by the BEFORE INSERT trigger). `FOR UPDATE` conflicts with the `FOR KEY SHARE` locks the nine inbound FKs take, which deadlocked reciprocal duplicate-cancellations (A→B, B→A) with 40P01. Normative lock order is order row → line rows → product/lot, products ascending id, expressed by `_lock_sales_order()` / `_lock_sales_order_lines()` / `_lock_allocation_products()`; line mutations take the order row first, and ship commit pre-acquires every shipment product ascending before BOTH planning loops. Legacy PATCH .../status runs the state + fulfillment guards BEFORE the MANUAL_TRANSITIONS gate. Race tests pin the endpoint's backend by `application_name` and assert via `pg_blocking_pids()` that the holder is its blocker and the initial order-row SELECT is its waiting statement — mutation-verified to fail when either row lock is removed with the state check left in. Migration rerun tests run in a throwaway schema on their own connection. **Review-3 pass:** `add_order_lines` takes the order row lock before its eligibility read and inserts, and requires `state='open'` (409 ORDER_NOT_OPEN otherwise) — adding a line advances the order. Every no-deadlock test now FORCES the interleaving with a holder parked on the contended resource one step past each writer's first lock, and asserts both endpoint backends are parked behind it before release; the check follows the blocking chain because Postgres reports only the direct blocker. Races capture the exact backend PID with `pg_backend_pid()` before invoking the endpoint (UUID `application_name` is for human diagnosis only, never for matching). Race cleanup no longer swallows exceptions — the one un-deletable case, an order pinned by append-only `trace_events`, is an explicit branch that closes it instead. Deadlock-freedom is claimed for SALES-ORDER write paths only; `make` / `pack` / `reassign_lot` product ordering is an out-of-scope follow-up with its own cross-review. **Review-4 pass (tests + docs only; `main.py` and `migrations/` byte-identical, verified with `git diff --exit-code`):** the three deadlock tests rebuilt with STAGED holders so the pre-fix ordering cannot pass them — ship-vs-close parks a holder on each product and asserts both writers sit in the LOW product's blocking chain and neither in the high one's; quantity-reduction-vs-exit moved onto the same order and line, asserting the exit is blocked DIRECTLY by the reduction which is itself blocked by the product holder. All three mutation-verified one at a time with full revert: reversing ship's product order, restoring `FOR UPDATE` in `_load_so_for_state_change` (Postgres reports 40P01 on the `FOR KEY SHARE OF sales_orders` FK check), and removing the order lock from the quantity path. `_assert_no_deadlock` also reads JSONResponse bodies, since these endpoints catch a deadlock into a 500 rather than raising. 116 tests in `tests/test_sales_order_state_model.py`; suite 697 passed + the known pre-existing test_recent_ledger failure. No dashboard/ and no openapi-gpt-v3.yaml changes (that file is already at its 30-operation ceiling). | Status conflated three unrelated questions: what an administrator decided about the order, what physically shipped, and what needs attention. There was no way to close an order short, record WHY it left the board, or mark one a duplicate of another — 'cancelled' was the only exit and it silently asserted nothing had shipped. | **DEPLOY ORDER: migration 051 must be applied to prod BEFORE this branch deploys — SATISFIED, applied 2026-09-10 22:30 UTC** — GET /sales/orders and the detail endpoint both select `so.state`, so the Sales Orders tab 500s against a pre-051 schema. Reverting the mirror (state change writing `sales_orders.status` directly in the same transaction: closed→'shipped', cancelled→'cancelled', reopen→status_before_exit) silently drops closed orders back into every `status=open` consumer — fifteen backend readers plus dashboard.js's own SALES_ORDER_OPEN_STATUSES list. Removing `status_before_exit` makes every reopen flatten to 'confirmed', losing 'ready'. The mirror deliberately BYPASSES PATCH /sales/orders/{id}/status and MANUAL_TRANSITIONS — routing it back through them fails, because that endpoint rejects 'shipped' outright and confirmed→shipped is not an allowed manual transition. `state_changed_by` is a SURFACE or SELF-REPORTED identity, never an authenticated one; it must never be fed `_operator_id()`, which returns the constant 'legacy-shared-key' on 100% of calls. Loosening cancel's unshipped precondition lets an order with pounds already out the door assert that they never shipped — and the SAME guard must stay on the legacy PATCH .../status='cancelled' route, or that endpoint becomes the way around it. Reverting the order row lock in `ship_order` commit, or moving the `state='open'` check off that locked read, reopens a lost-update window where a close/cancel committing concurrently is overtaken by a shipment. Re-adding `_expire_auto_fifo_allocations()` to the exit release makes closing one order silently release another order's stale reservation and desyncs `reservations_released` from what actually changed. Dropping `IS NOT NULL` from the state_reason CHECK silently re-admits terminal rows with no reason. Gating the backfill on the order rows instead of `migration_markers` re-backfills rows that have legitimately moved on. Widening the startup reconcile sweep to `shipped`/`invoiced` destroys the Open · Shipped distinction the state model exists to expose. Removing the order row lock from `add_order_lines` lets a line land on an order a concurrent exit has already taken off the board. Removing the parked holder from a no-deadlock test makes it pass vacuously — one writer simply finishes before the other starts, and nothing is proved. `tests/schema/schema.sql` needs a re-dump after the prod apply. | `migrations/051_sales_order_state_model.sql`, `main.py`, `tests/test_sales_order_state_model.py`, `docs/design/so-state-model-findings.md` |
| 130 | 2026-09-10 | Design standards category 17, Status & Data Display (branch `docs/design-standards-status-data-display`, based on `main` at `750601e`) — docs + test harness only, no application file touched, not deployed | Added STATUS-001–014 to `docs/design/FL-Design-Standards-MASTER.md` as new section 17, version bumped 1.0 → 1.1, Appendix A count 178 → 192, Appendix D added to record the change. No existing rule renumbered, reworded or removed. Eight of the fourteen are machine-checkable and were added to the Playwright design-audit harness (`tests/visual/lib/checks.mjs`, verdicts in `run-visual-audit.mjs`, findings in `lib/report.mjs`): STATUS-002 coloured nominal badges, -004 more than one danger/warning-toned element per row, -005 the `data-explain` chip explanation hook, -006 number formatting, -007 orphan dash placeholders, -008 row height ≤ 56 px at ≥ 1200 px, -010 developer vocabulary, -011 repeated disclaimers. Baseline over 388 captures: FAIL captures 36 / 94 / 170 / 64 / 42 / 54 / 84 / 22 respectively | The Sales Orders surface merged State, Fulfillment, Readiness and Health into one column, badged the ordinary case, and rendered raw stored decimals (`13500.0000`); the standards had no category covering any of it and the harness could not measure the redesign | Reverting drops the only written definition of the four order dimensions, the `data-explain` hook contract the redesign is being built against, and the eight checks that would otherwise have to be rewritten after the redesign. STATUS-005 fails on every chip until the redesign lands — that is expected, not a regression. Rule IDs are permanent: never renumber STATUS-001–014 | docs/design/FL-Design-Standards-MASTER.md, tests/visual/ |
| 129 | 2026-09-09 | Railway watch paths (branch `chore/railway-watch-paths`, based on `main` at `ad8f9f1`) — **DEPLOYED 2026-09-09 11:14 ET**: owner-approved PR #38 squash-merged as `f951070`; Railway `0d6e3d92` SUCCESS for the merge (expected final rebuild under the old config), /health 200 healthy with database connected and pool active at 15:14:46Z. Watch-path behaviour verified: the row-129 DEPLOYED docs push `a865478` produced Railway entry `38940a52` with status SKIPPED at 15:15:18Z, no build, `0d6e3d92` stayed live and /health stayed 200. NOTE: Railway CLI 5.49.6 warns that `railway.json` config-as-code is deprecated in favour of `.railway/railway.ts`; existing files keep working until 2026-12-01, so migrate before then | New `railway.json` with `build.watchPatterns` so Railway only rebuilds the FastAPI service when a commit touches root `*.py` (main.py, extraction.py), `requirements.txt`, `runtime.txt`, `railway.json`, or `dashboard/dashboard_config.json` (read from disk by three `/dashboard/api/*` handlers). Docs-only commits (changelogs, `docs/`, `*.md`), `dashboard/` UI assets (Netlify-hosted), `tests/`, `gpt-configs/`, `migrations/` (applied by hand in Supabase, never read by the service) no longer trigger a rebuild. Positive patterns only, no negations. | Every push to main, including docs-only commits, rebuilt the service and caused a ~5 min 502 window. | Reverting brings back a rebuild + 502 window on every docs commit. Removing `/dashboard/dashboard_config.json` would let config edits ship to git but never reach production. Adding a new root-level module without `/*.py` (or a new build-time file) requires extending the list. NOTE: the first deploy after merge still rebuilds; the config takes effect for subsequent pushes. | railway.json |
| 128 | 2026-09-09 | Sales Orders design audit 5 — **DEPLOYED 2026-09-09 10:45 ET**: owner-approved PR #37 squash-merged as `003119c`; Netlify production `6aa170feaf08540008be5211` ready at 14:45:39Z, live css v42 / js v59 / intake v12 / shell v5; Railway `a435e847-969a-4095-a7df-6cc6e8ef7701` SUCCESS for the merge, /health 200 healthy with database connected and pool active at 14:46:13Z | All columns fit above 1100px: use available desktop width, content-sized Status, wrapped Factory Ready stamp/dispatch badges/pallet text and Blockers labels. Sticky sorting retained; scrolling hint only at 769–1100px; phone cards retained. Shell-layout v5. Intake v12 / dashboard JS v59 / dashboard CSS v42 references remain above main; these three assets are unchanged. Strengthened fixture regression: 12/12 width/theme scenarios, including 1280px, long order IDs and a PO attachment. | Original scroll reachability test missed desktop clipping: Status and Pallets claimed excessive minimum width while Blockers was offscreen. | Keep native auto table layout, wrapping metadata, content-sized Status and available list width. Check all columns at scrollLeft=0; document overflow alone misses overflow:clip. Preserve sort offset and phone cards. Row 128 avoids deployed intake row 127. | dashboard/shell-layout.css; HTML cache references; tests/visual/run-sales-orders-layout.mjs; docs/design/audit-2026-09-09-batch-5.md |
| 127 | 2026-09-09 | SO intake follow-up — three findings from the read-only sales-order intake review (branch `fix/so-intake-followup`, based on `main` at `43960a4`; Codex cross-review approved at `1316b14` — **DEPLOYED 2026-09-09 ~10:21 ET**: PR #36 squash-merged to main as `30b4f11`; Netlify production deploy 6aa16b31012ffc000879d369 ready 14:20:44Z, live index serves intake-logic.js?v=11 / dashboard.js?v=58 / dashboard.css?v=41; Railway FastAPI deployment 8fa4a164-aa9e-432c-8e97-e524153c2554 SUCCESS (commit `30b4f11`; ~5 min 502 during the rebuild, /health 200 healthy, pool active at 10:21); remote branch deleted) | (1) `dashboard/intake-logic.js` `mergeRematch`/`soMergeRematch` take `{ preserveResolved }`: a line with `chosen` set is returned whole (unlocked) and only unresolved lines take the fresh match; `dashboard.js` `soRunMatch(opts)` remembers the option (`intake.matchOpts`), the PO-number `change` handler passes `{ preserveResolved: true }`, the customer `change` handler passes `null` (full re-match), Retry reuses the last mode, and a FAILED preserve-mode match only unlocks lines instead of `applyRematchFailure`. (2) `applyProductPick` copies `product.case_size_lb` into `lb_per_unit` (`lb_source='case_size'`) unless the line already has a `manual` conversion or is an lb-unit line, then recomputes pounds; `main.py` `_so_product_public` gains `case_size_lb` (always present, filled by new `_so_attach_case_sizes` — master `case_size_lb`, else `default_case_weight_lb`, else null — one `ANY(%s)` query per line for candidates + suggested product; the alias/exact conversion block now reads it instead of re-querying); suggestion chips and picker rows carry `data-case`, picks pass `case_size_lb` (`/products/search` already returned it). (3) `requirements.txt` declares `PyYAML==6.0.3` (matches `.venv-test`). **Codex cross-review fix (2026-09-09, same branch, second commit):** `applyProductPick` gated the master case size on `lb_source` (`!== 'manual'/'unit_is_lb'`), but `applyUnitChange` resets `lb_source` to `'none'` — so an unresolved CASE line changed to LB, quantity typed 2, then a 10 lb/case product picked previewed 20 lb and enabled Approve. Now the pick keys off the line's CURRENT normalized unit via new `unitIsLb()` (shared `LB_UNITS`; `SO_LB_UNITS` aliases it): an lb-unit line never takes a case size and, if a unit change dropped the conversion, gets its 1 lb/unit `unit_is_lb` identity restored so typed pounds carry through (2 lb); non-lb lines fill from the master unless `manual`. New node test reproduces the exact sequence (+ lbs/LB./pounds/# spellings, and LB→CASE still converts). intake-logic v10→11. Cache-busts js v57→58, intake-logic v9→11 (css untouched at v41). Tests: `tests/test_er_intake_logic.js` +3 (preserveResolved PO re-match; master case size on pick incl. fallback-to-typed, no-overwrite of manual, lb-line, ER unchanged; CASE→LB then pick = typed pounds), `tests/test_sales_order_extract.py` +1 (`test_suggestion_and_candidates_carry_master_case_size`). 47/47 node, 97 pytest (SO extract + JS wrapper + batch1 yaml guard) green. | Review found: editing the PO number re-ran the full merge, which wipes any human pick the (unchanged) alias/exact result does not confirm — i.e. every fuzzy/none line the user had resolved by hand was thrown away on a PO typo fix; a suggestion-chip or picker pick left case size empty so the user had to type the master value by hand on every fuzzy line; `import yaml` in `tests/test_batch1_correctness_security.py` relied on an undeclared transitive package. | Reverting (1): PO edits silently discard resolved lines again (and a failed PO re-match resets them). Reverting (2): chip/picker picks stop converting cases→lb (`lb_source` stays `none`, line shows "needs lb"); the backend `case_size_lb` key disappears from candidates and the JS test for it fails. Reverting only the Codex fix (back to the `lb_source` gate): a CASE→LB unit change followed by a pick multiplies typed pounds by the master case size and lets it be approved. Reverting (3): a fresh install of the test venv fails on `import yaml`. | `dashboard/intake-logic.js`, `dashboard/dashboard.js`, `dashboard/index.html`, `main.py`, `requirements.txt`, `tests/test_er_intake_logic.js`, `tests/test_sales_order_extract.py` |
| 126 | 2026-09-08 | Dashboard phone entry screen — IMP-069 / IMP-070 / IMP-072 / IMP-073 + the phone-header half of IMP-038 (renumbered from a colliding 119 on the 2026-09-09 rebase onto post-#34/#35 `main` `71d1674`, row-28 precedent; branch `feat/mobile-entry-screen`, originally based on `main` at `b216d6f` — **DEPLOYED 2026-09-09 ~09:45 ET**: PR #33 squash-merged to main as `8b391ab`; Netlify deploy 6aa162d236b12f00084fe7a3 ready 13:44:50Z, live index serves css v41 / js v57 / mini-calendar css v5 / js v6 / intake v9 / shell-layout v3; Railway /health 200 (no backend files in the merge); remote branch deleted; static dashboard assets and audit docs only, `main.py`, migrations and GPT instructions untouched) | Everything is scoped to `@media (max-width: 520px)` except two visually-hidden spans. **IMP-069** `b8c3283`: below 520px `mini-calendar.js` renders a compact form instead of the three-month strip — a 44px row (*Today · Tue, Sep 8*, a pill with today's open-SO count when non-zero, a ▾) that expands the current month inline with `--hit-min` prev/next and 44px day `<button>`s whose tap reads the day's ship count into an `aria-live` caption; open state and selected day survive the re-render a ship-count refresh triggers; a `matchMedia('(max-width: 520px)')` listener re-renders across the breakpoint. `renderStrip()` (desktop) is byte-identical; the old ≤520px CSS block is replaced by the compact rules. **IMP-038 (phone header only)** `ca9a8e4`: `.app-header` is a four-row grid at ≤520px — title + health badge, theme / Refresh / Updated at 44px, the collapsed calendar, search — with `.header-left` / `.header-right { display: contents }`; Needs Attention starts at y=357 of 844 with all seven chips above the fold (was y≈690). **IMP-073** `ca9a8e4`: `#health-badge` holds `<span class="health-label">Health</span> <span class="health-score">` — label "Health:" visible at every width after the rebase (post-#34 `main`'s desktop badge is a dialog-opening button reading "Health: N/100", so the sr-only desktop hiding was dropped); `refreshHealthBadge` writes into `.health-score` via `setHealthScore()`. **IMP-072** `ca9a8e4`: `formatShipByDate(dateStr, overdue)` adds an `#i-alert` glyph (`.ship-by-flag`, shown ≤520px) and sr-only ", overdue" text (every width) to the weekday line; both callers pass their overdue flag. **IMP-070** `ca9a8e4` + `731ca2a`: `white-space: nowrap` on `td .order-link`, `td .lot-link`, `td .er-reference` (new span round the ER reference / PO), `.order-number`, `.supply-lot-code`, new `.supply-reference` — inside the ≤520px block. `.ship-by-weekday` and `.health-badge` are `position: relative` to contain the sr-only spans. Cache-bust (rebased past live css v40/js v56): `dashboard.css?v=40→41`, `dashboard.js?v=56→57`, `mini-calendar.css?v=3→5`, `mini-calendar.js?v=4→6` (v4/v5 were used mid-branch; the four pages now reference v5/v6). **Rebase integration over #34/#35**: shell.js wraps the calendar in a `details.reference-calendar` disclosure and hides the header h1 at ≤768px, so the ≤520px header grid is re-templated to three rows ("cal cal health / theme refresh updated / search") with the disclosure — not the strip — in the cal cell; IMP-069's compact month renders inside the opened panel (shell-layout's 20px panel day-cell floor scoped to 521–768px; panel widened to calc(100vw-16px)/8px padding at ≤520px so seven 44pt day columns fit); the health badge keeps main's dialog-button behavior and gains min-height 44px at ≤520px (fixes a pre-existing 30px TOUCH-003 fail on main); harness `tab()` navigates via `.mobile-nav` when `.tab-bar` is hidden and S-02/S-02b open the disclosure first. | Phone review of production at 390px (IMP-068…073): three months of 8px display-only digits dominated the first screen and pushed Needs Attention below the fold; the header took four rows; the health badge was a bare number with only a tooltip; SO numbers wrapped at their hyphens across three lines; the overdue weekday was red-only. 384-capture re-run against `main`: TOUCH-003, ACCESS-008, LAYOUT-003 and LAYOUT-011 **cell-identical** (116 / 0 / 12 / 0 failing cells before and after); LAYOUT-020 moved 9 cells, 4 one way and 5 the other, all in the S-07/S-10/S-16/S-17/S-19/S-22 set that flips on unmodified `main` between runs (S-16/S-17 at 390 fail 7 of 12 baseline runs on the same 0.47 shift — the Activity sections emptied and refilled on refresh, SYS-2). Desktop pixel diff with `Date` frozen: 252 desktop-tier captures (1440 light/dark, 720@2× light/dark), 469 differing pixels in total, max channel delta 1/255 — the same anti-aliasing noise two captures of `main` show against each other (363 px). | **The nowrap rule must stay inside the ≤520px block until someone accepts the desktop re-flow**: at 1440px the SO # column already wraps `SO-` / `1421` on `main`, and a width-independent nowrap changes the Orders table's column widths (this is how the rule was first written and caught). **`.ship-by-weekday { position: relative }` and `.health-badge { position: relative }` must stay with the sr-only spans** — without them the absolutely-positioned hidden text escapes `.table-scroll` and widened the Expected Receipts tab by 259px at 390px (first cut, caught by LAYOUT-003). **`setHealthScore` and the two badge spans move together**: `badge.textContent = score` would wipe the label. **`.header-right { display: contents }` needs every grid-area assignment below it** — dropping the areas leaves the header's children auto-placed in a three-column grid. **`.mini-calendar-caption { white-space: normal }` must stay** — `.header-right`'s `nowrap` inherits through `display: contents` and the caption becomes one line that widens the document past 390px (caught in PR review; harness screen S-02b now measures the expanded month). The compact calendar depends on `matchMedia`; without it `isCompact()` is false and the desktop strip renders at every width (the old behaviour, not a crash). **shell-layout's `.reference-calendar-panel .mini-calendar-day { min-height:20px }` must stay scoped to 521–768px** — at ≤520px it would shrink the compact month's 44px tappable days back to ~23px (how the rebase was first caught). `.ship-by-flag` is `display: none` above 520px on purpose — lifting it shows the glyph on desktop, a deliberate follow-up. Not bumping `?v=` on a future edit to these assets serves stale cached copies against fresh HTML. | `dashboard/mini-calendar.js`, `dashboard/mini-calendar.css`, `dashboard/dashboard.css`, `dashboard/dashboard.js`, `dashboard/index.html`, `dashboard/sankey.html`, `dashboard/process-flow.html`, `dashboard/traceability.html`, `docs/design/audit/06-browser-check.md`, `docs/design/audit/IMPROVEMENTS-MASTER.md`, `docs/design/audit/pr-screenshots/feat-mobile-entry-screen/`, `tests/visual/lib/screens.mjs` |
| 125 | 2026-09-09 | Design audit Batch 4 — **DEPLOYED 2026-09-09 ~09:04 ET** (PR #35 squash `a1b592f`; Netlify deploy 6aa1594946e5d200089c2309 ready, live index serves css v40 / shell-layout v3; Railway rebuild of identical code, /health 200; remote `fix/design-audit-4` deleted) | CSS-only readiness wrapping below remaining quantity, compact phone order toolbar, themed native controls and history padding; dashboard CSS 40 / shell CSS 3. 59 Node + 12 Python tests passed; local fixture review/paperclip and before/after checks passed at 390/1440 light/dark. | Four findings from the live production audit. | Preserve full-width readiness below quantity and separate checkbox click guard; keep minimum action targets, intake sticky footers, native control semantics and theme tokens. No application JS behavior or backend changes. | `dashboard/dashboard.css`, `dashboard/shell-layout.css`, CSS references in five HTML pages, `tests/test_recent_ledger.py`, fixture browser harness |
| 124 | 2026-09-08 | Design audit Batch C — **DEPLOYED 2026-09-09 ~08:45 ET** (PR #34 squash `7270d2a`; Netlify 6aa154c0 serving js56/css39/intake9; Railway e2e131e4 rebuild of identical backend) | Shared type scale; accessible persistent sorting/resizing, labelled clearing and visible view choices; known-note-reference/supplier selection; compact notes/feed; shareable product/lot/order/history paths; flow relationship table; production refresh/run links; offset-aware production dates. 46 Node + 15 Python tests; local read-only browser checks. | Findings 13, 14, 17, 18, 19, 21, 23, 24, 25, 26, 27, 29, 30, 32, 34. | Keep design-controls.js loaded before dashboard/inline scripts, and shell assets together. Keep parent/detail row groups and dispatch-default exceptions when sorting; do not format literal lot codes as dates. Do not append Z to an offset-aware timestamp. Preserve Batch A quantity/intake/readiness semantics, Batch B navigation and product IDs. Type-size finalization and real-device validation remain open; no backend changes. | `dashboard/design-controls.js`, `dashboard/history.html`, `dashboard/history.js`, dashboard CSS/JS and reviewed pages, `tests/test_batch_c_ui.js`, `tests/test_recent_ledger.py`, `docs/design/audit-2026-09-08-batch-c.md` |
| 123 | 2026-09-08 | Design audit Batch B — **DEPLOYED 2026-09-09 ~08:45 ET** (PR #34 squash `7270d2a`, with Batch C) | Shared global search and reference-calendar reveal; mobile primary navigation and quick actions; responsive order cards and distinct 44 px controls; contextual trace-lot buttons; mobile receipt action bars; explicit-column Material Flow labels and phone flow cards. 15 Python + 40 Node tests passed; read-only browser checks at four widths. | Findings 2, 3, 4, 5, 6, 7, 15, 16, 20, 22, 31. | Keep shell asset includes together: dashboard delegates search to shell.js. Preserve keyboard result identity, stale-response suppression, existing record-opening paths, ready-label click guard, explicit Sankey columns/padding, all Batch A gates and purchasing-unit semantics. No backend or production data changes; C remains deferred. | `dashboard/shell.js`, `dashboard/shell-layout.css`, dashboard/index/sankey/process-flow/traceability assets, `tests/test_recent_ledger.py`, `docs/design/audit-2026-09-08-batch-b.md` |
| 122 | 2026-09-08 | Batch A deployment verification | User-approved merge advanced main to 32b96d2; read-only live checks now show JS v53 / CSS v36 and request_unit in the Supplies API. | Closes row 120’s pending deployment status. | Preserve all Batch A semantics and count/purchasing-unit exceptions from row 120. | Batch A commit 32b96d2; no new production writes |
| 121 | 2026-09-08 | AI-assisted sales-order intake (renumbered from a colliding 119; rows 119/120 = design-audit session, row-28 precedent) — migration 050, extraction kind='sales', 3 endpoints, New Sales Order modal (branch `feat/so-intake`, commits `31b45c4`/`ca3442d` + Phase 3, based on `main` at `bd974ae` — **DEPLOYED 2026-09-08 ~18:30 ET**: migration 050 applied to prod (session pooler) BEFORE merge fea70ba (conflicts vs Batch A 32b96d2 resolved: cache-busts renumbered css v37 / js v54, aria-label + paperclip combined, row renumbered 119→121); schema re-dumped f3bacdf; Railway dd0ebb2e SUCCESS 18:28 + Netlify serving v54/intake-logic v8; live-verified (3 routes 401 no-key, match 200 w/ dashboard key, orders list carries customer_po/source_document_id, POST /sales/orders still 403 on dashboard key, ER upload path healthy); design + owner rulings 1–10: `docs/designs/sales-order-intake.md`) | Customer PO (PNG/JPG/PDF ≤15 MB, or ⌘V paste) dropped into the NEW "New Sales Order" modal on the Sales Orders tab (no create modal existed — creation was GPT-only) → `POST /sales/orders/extract` stores the file through the SAME `_upload_intake_document()` pipeline as ER intake (`purchase_documents.document_kind` 'purchase'\|'sales', sha256 dedupe scoped per kind) → kind-aware `POST /purchase-documents/{id}/extract` (`extraction.extract_purchase_document(..., kind='sales')`: buyer-not-vendor customer_name, customer_item_code, unit_price ≥0-or-null) → `POST /sales/orders/match` (deterministic: `customer_product_aliases` code→description latest-wins → exact with a private-label leak guard [an exact text hit on another customer's private_label SKU outside the prior-sales pool is demoted] → fuzzy restricted to the customer's prior-sales pool [cancelled orders AND lines excluded] → human; cases→lb via alias/product `case_size_lb` only for alias/exact; lb-unit lines convert at 1 lb/unit; response carries `prior_sales_product_ids` + per-product `label_type`/`prior_sales` for the picker) → review screen (shared `intake-logic.js` state machine — renamed from `er-intake-logic.js`, no shim; customer dropdown re-matches on change; required Customer PO with dedupe banner + force; stacked line cards with qty×unit → case size → order lb → unit price [price stored only when basis is unambiguous: per-case on cases lines, per-lb on lb lines — else "basis unclear — not stored"]; full-catalog picker, prior-sales products first, amber "Private label — first sale" warning, never a block) → `POST /sales/orders/extract/approve` creates the order ATOMICALLY via the new `_create_sales_order_core()` (factored from POST /sales/orders — manual/GPT path byte-identical incl. response shape), `status='confirmed'`, `customer_po` + `source_document_id` stamped, per-line notes "PO <po>: <customer wording> — qty unit", aliases upserted latest-wins, advisory lock on (customer, normalized PO) with 409 DUPLICATE_PO warn+force, server-side ALIAS_CONVERSION_MISMATCH + PO_REQUIRED. Paperclip → signed URL on sourced order rows (GET /sales/orders now returns customer_po + source_document_id). 3 new DASHBOARD_KEY_ALLOWLIST entries; `POST /sales/orders` itself deliberately stays OFF the allowlist (it auto-creates customers). `/products/search` additionally returns `label_type`. Cache-busts `dashboard.css?v=36`, `dashboard.js?v=53`, `intake-logic.js?v=8`. 88 tests in `tests/test_sales_order_extract.py` (incl. two-real-connection same-PO approve race + readonly tripwire) + 44 node logic tests; suite 573 passed + known pre-existing test_recent_ledger failure; `tests/test_expected_receipt_extract.py` untouched and green (ruling 9). Browser-verified end-to-end on local uvicorn + faked extractor/storage (alias/fuzzy/human lines, PL-warning pick, duplicate→force flow, alias learning loop, 500/380px widths, no console errors; test rows cleaned from the test DB). | Customer POs arrive as photos/PDFs and were retyped into the office GPT line by line; PO numbers lived in notes free text (no dedupe possible), and nothing prevented a fuzzy product suggestion from surfacing another customer's private-label SKU. | **DEPLOY ORDER: migration 050 must be applied to prod BEFORE this branch deploys** — the shared upload INSERT names `document_kind` and GET /sales/orders selects `customer_po`/`source_document_id`, so BOTH the ER intake upload and the dashboard orders tab 500 against a pre-050 schema. Reverting the per-kind sha256 dedupe scope makes a customer PO resume a vendor-PO document (wrong extractor). Reverting the exact-tier private-label guard or the prior-sales fuzzy restriction leaks private-label SKUs across customers. Loosening approve's `document_kind='sales'` check lets vendor POs become sales orders. Renaming/aliasing `intake-logic.js` back needs every cache-bust re-bumped — index.html loads it by the new name only. The purchase path must keep calling `extraction.extract_purchase_document(content, mime)` with TWO args (ER test fakes pin that signature). `tests/schema/schema.sql` carries a pending `\ir 050` block until the prod apply + re-dump. | `migrations/050_sales_doc_intake.sql`, `extraction.py`, `main.py`, `dashboard/intake-logic.js` (renamed from `er-intake-logic.js`), `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `tests/test_sales_order_extract.py`, `tests/test_er_intake_logic.js`, `tests/test_er_intake_logic_js.py`, `docs/designs/sales-order-intake.md` |
| 120 | 2026-09-08 | Approved design audit Batch A — LOCAL ONLY, NOT DEPLOYED | Normalize weight-labelled ledger units while preserving counted supplies and purchasing-unit requests; gate manual expected receipt Save; distinguish Factory Ready from advisory dispatch checks; independently load dispatch attention; add accessible health details and control labels; clarify operational wording and production zero/unavailable states. JS v53, CSS v36, inline page links v2. 63 Python + 40 Node tests passed; browser fixture checks passed. | Findings 1, 8, 9, 10, 11, 12, 28, 33. DB trace proved Recent Entries mixed pounds with case UoM; Activity matched checked ledger shipments. | Reverting restores mislabeled weights, ambiguous readiness and unknown counts, ungated Save and unlabeled controls. Keep native count units, separate request_unit purchasing units, signed effective ledger/correction values, advisory shipping policy, and divergent closed-order visibility. B/C remain deferred; no production data writes or migrations. | `main.py`, dashboard JS/CSS/index/process-flow/traceability (sankey navigation versions only), `tests/test_recent_ledger.py`, `tests/test_supplies.py`, `tests/test_batch_a_ui.js`, `docs/design/audit-2026-09-08-batch-a.md` |
| 119 | 2026-09-08 | Design audit documentation (not deployed) | Imported original 34-finding Codex audit and high-priority browser recheck verbatim; three-batch triage and implementation approval pending. | Preserves evidence and rule IDs for scoped remediation. | Removing reports loses the reviewed baseline; no runtime behavior changed. | `docs/design/audit-2026-09-08-codex.md`, `docs/design/audit-2026-09-08-recheck.md` |
| 118 | 2026-09-08 | ER-intake review screen: card layout + match/lb-source honesty (dashboard-only, no backend change — **DEPLOYED 2026-09-08** with this push) | Live-smoke fixes to the review screen (live case: "tote of honey" / Dutch Gold Honey / fuzzy 43% → Honey 11030, prod purchase_documents row 1). Behavior: an explicit product pick (suggestion click or typeahead) now sets `match_source='chosen'` — the amber "Fuzzy n%" badge can never coexist with an enabled Approve; "Change" restores the server's verdict (`server_match_source`/`server_confidence` kept on each line), including after a failed re-match (`applyRematchFailure`). `qty_lb_source` is now only `'computed'` (qty × lb/unit) or `'manual'` (typed directly) — editing lb/unit no longer stamps the untouched expected-lb field "manual"; qty/unit carry a `qty_source` tag (`from document`/`edited`). lb/unit + expected-lb inputs have NO placeholders ("?"/"required" removed — INPUT-022) and each empty value shows an amber "needs lb" flag. Footer totals name the blockers while Approve is disabled ("N line(s) need a product or lb" — FEEDBACK-008). Verified: untouched fuzzy line CANNOT enable Approve (node repro of the exact live /match payload; prod created zero ERs during the smoke — the button was disabled, but only the app-standard 0.55-dim + dashed outline marked it). Layout: the per-line table (8 columns, modal widened to 940px, horizontal scroll at real window sizes) replaced by stacked cards — vendor text verbatim + badge / product row (name + code + Change, or picker + suggestions) / one quantity row qty × unit → lb/unit → expected lb with a source tag beneath each / Include + Save alias footer row; header is a compact 2-column grid (supplier full-width, reference + date beside each other); totals + Start over/Approve sticky at the panel bottom (body bottom-padding dropped in review mode so cards can't peek beneath); `.er-modal.er-reviewing{max-width:940px}` removed — modal stays ~520px, zero horizontal scrolling verified in Chrome at 500px and 380px modal widths (local uvicorn harness, faked extractor/storage, end-to-end approve exercised then cleaned from the test DB). Cache-busts `dashboard.css?v=35`, `er-intake-logic.js?v=7`, `dashboard.js?v=52`. 35/35 node logic tests; suite 485 passed + known pre-existing test_recent_ledger failure. | Owner's live smoke read the review screen as "fuzzy suggestion enabled Approve": the Fuzzy badge survived a product pick, a manual lb/unit edit labeled the untouched expected-lb field "manual", placeholder text sat inside empty lb inputs, and the 8-column table forced horizontal scrolling in a ~500px modal with the approve state ambiguous. | Reverting `match_source='chosen'` on pick resurrects the Fuzzy-badge-with-enabled-Approve ambiguity; restoring input placeholders re-violates INPUT-022 (hint mistaken for value); restoring `qty_lb_source=lb_source`/'manual' plumbing re-labels untouched fields; bringing the table back reintroduces horizontal scroll at ≤500px (LAYOUT-003). `server_match_source` must keep being set in `buildReviewLine` — `clearChosen`/`applyRematchFailure` read it. | `dashboard/er-intake-logic.js`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `tests/test_er_intake_logic.js` |
| 117 | 2026-09-08 | AI-assisted expected-receipt intake (renumbered from a colliding 115; rows 115/116 = hit-target/contrast sessions, row-28 precedent) — migration 049, extraction module, 5 endpoints, dashboard review flow (branch `feat/er-intake`, commits `2668905`/`37c61f3`/`cf429f0`/`c837a8e` + Phase 3, based on `main` at `4a532ff` — **DEPLOYED 2026-09-08**: migration 049 applied to prod (session pooler) BEFORE merge 1c795a9 → Railway + Netlify live, owner smoke passed; 3 audit rounds closed (docs/designs/er-intake-audit-{1,2,3}.md); design: `docs/designs/expected-receipt-intake.md`) | Vendor PO/confirmation (PNG/JPG/PDF ≤15 MB) dropped into the New Expected Receipt modal → `POST /expected-receipts/extract` inserts a `purchase_documents` row FIRST, then uploads to private Supabase Storage bucket `purchase-documents` (auto-created), then calls `extraction.extract_purchase_document()` (Anthropic forced tool-use, model `claude-sonnet-5` / `EXTRACTION_MODEL` env, nullable ref/date/unit fields, never-guess + verbatim-description prompt; `anthropic==0.69.0` + `httpx==0.27.2` pinned — the 1.x SDK's vendored httpx2 needs anyio≥4.10 and conflicts with FastAPI 0.104.1) → `POST /expected-receipts/match` (deterministic, no LLM: `supplier_product_aliases` → exact → strong fuzzy → V1-stubbed AI candidate pick → human; product-master lb conversion ONLY for alias/exact; fuzzy/none keep expected_qty_lb null until a human confirms) → review screen (supplier candidates + create-supplier, duplicate banner across ALL statuses with force override, per-line product picker, lb-source tag next to every computed value) → `POST /expected-receipts/extract/approve` creates one ER per line ATOMICALLY via the new `_create_expected_receipt_core()` (shared with manual POST /expected-receipts), upserts aliases latest-wins, stamps the document approved. Also: retry endpoint `POST /purchase-documents/{id}/extract` (extraction failure keeps file+row), signed-URL endpoint, `expected_receipts.source_document_id` + paperclip link on ER rows, 5 DASHBOARD_KEY_ALLOWLIST entries, `dashboard.js?v=43→44` / `dashboard.css?v=30→31`. 88 tests in `tests/test_expected_receipt_extract.py`; suite 400 passed (+1 pre-existing test_recent_ledger failure, also on clean main); browser-verified end-to-end on local uvicorn + faked extractor/storage. | Expected receipts had ZERO rows ever entered (2026-08-24 traceability audit) — manual line-by-line entry was the adoption blocker; supplier wording→product mapping was re-derived by a human on every PO. | **DEPLOY ORDER: migration 049 must be applied to prod BEFORE this branch deploys** — main.py selects `er.source_document_id` in EXPECTED_RECEIPT_SELECT_SQL, so the Expected Receipts endpoints 500 against a pre-049 schema. Reverting the row-first order in /extract lets readonly-armed requests write orphan Storage objects. Reverting `_create_expected_receipt_core()` forks manual vs intake creation semantics. Removing the alias-before-fuzzy order or the alias upsert breaks learned matching (aliases stop winning / stop updating). Loosening the product-master lb gate to fuzzy matches auto-converts pounds for products a human never confirmed. Unpinning anthropic/httpx breaks the Railway build (anyio conflict) or TestClient. Env prerequisites: `ANTHROPIC_API_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` on Railway. `tests/schema/schema.sql` carries a pending `\ir 049` block until the prod apply + re-dump. | `migrations/049_purchase_doc_intake.sql`, `extraction.py`, `main.py`, `requirements.txt`, `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `tests/test_expected_receipt_extract.py`, `docs/designs/expected-receipt-intake.md` |
| 116 | 2026-09-08 | Dashboard contrast — systemic ACCESS-008 clusters A1/A4/A5/A7/A8 + IMP-068 (branch `fix/systemic-contrast-c`, based on `main` at `feefa17` — **PR open, NOT merged/deployed**; static dashboard assets and audit docs only, `main.py`, migrations and GPT instructions untouched) | One commit per cluster. **A5** `f99553b`: new token `--primary-text` (dark `#669ef8`, light `#195aea`) in both `dashboard.css` theme blocks and the `:root` of `sankey.html`, `process-flow.html`, `traceability.html`; `.tab.active` (colour + `border-bottom-color`), `.lot-link`, `.order-link`, `.show-more-btn`, the three pages' `a` rule, every `:focus` border / outline that was on `--primary` and the selected day-card inset ring now read it. **A4** `001f97b`: dark `--primary` `#3b82f6` → `#1b6df5` (light stays `#2563eb`); the four hard-coded `.site-nav-link.active { background:#3b82f6 }` copies and process-flow's `.data-failure-retry` read `var(--primary)`. **A1** `4578b2f`: `opacity: 0.5–0.65` removed from `.note-card.done`, `tr.order-row.so-ready`, `.allocation-row.allocation-status-released/superseded/shipped`, `tr.er-inactive td`, `.supply-request-done td`, the two empty-state icons and the depleted-lots `<table>` string in `dashboard.js`, each replaced by `color: var(--text-muted)` (or `--text-dimmed`) on the text; `.so-ready-pill` `#86efac` → `var(--badge-green-text)`; `button:disabled` in `interaction.css`, `.btn-refresh:disabled`/`.btn-sm:disabled` and `.btn-refresh.loading` keep their opacity and add `outline: 1px dashed currentColor; outline-offset: -3px`. **A7** `ef75588`: `::placeholder { color: var(--text-muted); opacity: 1 }` in `dashboard.css` and on traceability's `.search-input`; `.form-group input[type="number"]` added to the field rule. **A8** `cea45fe`: `[data-theme="light"]` token blocks in the three chart pages plus a `<head>` script setting `data-theme` from `localStorage.dashboard-theme`; their hard-coded banner / badge / card / danger colours tokenised in both themes; process-flow `--text-muted`/`--text-dim` `#888`/`#555` → `#989898`, `--badge-idle-text` → `#a7a7a7`, `--danger-text` `#e46868`, `--badge-active-bg` `#188462`; traceability `--text-dimmed` `#64748b` → `#8392a6`, `--danger` → `#f16161`, `colorHex` read from the `--col-*` tokens, print block on the light palette; sankey `--text-dimmed: #929fb1`; scheduler `--amber` `#b46a00` → `#9f5e00`, `--idle` → `#697380`, `--ok` → `#2d7a4d`, new `--ink3: #616b7d` for `.dtl-breakdown`, `::placeholder` on `--ink2`, print blackens the topbar accents. **IMP-068** `e171ed9`: `overflow-wrap: anywhere` removed from `.att-label`; `@media (max-width: 420px) { .attention-strip { grid-template-columns: 1fr } }`. Cache-bust `dashboard.css?v=31→32`, `dashboard.js?v=44→45`, `interaction.css?v=1→2` on all five pages (`d65bfff`). | ACCESS-008 was the last open contrast rule: 208 of 384 captures failed, five root causes (`07-systemic-clusters.md`): an opacity veil on text rows (worst 1.16:1), `--primary` serving as both a fill and a text colour, unstyled placeholders (2.69:1), and three pages with no light palette. Re-run: **ACCESS-008 208 → 0 failing cells (rule closed)**, TOUCH-003 116→116, LAYOUT-003 12→12, LAYOUT-011 0→0 cell-identical, LAYOUT-020 ±4 cells of the pre-existing clock-dependent header shift. IMP-068 is the mid-word label break seen on a phone in production. | **`--primary` and `--primary-text` must stay split** — putting text back on `--primary` returns dark links to 3.17:1 (worse than before A4), and putting fills on `--primary-text` puts white on `#669ef8` at 2.7:1. **The focus rings must stay on `--primary-text`**: the A4 fill value is 2.69:1 on `--search-bg`, below the 3:1 non-text floor. **The three pages' head script and their `[data-theme="light"]` blocks move together**: without the script the light block is dead code in production; without the block the script does nothing. `colorHex` in traceability reads tokens through a Proxy — restoring the literal table leaves the light graph on dark-surface colours. Restoring any `opacity` on a text row re-fails every cell in it, and `.so-ready-pill` needs a per-theme colour (its old `#86efac` fails at 1.5:1 in light even without the veil). Removing the dashed outline from `button:disabled` leaves the disabled state carried by a translucent colour alone. Restoring `.att-label { overflow-wrap: anywhere }` reintroduces the mid-word breaks at 390 px. Not bumping `?v=` on a future edit serves stale cached copies. | `dashboard/dashboard.css`, `dashboard/dashboard.js`, `dashboard/interaction.css`, `dashboard/index.html`, `dashboard/sankey.html`, `dashboard/process-flow.html`, `dashboard/traceability.html`, `dashboard/scheduler/seven-wells-production-board.html`, `docs/design/audit/06-browser-check.md`, `docs/design/audit/IMPROVEMENTS-MASTER.md` |
| 115 | 2026-09-08 | Dashboard hit targets — `--hit-min` token, systemic clusters T1/T2/T3/T4/T6/T7/T8/T9/T10/T12 (branch `fix/systemic-touch-b`, based on `main` at `4a532ff` — **PR open, NOT merged/deployed**; static dashboard assets and test tooling only, `main.py`, migrations and GPT instructions untouched) | One token `--hit-min: 44px` in `dashboard.css`'s `:root` and in the `:root` of `sankey.html`, `process-flow.html`, `traceability.html`. **Standalone controls** take it directly: `min-height` on `.btn-sm`, `.btn-refresh`, `.tab`, `.supplies-subtab`, `.notes-filter-btn`, `.btn-secondary`, `.btn-back`, `.site-nav-link` (made `inline-flex` so `min-height` applies), the traceability `.trace-btn` / `.dir-btn` / `.export-btn` / `.search-input`, and every `input` / `select` / `textarea` through one `:where()` rule; `min-width` + `min-height` on `.btn-theme`, `.btn-close`, `.site-nav-toggle`, `.graph-ctrl-btn`, `#cal-prev` / `#cal-next`; `.mini-calendar-nav` width 30/26/24 → `var(--hit-min, 44px)` (`498a530`, `6bef15f`, `ae29903`, `dcf3b1a`). **Controls in dense rows** keep their box and carry an invisible `position:absolute` `::before` of at least 44 × 44 centred on them (`.er-actions .btn-sm`, `.allocation-release-btn`, `.order-inventory-toggle`, `.supply-request-done-btn`, `.order-expand-toggle`, `.note-action-btn`, `.show-more-btn`); `.note-action-btn.edit/.delete` and `.order-ready-cell .check-hit` regions are offset so no neighbouring glyph is covered. **Checkboxes / radios** stay native at 20 px with `accent-color`; the wrapping `<label>` is the 44 px region (`:where(label:has(> input[type=checkbox|radio]))` at `min-height`), and `dashboard.js` now wraps `.order-ready-checkbox` and `.note-checkbox` in `<label class="check-hit">` — the row-click `stopPropagation` guard moved to that label. Excluded on purpose: the order-line qty / price inputs in edit mode (`.order-edit-num-cell .order-edit-input { min-height: 0 }`). **Harness** (`d60fb08`): `checks.mjs` `touchTargets()` unions an absolutely-positioned `::before`/`::after` and a wrapping checkbox/radio label into the measured hit region, records `tag`/`cls` per failure and a `passedViaExtension` list. Cache-bust `dashboard.css?v=30→31`, `dashboard.js?v=43→44`, `mini-calendar.css?v=2→3` on all four pages. | TOUCH-003 was the largest open rule: 316 of 384 captures had at least one control under 44 pt because no minimum-hit-size rule existed anywhere in the CSS (SYS-4). Re-run: **TOUCH-003 316→116 failing cells (200 cleared), zero regressions on a per-cell diff of all five rules**; ACCESS-008 208→208, LAYOUT-003 12→12, LAYOUT-011 0→0 cell-identical; LAYOUT-020 ±3 cells of the pre-existing clock-dependent header shift. Row density held: 1,216 of 1,221 measured rows unchanged in height. | **The `.check-hit` wrapper and the click guard in `bindOrderReadyToggles` move together**: without the label the checkbox is 20 px again and the harness stops crediting it; without the guard on the label a tap in the 44 px region both toggles Factory Ready and opens the order detail. **The `::before` rule needs `position: relative` on its selectors** — dropping the first block leaves the pseudo positioned against the nearest positioned ancestor (the table scroller) and the harness's containing-block check rejects it. **The `:where()` rules must stay zero-specificity** — rewriting them as plain selectors would override every id-scoped field size and the `.order-ready-checkbox` green. **`.allocation-form input`'s `min-height`** must reference the token, not a px value below 44, or its class selector beats the shared rule. Reverting `d60fb08` alone leaves the CSS in place but reports every in-row control as failing again (the region is real, the old measurement could not see it). Restoring `.today-tile-retry { min-height: 36px }` is harmless (the later `.btn-sm` rule wins). Reverting the `mini-calendar.css` widths also needs `?v=` bumped again or phones keep the cached 44 px copy. Not bumping `?v=` on a future edit to these assets serves stale cached copies against fresh HTML. | `dashboard/dashboard.css`, `dashboard/dashboard.js`, `dashboard/mini-calendar.css`, `dashboard/index.html`, `dashboard/sankey.html`, `dashboard/process-flow.html`, `dashboard/traceability.html`, `tests/visual/lib/checks.mjs`, `docs/design/audit/06-browser-check.md`, `docs/design/audit/IMPROVEMENTS-MASTER.md` |
| 114 | 2026-09-08 | Dashboard design tokens + two media queries — systemic clusters A2/A3/A6/A9/O1/F1 (branch `fix/systemic-tokens-a`, based on `main` at `7c00d8e` — **PR open, NOT merged/deployed**; static dashboard assets and test tooling only, `main.py`, migrations and GPT instructions untouched) | Six colour-token values changed to clear WCAG AA 4.5:1 on every surface each was measured rendering on (`--danger` to 4.6:1 — its nearest 4.5-passing value `#f15d5d` lands at exactly 4.50 with no margin), and two media queries added. Tokens: light `--text-muted` `#64748b`→`#5b697e` (`86ea5a4`); `--text-dimmed` dark `#64748b`→`#909daf`, light `#94a3b8`→`#5b697e` (`d9d77b7`); dark `--danger` `#ef4444`→`#f16161` (`414f1ea`); light `--category-granola` `#a16207`→`#955b06`, `--category-graham` `#15803d`→`#147839` (`8674c0a`). Layout: `@media (max-width:768px) { .header { flex-wrap: wrap; gap: 8px } .header-right { width: 100%; flex-wrap: wrap } }` added to the inline stylesheets of `sankey.html`, `process-flow.html`, `traceability.html` (`163bd33`), and `@media (max-height:600px) { .app-header { position: static } .tab-bar { top: var(--site-nav-h) } }` added to `dashboard.css` (`70fe9a2`). Harness: the twelve-per-capture failure caps at `tests/visual/lib/checks.mjs:112` and `:256` removed (`e801df3`). `dashboard.css?v=29→30`. | 384-capture re-run: ACCESS-008 288→208 failing cells, LAYOUT-003/ACCESS-001 28→12, **LAYOUT-011 12→0 (rule closed)**, all rules 655→546, zero regressions on a per-cell diff. | **The `.tab-bar` rule inside the `max-height:600px` block must not be removed on its own.** `.tab-bar`'s offset is `calc(var(--site-nav-h) + var(--header-h))` and `--header-h` is published by the ResizeObserver at `dashboard.js:120-127`, which keeps measuring `.app-header` whether or not it is sticky — dropping the header to `position: static` without re-anchoring the tab bar leaves it stuck 249px below the nav with a gap. Take both declarations or neither. Reverting the three `.header` flex-wrap blocks also re-inerts `mini-calendar.css:136-143`, whose `order: 10; width: 100%` only does anything inside a wrapping flex container. Reverting the token values restores 80 ACCESS-008 failures. Reverting the `checks.mjs` caps does not change any verdict — only how much evidence `results.json` keeps. | `dashboard/dashboard.css`, `dashboard/index.html`, `dashboard/sankey.html`, `dashboard/process-flow.html`, `dashboard/traceability.html`, `tests/visual/lib/checks.mjs`, `tests/visual/lib/report.mjs` |
| 113 | 2026-09-07 | Dashboard entry screen — attention strip (IMP-030) (branch `feat/entry-attention-strip`, commit `ae01e06`, based on `main` at `a6b65e0` — **PR open, NOT merged/deployed**; static dashboard assets only, `main.py`, migrations and GPT instructions untouched) | A "Needs Attention" strip above Today So Far on the Operations tab, rendering all seven operational attention counts as chips that deep-link into the view that resolves each one: overdue expected receipts (Expected Receipts + "Overdue only"), overdue sales orders (Sales Orders, open scope + "Overdue only"), dispatch-blocked orders (Dispatch Queue + "blocked"), low-stock supplies (Supplies "All" sub-tab), open supply requests (completed hidden), to-dos due today or overdue (Notes "To-Dos" filter), and failed background refreshes (re-runs them). **No new API calls** — six counts are derived from data `refreshAll()` already fetches. Dispatch-blocked needs `/sales/orders/fulfillment-check`, which is fetched only while the Dispatch Queue filter is selected, so that chip reads "—" until the user opens the dispatch queue once and the real figure thereafter, rather than adding a request or printing a fabricated zero. Failed refreshes were previously discoverable nowhere (each loader catches its own error; `Promise.allSettled` in `refreshAll` swallows the rest) — a registry keyed on load-error element ids now mirrors exactly the set of load errors on screen, with modal/per-action error ids excluded by allowlist. `initTabs` grew an extracted `activateTab()` so the chips switch tabs through the same path as a tab click. FEEDBACK-011: no state carried by hue — the three states differ in grayscale by the numeral, the left-edge bar (dotted/2px/5px solid), the count's font weight, and a flag glyph shown only when non-zero. NOTIFY-010: a count is shown only when the data provably covers the whole population, otherwise the last honestly derived figure (labelled as such in the accessible name). LAYOUT-020: all seven chips ship in static markup with an em-dash placeholder, grid tracks are container-sized, the count track is a fixed 4.5ch and the printed value caps at "999+". Zero states are neutral — shown, not hidden, not red. Verified with a jsdom harness driving the real page through the real controls: 33 checks (all seven counts, three states, strip geometry stable across refresh, every deep link's tab + filter result, failure count rising and clearing, no uncaught errors); grayscale and light-theme rendering checked visually. Cache-busters `dashboard.css` v28→v29, `dashboard.js` v42→v43. | Every operational attention item sat behind a tab, several behind a tab plus a filter change, and there is no notification channel in this product (no Notification API, service worker, push, or email trigger) — so NOTIFY-011 carries the entire attention model and nothing was promoted. A failed background refresh was visible nowhere at all, meaning the other six counts could be silently stale with no indication. | Reverting returns the dashboard to an entry screen that shows no attention item, re-hiding overdue receipts, overdue and dispatch-blocked orders, low stock, supply requests and due to-dos behind tabs and filters, and makes failed background refreshes invisible again (NOTIFY-011, Hard rule). Removing the static chip markup or the fixed 4.5ch count track reintroduces a layout shift under the user on every refresh (LAYOUT-020, Critical). Deleting the flag glyph or the left-bar weights leaves colour as the sole state carrier (FEEDBACK-011, Hard rule). Replacing the dispatch-blocked "—" with 0 fabricates a badge count (NOTIFY-010). Dropping the allowlist in `REFRESH_ERROR_IDS` lets per-action failures (e.g. mark-request-done) inflate the refresh-failure count. | `dashboard/index.html`, `dashboard/dashboard.css`, `dashboard/dashboard.js` |
| 112 | 2026-09-07 | Dashboard error surfacing — IMP-044 + ERROR-010 (branch `fix/error-surfacing`, rebased onto `main` at `da76ffe`, which already carries Band 0 (PR #24) — **merged as PR #25 into `main` at `a6b65e0` on 2026-09-08 and deployed**; verified live — the deployed `sankey.html`, `process-flow.html`, `traceability.html`, `dashboard.js` and `dashboard.css` are byte-identical to `a6b65e0` apart from Netlify's pretty-URL rewriting of the nav links, and the `getFallback*` sample-data paths are confirmed gone. Static dashboard assets only, `main.py` and migrations untouched; sits on top of the merged `fix/ux-band-0` work, not independent of it) | Four commits. **`c972526`**: `traceability.html`'s `setStatus`, `suggestSimilar`, and `showDisambiguation` built `#statusBar` content with `innerHTML`, interpolating raw API error bodies (`Trace failed: ${e.message}`), typed lot codes, and the 409 disambiguation payload's `product_name` / `product_id` / `entry_source`. All three now build DOM nodes with `textContent` and bind real `addEventListener` handlers in place of inline `onclick` attributes. Grepped the other standalone pages: `process-flow` (`showError`/`showStale`), `sankey` (`showBanner`) already used `textContent`, and the scheduler escapes with `esc()` and makes no network calls. **`028c685`**: `process-flow`'s `apiFetch` threw `API <status>: <path>` and `sankey`'s threw `HTTP <status>` — both discarded the response body at the fetch boundary. Both now read it and throw `HTTP <status> <path>: <body>` with `error.status`, `error.body`, and a parsed `error.payload`, the shape `dashboard.js`'s `fetchSalesAPI` already uses; the banners render that verbatim in a selectable monospace `.banner-detail` block with a Copy button (`navigator.clipboard` + `execCommand` fallback, the scheduler's `copyScheduleText` pattern). **`684af02`**: deleted `getFallbackData()`, `getFallbackProductDetails()`, and `useFallback` from `process-flow` and `getFallbackLinks()` from `sankey`. A failure with no prior successful load renders an explicit `.data-failure` state (heading, a sentence saying the data source failed and that nothing shown is a real number, the verbatim error, Retry). `sankey` now tracks `lastSuccessTime`; a failed refresh with a prior success keeps the real chart under *“Refresh failed — showing data from &lt;time&gt;”*. `process-flow` keeps its stale banner, now with the verbatim error and a Retry, and only when a prior success exists. **`a6ae9d3`**: `supplyApiErrorMessage` returns `HTTP <status>: <message>` instead of dropping the status, and anchors its match at `^HTTP`. Cache-bust `dashboard.js?v=41` → `?v=42`, one above the value Band 0 left on `main`. | On an API failure both chart pages drew fabricated numbers — lots `GH-2026-0316` / `CS-2026-0087` and flows such as *“Graham Crumb 25lb → DOT Foods, 2,400”* — in the same styling as real data behind a small warning banner, on a page whose whole purpose is to be read for volumes; the Sankey case was also reachable from a plain window resize via the debounced refetch. Separately, the failure reason was thrown away at the fetch boundary, so no error surface on either page could say *why*, and traceability routed API-controlled text through `innerHTML`. | Restoring any `getFallback*` generator re-introduces invented production and shipment numbers presented as real data — this is the OTHER-003 hard-rule failure and the most dangerous single regression in this row. Reverting the `apiFetch` bodies-preserved change silently empties every `.banner-detail` block and both Copy buttons (they render only when a `detail` string is present), leaving the new failure states correct but uninformative. Reverting `setStatus` to `innerHTML` re-opens API-controlled text as markup in the traceability status bar. `renderDataSourceFailure` / `renderChartFailure` and the `lastSuccessTime` checks must move together: dropping `lastSuccessTime` from `sankey` makes every failed refresh wipe a good chart instead of keeping it. Removing the `HTTP <status>` prefix from `supplyApiErrorMessage` again loses the failure class in the one string users copy out. Not bumping `?v=` on a future `dashboard.js` edit serves a stale cached copy against fresh HTML. | `dashboard/traceability.html`, `dashboard/process-flow.html`, `dashboard/sankey.html`, `dashboard/dashboard.js`, `dashboard/index.html`, `docs/design/audit/IMPROVEMENTS-MASTER.md` |
| 111 | 2026-09-07 | Dashboard UX — design-audit Band 0 (branch `fix/ux-band-0` — **merged as PR #24 into `main` at `da76ffe` on 2026-09-08 and deployed**; verified live serving `dashboard.css?v=28` / `dashboard.js?v=42`. Static assets only, `main.py` and migrations untouched) | Six Critical improvements from `docs/design/audit/IMPROVEMENTS-MASTER.md`, one commit each. **IMP-001** `4a96cc7`: declared `--bg-card` in both theme blocks and moved the lot-disambiguation button styles into `.disambig-*` classes, removing `background: var(--bg-card, #fff)` (ACCESS-008, ~1.1:1 contrast). **IMP-002** `45be3eb`: replaced the hard-coded sticky offsets with `--site-nav-h`/`--header-h`, seeded at the old desktop values and kept current by a `ResizeObserver` on `.site-nav` and `.app-header`; `.tab-bar` uses `top: calc(var(--site-nav-h) + var(--header-h))` and the stale `.tab-bar { top: 91px }` in the `≤768px` block is gone (LAYOUT-003). **IMP-006** `44c5eb5`: `#er-qty` is `type="number"` with a strict `readNumericInput` and a blur check; the scheduler's ten `parseFloat` sites now go through `numIn`/`rejectNum`, including the pasted-import `qty`/`open_lbs` columns (INPUT-007/008). **IMP-004** `6083037`: new `dashboard/interaction.css` linked from all five pages, carrying the product's first `:active` rule plus `:disabled` and `.is-submitting`; the four commit paths (note save, note done, Factory Ready, expected-receipt close/cancel) disable and relabel while in flight (ACTION-002, FEEDBACK-001). **IMP-005** `c451682`: new `dashboard/fetch-timeout.js` (`window.FL.fetchWithTimeout`, 15 s default, `AbortController`, distinguishable `StallError`); all 13 `fetch` call sites in `dashboard/` route through it and a stall renders a message with a working Retry (FEEDBACK-003). **IMP-003** `4f34036`: shared `.table-scroll` wrapper with per-table minimum widths on the eight unwrapped tables (LAYOUT-003). Cache-bust bumps: `dashboard.css?v=28`, `dashboard.js?v=41`, `mini-calendar.js?v=4` on all four pages. Band 1+ items deliberately untouched. | Six Critical / Hard-rule failures from the UX audit: near-white-on-white at the lot-disambiguation decision point; the tab bar covering global search and Refresh on every phone and at 200% zoom; `parseFloat("12O")` committing 12 on an incoming-delivery quantity; no press state anywhere and four double-fireable commits; no fetch timeout, so a hung API left every loading state waiting forever; and wide tables scrolling the whole page out from under the fixed nav. | Reverting `interaction.css` or `fetch-timeout.js` (or unlinking either) leaves `FL` undefined and every `FL.fetchWithTimeout` call throws — the pages will not load data at all; the link/script tags and the files must move together. Restoring `.tab-bar { top: 91px }` re-hides the header on phones. Removing `initStickyOffsets()` is safe on desktop (the CSS defaults reproduce the old values) but re-breaks the wrapped-header case. Putting `parseFloat` back on any quantity input silently re-accepts `"12O"` as 12. Dropping the `.table-scroll` wrappers without also dropping the `min-width` rules is harmless, but dropping the wrapper `<div>`s in `dashboard.js` while keeping the CSS leaves the page scrolling horizontally again. Not bumping `?v=` on a future edit to these assets serves stale cached copies against fresh HTML. | `dashboard/interaction.css`, `dashboard/fetch-timeout.js`, `dashboard/dashboard.css`, `dashboard/dashboard.js`, `dashboard/index.html`, `dashboard/mini-calendar.js`, `dashboard/sankey.html`, `dashboard/process-flow.html`, `dashboard/traceability.html`, `dashboard/scheduler/seven-wells-production-board.html` |
| 110 | 2026-08-31 | Trace emission hooks — §9 step 3 (branch feat/trace-emission — NOT pushed/deployed; no migration: 048 tables already in prod) | TRACEABILITY_DESIGN §4/§3.4/§10 emission layer: new `emit_trace_event()` (fail-hard, aggregates quantities per (lot_id, role), drops zero-sum rows) + `trace_emit_enabled()` (TRACE_EMIT_ENABLED env flag, default ON when absent/unrecognized, read once per request) + `_txn_trace_times()`. Hooks at all §4 sites, each placed after ALL transaction_lines/ILC/allocation side-writes (incl. _expire_auto_fifo_allocations/_shrink_overallocated_products) as the last write before commit, never inside a best_effort_audit_insert savepoint: /receive commit (received +qty, source_party=shipper), /make commit (each ILC row → input −, output lot → output +, transformation), /pack commit (same shape incl. add-in ILC rows), /ship commit (each shipped lot → shipped −, destination_party/customer_id, after the inventory_shipped shrink), SO-ship commit (one event per per-line ship txn inside the loop, +sales_order_id), /adjust + both /inventory/found* (adjusted, signed; found paths emit OUTSIDE the audit savepoint), void/restore/amend markers appended at the end of `_append_transaction_correction` (correction_id set, no lot rows — covers /void/{id} and the corrections endpoint), /admin/lots/merge → one 'merge' marker per affected transaction (correction_id NULL per the 048 pairing CHECK; already-marked txns skipped so a second merge can't trip the NULLS-NOT-DISTINCT unique; response gains trace_merge_markers). occurred_at/business_date copied from the transactions row (RETURNING id, occurred_at, business_date at insert sites; SELECT for markers). operator_id left NULL (FR-15 interim rule for new tables). Previews emit nothing. Bundled housekeeping: schema.sql re-dumped post-048 (diff = 048 objects only) and test_trace_tables_048.py gained the REVERT_048_SQL scratch preamble (047 pattern). tests/test_trace_emission.py: 24 tests (per-endpoint commit/preview, fail-hard rollback proofs on /receive+/make, flag gate, void/restore/amend markers, double-fire uniq, 044 expire/foreign-pin interplay, merge markers + skip). Suite 344/344. | §9 step 3: from deploy forward every operational commit dual-writes its canonical trace event, closing CC-2 structurally for new data; without it the 048 tables stay empty and the step-4 backfill has no live layer to join. | Removing a hook silently breaks R1 coverage for that path (untraceable commits reported as success — the exact §4 failure mode). Wrapping emit_trace_event in try/except or a savepoint reintroduces best-effort trace loss. Moving an emit above the allocation side-writes lets roles diverge from the final deduction plan. Setting TRACE_EMIT_ENABLED=false is the sanctioned rollback: commits succeed, R1 shows the debt. Reverting the schema.sql re-dump breaks test_trace_emission.py (shared test DB loses the trace tables) and test_trace_tables_048.py's revert preamble. | `main.py`, `tests/test_trace_emission.py`, `tests/test_trace_tables_048.py`, `tests/schema/schema.sql` |
| 109 | 2026-08-31 | Trace tables schema — migration 048 (DEPLOYED 2026-08-31: migration 048 applied to prod ~15:4x ET via 5432 session-pooler write path BEFORE merge per deploy-order rule; read-only verification: both tables exist EMPTY, all 4 triggers present, trace_events_txn_type_uniq indnullsnotdistinct=t, ledger_enforce_created_at source contains trace_backfill_048 AND retains api_backfill, latest transaction (id 2146) created_at_source='database', /health 200 + keyed GET /lots/999 200 + dashboard daily-entries 200 on the old build; PR #22 merged as 165bf49; Railway deploy 8378758f SUCCESS, /health 200 + daily-entries 200 on the new build — app code unchanged, rebuild of identical behavior) | TRACEABILITY_DESIGN §3.2/§3.3, migration plan §9 step 2 — trace tables ONLY, no emission code, main.py untouched. `migrations/048_trace_tables.sql`: `trace_events` (event_uuid, event_type/epcis_type CHECKs, FK transaction_id→transactions, correction_id→ledger_corrections, occurred_at/recorded_at/business_date, GENERATED `late_recorded` = (recorded_at − occurred_at) > 24 h, party/location columns, correction-pairing CHECK `trace_events_correction_pairing` (void/restore/amend ⇔ correction_id set), `trace_events_txn_type_uniq` UNIQUE **NULLS NOT DISTINCT** (transaction_id, event_type, correction_id) — deliberate deviation from the doc's plain UNIQUE, which is vacuous for NULL correction_ids and would never catch a double-fired emission hook; 3 indexes incl. partial `trace_events_late_idx` WHERE late_recorded) + `trace_event_lots` (FKs to trace_events/lots, role CHECK, quantity_lb<>0, sign CHECK `tel_role_sign` (output/received>0, input/shipped<0, adjusted/moved either), `tel_event_lot_role_uniq`, 2 indexes). Both tables get the 039 protections (trg_*_created_at / trg_*_append_only reusing ledger_enforce_created_at / ledger_block_append_only_change). `ledger_enforce_created_at` REPLACED (same move as 046) adding the `trace_backfill_048` created_at_source carve-out on the two trace tables for the §9 step-4 backfill; on trace_events the marker also (and alone) unlocks a caller-supplied historical `recorded_at` — every other insert gets recorded_at forced to clock_timestamp() (else the 039-backfill-stamped created_at rows would read falsely late); created_at itself stays database-owned always; transactions/api_backfill behavior byte-identical. Idempotent (IF NOT EXISTS / CREATE OR REPLACE / DROP TRIGGER IF EXISTS). tests/test_trace_tables_048.py: 40 tests (fresh-apply + re-run no-op on scratch DBs, all CHECK/unique/sign combinations, 24 h boundary, append-only, carve-out acceptance + cross-table isolation + 046 regression guard, FK integrity); suite 320/320. | §9 step 2 of the trace migration plan: the emission hooks (step 3) and backfill (step 4) need the tables to exist first, deployable as a zero-risk inert step. | Once applied: dropping the tables breaks step-3 emission and orphans the step-4 backfill. Reverting `ledger_enforce_created_at` to the 046 version silently strips the trace_backfill_048 carve-out (backfill rows lose their provenance marker and historical recorded_at → 500+ rows falsely late). Downgrading `trace_events_txn_type_uniq` to plain UNIQUE lets duplicate operational events insert silently. Removing recorded_at trigger enforcement lets any caller fake entry time, corrupting late_recorded. | `migrations/048_trace_tables.sql`, `tests/test_trace_tables_048.py` |
| 108 | 2026-08-31 | Lot identity: lot_uuid + two-tier code normalization (DEPLOYED 2026-08-31: PR #21 merged as d237754; migration 047 applied to prod ~14:33 ET via session pooler 5432 BEFORE merge per deploy-order note; Railway deploy 10e28c90 SUCCESS 14:35 ET, /health 200; post-apply read-only verification: 1,048/1,048 lots with distinct non-null lot_uuid, lots_product_code_norm_uniq present with 0 violation groups, lots_code_format_chk convalidated=false; smoke: GET /lots/999 returns lot_uuid on new build, logs clean of T2-machinery errors. REMAINING FOLLOW-UP: remove the pending `\ir` 047 block from tests/schema/schema.sql after a prod schema re-dump via scripts/dump_prod_schema.sh) | TRACEABILITY_DESIGN §3.1 identity layer only (no trace tables/emission — that is 048). Migration `047_lot_identity.sql`: `lots.lot_uuid` uuid NOT NULL DEFAULT gen_random_uuid() + UNIQUE `lots_lot_uuid_key`; tier-1 HARD unique index `lots_product_code_norm_uniq` ON (product_id, upper+trim/collapse-whitespace+strip-trailing-'LOT' of lot_code) WHERE status IS DISTINCT FROM 'merged'; NOT VALID format CHECK `lots_code_format_chk` (new rows only, never VALIDATEd). main.py: `find_or_create_lot` pre-checks tier-1 twins → 409 (index UniqueViolation mapped to 409 as race backstop) and returns lot_uuid; tier-2 SOFT `suspicious_code_similarity` warning (aggressive key: also strip all non-alphanumerics) at mint time on /receive, /make, /pack, /inventory/found* — warning only, never blocks; rename gets the same tier-1 409 pre-check; lot_uuid added (additive) to receive/make/pack commit responses (`lot_uuid`/`output_lot_uuid`), found responses, rename, GET /lots/{id}, /lots/by-code, /lots/by-supplier-lot. No GPT schema change (30-op cap untouched). AMENDED same day: `normalize_lot_code_input()` (upper + btrim + collapse internal whitespace, NOTHING else — punctuation/'LOT' tokens stay as typed, tier-2 warns instead) now applied to every caller-supplied lot code before lookup/insert (receive/make/pack preview+commit, both found endpoints, rename), so lots_code_format_chk can never 500 on casing/whitespace and lookups hit the same lot the constraint sees. tests/schema/schema.sql carries a pending `\ir` 047 block (remove after prod apply + re-dump); tests/test_lot_identity_047.py (20 tests, incl. scratch-DB migration builds with merged-twin historical shapes + input-normalization coverage); suite 280/280. | Twin lot codes (case/whitespace/'Lot'-suffix variants of one physical lot, worklist Groups A/B) could mint separate lots; lots had no rename-stable machine identity for the QR/trace work. | Dropping the index re-opens twin minting; dropping lot_uuid breaks the (future) QR payloads and the 047 tests; VALIDATEing the CHECK breaks on ~historical codes; removing the index's merged-predicate makes twin-merges impossible to complete; new-row lot codes must match `^[A-Z0-9][A-Z0-9 ./#-]{2,49}$` (uppercase!) or inserts fail lots_code_format_chk; removing normalize_lot_code_input() lets lowercase/whitespace user input 500 on that CHECK and split lookups from the constraint's view of the code. | `migrations/047_lot_identity.sql`, `main.py`, `tests/schema/schema.sql`, `tests/test_lot_identity_047.py` |
| 107 | 2026-08-31 | Prod data: lot twin merges (data-only, no code/deploy; trace pre-clean §9 step 0) | Owner-approved hygiene merges via POST /admin/lots/merge, one at a time with read-only verification after each: lot 1003 `JUL 15 2026` → 999 `JUL15 2026` (product 107 Batch Classic Granola #9; 2 line corrections 0f144ec0…/8b994421…, merged_at 2026-08-31 17:49:06Z) and lot 401 `BB041327 Lot` → 410 `BB041327` (product 145 SS Chocolate Chip case; corrections 822671b9…/b8f77428…, merged_at 17:49:25Z). Both pairs zero on-hand before and after; survivor histories intact + absorbed; ILC unchanged (999 keeps 2 rows); 0 allocation moves. Post-merge strengthened normalized-code dry run (upper + strip trailing LOT + strip non-alphanumerics, excluding merged): 0 within-product twin groups — migration 047 index unblocked. Receipts: docs/trace-preclean-worklist-2026-08.md §4. | Normalized-code twin lots (typo'd space / trailing "Lot" word) split single physical lots into two rows, colliding under the planned 047 strengthened unique index. | Un-merging (manual status flip without moving lines back) would corrupt lot histories; sources 1003/401 must stay status=merged with merged_into_lot_id set. Lot 1003 retains code `JUL 15 2026` — renaming survivor 999 to that code requires renaming 1003 aside first (UNIQUE product_id+lot_code has no merged predicate in the DB today). | prod data only; docs/trace-preclean-worklist-2026-08.md |
| 106 | 2026-08-31 | Audit-insert savepoints + /make advisory lock + lot-code sequence fix (branch fix/audit-insert-savepoints — merged to main via PR #20, merge commit e1ba515; deployed to Railway as e1ba515, deploy SUCCESS health 200 on 2026-08-31) | Problems 1–3 of the 2026-08-25 data-entry audit (docs/data-entry-inventory.md Risk notes). (1) New `best_effort_audit_insert()` runs the 4 best-effort audit inserts (product_verification_history ×2, lot_reassignments, inventory_adjustments) inside `SAVEPOINT audit_insert_sp`; failures log at ERROR (`AUDIT_INSERT_FAILED`) and surface as additive `audit_warning` response field; `/inventory/found-with-new-product` now also writes its missing inventory_adjustments row. (2) `/make` commit takes `pg_advisory_xact_lock(3)` before lot-code generation (receive=1, found=2). (3) New `next_lot_sequence()` computes the next suffix as numeric MAX over `-\d{1,6}$` suffixes at all 5 generator sites (receive, make preview+commit, found ×2); nonnumeric manual codes are ignored instead of resetting the counter to 001, and counting works past -999. 16 new tests incl. a real cross-connection pg_locks blocking test; suite 260 passed. | (1) A DB-level audit-insert failure (e.g. over-length varchar from unbounded request strings) aborted the whole transaction; `get_db_connection()`'s commit silently became ROLLBACK while the endpoint returned success:true — the primary write vanished. (2) Two concurrent makes could mint identical B-codes, or silently fold two same-product runs into one lot via ON CONFLICT DO NOTHING. (3) A manual code sharing the prefix with a nonnumeric suffix (sorts above digits) reset the counter to 001, colliding with the existing -001 lot; the found-with-new-product copy had no try/except and 500'd on the parse. | Reverting (1) restores silent primary-write rollback on audit failure — success:true with nonexistent ids; response consumers may now rely on `audit_warning`. Reverting (2) reopens the concurrent duplicate-B-code race. Reverting (3) restores the 001-reset collision and the past-999 counter stall. The three fixes are independent; each can be reverted alone but all guard the same audit. | `main.py`, `tests/test_audit_savepoints_lotcodes.py` |
| 105 | 2026-08-25 | Entry provenance: entry_backfilled flag + Daily Entries entered-at basis (branch feat/backfill-flag-daily-entries c1cd975 — NOT pushed/deployed; migration 046 NOT applied to prod) | Migration 046: transactions.entry_backfilled + one-time recon data fix (operator inv-recon-2026-08-17, backdated rows; append-only trigger disabled for the statement; idempotent). Daily-entries API: entered_at = legacy "timestamp" for migration_backfill_039 rows, late/entered filtering on that basis, entry_time_reliable true for them, new entry_backfilled field; dashboard badge keys only off entry_backfilled (js v38). 3 new tests; suite 232 passed. | Reverting restores the false "backfilled" badge on ~all pre-8/11 rows and resumes reading created_at (=039 stamp) as entry time — every lag metric overstates. Permanent rule: created_at_source alone must never be rendered or computed as "backfilled/late"; the explicit flag is the only badge source, and pre-039 entry time lives in legacy "timestamp" (naive UTC). Deploy order: apply migration 046 BEFORE deploying the app code (endpoint selects bt.entry_backfilled). Remove the schema.sql pending include after prod apply + re-dump. | `migrations/046_entry_backfilled_flag.sql`, `main.py`, `dashboard/dashboard.js`, `tests/test_daily_entries.py` |
| 104 | 2026-08-25 | Data-health follow-ups (read-only; no deploy) | F1 refined: §3b's "100% backfilled July" was a created_at artifact — legacy "timestamp" shows 96–100% same-day live entry for every product family from Jul 1; sole true reconstruction = the 8/17 recon session (7/24–8/14). Aged on-hand baseline added (§8). PR #17 confirmed the only trace-fix branch (9e78fbc). lot-code-cleanup draft now renames lot 578 'MAY 011 2026'→'MAY 11 2026-dup' (owner decision, no merge). | The F1 finding means created_at_source alone must never be read as "entered late" — always fall back to legacy "timestamp" for pre-039 rows (same rule as §3a's occurred_at caveat). Reverting the report/SQL drafts loses the baseline; nothing executable. | `docs/data-health-baseline-2026-08-24.md`, `docs/sql/lot-code-cleanup-2026-08-25.sql` |
| 103 | 2026-08-25 | Trace fix + entry provenance DEPLOYED (renumbered from 100 — collision with upstream rows 100-102, row-28 precedent) (PRs #17/#18; migration 046 applied to prod ~14:59 ET) | Merged PR #17 lot-id-based forward trace (head `9e78fbc`, merge `b9062e7`) and PR #18 occurrence timestamps + `entry_backfilled` (head `e6e0f2f`, merge `6a3d37f` = new main). Applied `046_inventory_occurred_at.sql` via session pooler 5432 (`psql -X -v ON_ERROR_STOP=1 -f`): adds `transactions.entry_backfilled` (boolean NOT NULL DEFAULT false), updates `ledger_fill_transaction_business_time` (caller-supplied `occurred_at` preserved; legacy `timestamp` fallback) and `ledger_enforce_created_at` (`created_at_source='api_backfill'` sets `entry_backfilled=true` on insert; created_at stays database-owned), and marks the 77 8/17 inventory-recon rows `entry_backfilled=true` inside a guard that aborts unless the selector (operator `inv-recon-2026-08-17`, notes marker, created 8/17 NY, business_date earlier) matches exactly 77. Verified on 6543 read-only: count 77. Railway + Netlify live from `6a3d37f` (dashboard.js v40 md5-identical). Spot checks: txn 1888 backfilled=false/days_late 0; txn 1953 backfilled=true/days_late 97; lot 1318 → pack 2093 → FG 1326 in stock, no 1321/2087/2090 contamination. | Forward trace of batch lots previously matched by lot_code (ambiguous "AUG 21 2026" attached coconut ships 2087/2090 to granola); backfilled entries were indistinguishable from live entries in Activity. | Reverting the trace code re-introduces cross-product lot-code contamination in traceability. Dropping/reverting 046 loses backfill provenance and the recon-row classification; re-running 046 after new backfills exist will ABORT on the 77-row guard (edit the guard, don't bypass it). `entry_backfilled` is trigger-maintained — never set it directly in app code. | `migrations/046_inventory_occurred_at.sql`, `main.py`, `dashboard/traceability.html`, `dashboard/dashboard.js` |
| 102 | 2026-08-25 | Entry occurrence and explicit backfill provenance — **PR #18, NOT deployed** | Migration-039 rows use legacy `timestamp` as effective entered time and never receive a Backfilled badge solely from `created_at_source`. The single migration 046 adds one `transactions.entry_backfilled` boolean; API `backfill:true` inserts set it, and the one-time post-039 recon update selects by reliable `created_at` entry day, operator, note, and pre-8/17 business date. A hard guard requires exactly **77** recon rows, including two dated 2026-05-12. Activity, recent events, shipping/receiving, and lot timelines expose the flag; badges key only off it. Up/down/up passed with 77/77 flagged and sources unchanged. Suite **244 passed, 202 warnings**; dashboard JS 40; GPT schemas **30/22**. Final commit `b837cec`. `shipments.shipped_at` now takes `occurred_at` when supplied. The 046 recon selector is deliberately narrower than operator + business_date (adds notes LIKE and entered-day predicates) with a hard 77-row abort guard, verified equivalent on prod 2026-08-25. | Migration 039 stamped historical rows with one migration-time `created_at`; it did not prove those entries were backfilled. The prior PR selector omitted the two May recon rows. An independent boolean is required so intentional reconstruction and technical migration provenance cannot be confused. | Reverting restores false provenance semantics or loses the exact 8/17 reconstruction marker. Keep legacy `timestamp` as the entered-time basis for actual migration-039 rows; keep the post-039 recon selector guarded at 77; preserve `created_at_source` on existing rows; render Backfilled only from `entry_backfilled`. Apply migration 046 before deploying code that selects the new column. | `main.py`; dashboard Activity; migration 046 + down; timing/provenance tests; changelogs |
| 101 | 2026-08-25 | Inventory occurred time vs entry time — **PR branch, NOT deployed** | Rebased implementation commit `7131898` onto PR #17 head `9e78fbc`. Receive, standalone ship, make, pack, adjust, both found-inventory writes, and sales-order shipment preview/commit accept optional ISO-8601 `occurred_at` and `backfill`. Offset-free values are America/New_York plant-local; offset-aware values normalize to plant time. More than 5 minutes future → 400; more than 14 days old → 400 unless `backfill=true`, which keeps database-owned `created_at` and marks `created_at_source='api_backfill'`. Omission retains the migration-039 fallback. Transaction-wrapped migration 046 preserves explicit occurrence time and backfill provenance with a down migration; re-run and down/up passed only on local port-5432 `factory_ledger_test`. Office/Floor schemas stay 30/22 operations and ask “When did this happen?” only when the event was implied not-now. Activity shows Occurred/Entered, a >60-minute lag chip, and backfilled badge; CSS 27 / JS 38. Post-rebase suite **239 passed, 199 warnings in 3.48s**. No prod DDL/data write, deploy, merge, FIFO/allocation, trace endpoint/history, or traceability-dashboard change. | All live inventory writes previously supplied only `get_plant_now()` as the legacy transaction timestamp; migration 039 therefore derived occurrence from entry-time data. Actual event time and entry lag were unmeasurable, and the GPT had no safe way to record late entries. | Reverting collapses occurrence back onto entry time, removes future/age safeguards and explicit backfill provenance, and hides lag in Activity. Keep `created_at` database-owned; never substitute occurrence time into FIFO/allocation ordering. Preserve the NULL fallback for callers that omit the new field, and keep the 30-operation Office cap. | `main.py`; `migrations/046_inventory_occurred_at.sql`; `migrations/down/046_inventory_occurred_at_down.sql`; `tests/schema/schema.sql`; `tests/test_inventory_occurred_at.py`; `tests/test_recent_ledger.py`; both GPT schemas; `dashboard/dashboard.js`; `dashboard/dashboard.css`; `dashboard/index.html`; changelogs |
| 100 | 2026-08-25 | Batch/output-lot forward trace — **PR #17 branch, NOT deployed** | Commit `6f85045` fixed `trace_ingredient`'s output-lot consumption query by numeric lot id, but its changelog incorrectly claimed the unchanged dashboard excluded same-code shipments; independent review correctly requested changes because the UI still compared lot-code strings. Follow-up `97cbf7d` carries additive `lot_id` and `product_id` through both `used_in_batches` branches, nested trace lot references, and `/transactions/history` lines, then keys dashboard supplier/shipment matching and output-node identity only by numeric lot id. Commit `4ccf7c2` removes the last defensive lot-code-derived identity fallback from the dashboard history index. The original backend collision regression remains; a new dashboard-data regression combines the trace endpoint and history, packs and ships both same-code product lineages, and proves the other lineage's shipment id is absent. Full suite **231 passed, 193 warnings**. Transaction-scoped read-only prod verification: lot 1318 → pack 2093 → FG lot 1326 → no shipments, with 2087/2090 absent (old code-only control finds both); lot 965 → pack 2064 → FG lot 1308 → shipment 2086 → Dingman's Dairy. No schema, migration, GPT schema, deployment, prod write, or merge. | The backend dead end was fixed in `6f85045`, but the dashboard still matched history lines by non-unique lot code, so its rendered data could attach coconut shipments 2087/2090 to granola lot 1318. Lot codes collide across products and cannot serve as identity. | Reverting `97cbf7d` or `4ccf7c2` restores UI-level cross-product shipment contamination, output-node collapse, or a lot-code-derived history identity fallback for same-code lots. Keep all trace joins, history matching, supplier matching, shipment matching, graph node identity, and history-index identity keyed by numeric lot id; typed code may only be used to enter the trace together with product id. | `main.py`, `dashboard/traceability.html`, `tests/test_batch1_correctness_security.py`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 99 | 2026-08-25 | Data-health baseline + Forms-vs-FL cross-check (read-only audit; no deploy, no prod writes) | Added `scripts/data_health.py` and `scripts/forms_crosscheck.py` (both: 6543 pooler, per-query `BEGIN TRANSACTION READ ONLY`, no session GUCs) and generated `docs/data-health-baseline-2026-08-24.md`. Measured: occurred_at is entry-defaulted (lag unmeasurable; only the 8/17 recon backdates), backfill cliff exactly at epoch 8/11, ship linkage 100% via `sales_order_shipments` (order_reference ≈ dead), receipts 100% supplier+BOL but `expected_receipts` 0 rows ever, batch trace completeness 0/49 (a handful of found-inventory coconut/FOUND lots), 159 lot-code collisions ("AUG 21 2026" → txns 2087/2090 confirmed SKU 10010 coconut ships, not granola), F4 case-lot misattribution 6.2% of 30,840 matched cases, post-epoch forms↔FL pan agreement perfect (Aug 20 12=12 confirmed), Quali Pack 8/4 receipt missing from the form, shipping form has no product/lot columns. `data/forms/*.csv` gitignored, never committed. | Owner needed measured baselines before tightening floor data entry; no prior quantified forms-vs-FL comparison existed. | Nothing executable reverts — analysis artifacts only. The report's numbers are a point-in-time baseline (2026-08-25); re-run both scripts rather than editing the doc. Deleting the `.gitignore` `data/forms/*.csv` entry risks committing floor PII/vendor data. | `scripts/data_health.py`, `scripts/forms_crosscheck.py`, `docs/data-health-baseline-2026-08-24.md`, `.gitignore` |
| 98 | 2026-08-24 | 044 series **DEPLOYED 2026-08-24 ~14:17 ET** (push `4d9a196..35e3171` to origin/main; supersedes row 97's "NOT pushed" status) | Railway auto-deploy began serving the new code at 14:17:27 (fulfillment-check switched from the legacy `fulfillable/can_fulfill` shape to `dispatch_ready`+blockers). Netlify deployed by 14:16:47: live `dashboard.js` byte-identical to the `35e3171` file (194,570 bytes, v=37), `SALES_API_BASE` = Railway URL. Read-only prod smoke, dashboard key only: `GET /sales/orders?limit=5` 200 with `dispatch_ready`/`blockers`/`remaining_effective_lb`; fulfillment-check 200 — 19 open-set orders, 0 dispatch_ready, blocker codes `unallocated`(19) `shortage`(14) `not_floor_ready`(14) `fulfillment_diverged`(3); order 39 detail 200 with order-level `shipped_effective_lb` + line `readiness`; allocations GET 200, count 0; no-key 401, wrong-key 403. No prod SQL, no prod writes, no allocation created. Owner round-trip verify: prod smoke allocate 100 lb + release on SO-260814-002 line 687, allocation id 1, persisted after reload (GET shows one `released`/`manual`/SKU-level 100 lb row, `release_reason=manual_release`, note `PR6 prod smoke`, `created_by=dashboard`, released_at 2026-08-24T18:27:59Z); ALLOCATIONS_ENFORCED confirmed absent in Railway env (default OFF). | Migrations 044/045 had been live in prod since 2026-08-19/20 with the application code undeployed; operators lacked readiness/allocation features the DB already supported. | Pre-deploy fulfillment-check was NOT 404 — the route pre-existed (rows 83–84); deploy watches must probe the `dispatch_ready` field, not the status code. `ALLOCATIONS_ENFORCED` has NO read-only prod probe: with zero allocations, absence of `allocation_warning` is expected under BOTH flag states — confirm observe mode via the Railway env UI, never by response absence. Rollback = redeploy prior Railway build; the 044/045 tables tolerate it (empty/unread), but the dashboard v37 bundle would then call readiness fields that old code omits. | (deploy record; no files) |
| 97 | 2026-08-24 | 044 series squash-merged to main (branch `feat/044-so-allocations`) — **LOCAL commit `15b3cd6`, NOT pushed, NOT deployed** | Squash-merged the full 044 allocation series (rows 80–96) onto main over `4d9a196` (B-1 `cf2f54f`, already in prod). Changelog conflicts resolved: main's row 79 kept, branch rows 79–95 renumbered 80–96 with cross-refs bumped. Merged-tree suite **229 passed, 192 warnings** (223 branch + 6 B-1). PR 6 manual test: all-pass; step 10 (flag-on preview warning) deferred until an authorized staging/local flag-on run. Ship-and-note items for PR 6.1: manual-test doc `psql -v` interpolation (`-c` does not interpolate `-v` variables — step 1 confirmation query needs a literal id or stdin), `mini-calendar.js` separate API base, cancelled-line units display. Office `.venv-test` rebuilt as `.venv-test.nosync` + symlink after iCloud evicted the synced venv; `.gitignore` entries pending. | Branch work was complete and reviewed but unmerged; main had gained B-1, colliding on changelog row 79 and the top of CHANGE_LOG.md. iCloud eviction had emptied the synced `.venv-test`. | Reverting the merge orphans prod migrations 044/045 (both applied) once deployed and reopens every regression guard in rows 80–96. Renumbering back re-collides row 79. The `.nosync` venv must not be committed; add `.venv-test`/`.venv-test.nosync/` to `.gitignore` before any `git add -A` habit ever returns. Branch `feat/044-so-allocations` is preserved unmerged for audit (known-bad intermediates `e87219c`, `4e51115`) — do not delete or cherry-pick. | `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 79 | 2026-08-21 | orders matrix XLSX export — bulk per-lb SKUs | **DEPLOYED 2026-08-21 08:23 ET** (commit `cf2f54f`, fast-forward from `fix/b1-matrix-bulk-lb`; Railway deployment `3de52662-75b6-4b32-8090-8061504d5ea3` Success). `GET /export/orders-matrix.xlsx` now treats NULL `case_size_lb` as the existing "sold by the pound" marker instead of a fatal error. SQL selects `COALESCE(sol.quantity_lb / NULLIF(p.case_size_lb, 0), sol.quantity_lb) AS qty` so per-lb rows return pounds; the handler maps NULL case size to `lb_per_case = 1.0` and raises 422 only for `case_size_lb <= 0` or NULL qty. New `_matrix_column_header` suffixes per-lb columns " (lb)" on both sheets unless the shortened name already ends in per/lb (70013/70016 do, so they are not suffixed). `_matrix_quantity_note` returns `Optional[str]`: a per-lb cell gets lb-based pan math when a pan yield is on file and no comment at all otherwise, so cases-to-pans text never lands on a pounds cell. 6 new tests (9 total in the file); suite 25 passed / 120 DB-skipped. Pre-deploy verification against the production DB (read-only): HTTP 200, valid 18,505-byte XLSX, Cases/Pounds sheets, 20 product columns, 16 order rows, and **all 20 column totals reconciled to `SUM(sol.quantity_lb)`** under the export's exact filters — 70013 = 4,000 lb, 70016 = 6,000 lb. Post-deploy production acceptance: 200 + XLSX media type with both the master key and the scoped `dashboard-key-2026`, both bulk headers present and not doubled up. No frontend, schema, migration, product-data, order-data, or GPT operation changed; `openapi-gpt-v3.yaml` untouched at 30 operations. | The dashboard "Export Matrix (xlsx)" button failed for every user with 422 `INVALID_ORDER_EXPORT_CASE_SIZE` naming SKUs 70013 and 70016. Both are "#9 Bulk per/lb" finished goods whose `case_size_lb` is NULL by design (the same convention row 54 records for 70004), so the mandatory case conversion aborted the entire workbook. Not an auth fault — the scoped dashboard key was already on `DASHBOARD_KEY_ALLOWLIST` (row 63); master and scoped keys returned byte-identical 422s. | **Narrows row 61's guard — read before reverting.** Row 61 required the export to hard-fail on invalid case data; that guard is retained but narrowed to `case_size_lb <= 0`. NULL is now exempt per row 54's bulk per-lb convention. Row 61's actual scenario (raw ingredient 11033 sold by weight) is still excluded upstream by the `p.type = 'finished'` filter, which is unchanged and still covered by `test_orders_matrix_query_excludes_non_finished_products`. Reverting this row restores total export failure whenever any open order contains a bulk per-lb line — and 70004 will trigger it the next time it is ordered. Re-widening the check to reject NULL again re-breaks 70004/70013/70016. Removing the `<= 0` branch instead would silently admit corrupt finished-goods case data, so that half must remain. | `main.py`, `tests/test_orders_matrix_export.py` |
| 96 | 2026-08-21 | Review fix for FR-8 PR 6 dashboard commit `1eb68f4` (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed/deployed** | Dispatch Queue no longer synthesizes `note:null`; its Factory Ready checkbox and inline note input/Save are disabled with the exact All Open Orders tooltip, and defensive handler/writer guards prevent a dispatch-derived ready POST. Closed shipped/invoiced/cancelled orders render `—` for Dispatch and Blockers; cancelled lines classify before null-readiness service detection. Allocation create/release write errors remain actionable, but successful writes are separated from detail refresh: refresh failure is the soft `Saved — refresh to see updated state` warning. Removed dead `LINE_ALLOCATION_EXCEEDED`. Exactly 500 `/inventory/current?limit=500` rows visibly warn that the picker may be incomplete and suppress the definitive no-positive-lots message. Orders-list effective remaining restores the `· N units` suffix in every mode using existing list unit data plus the dispatch-line fallback. Manual coverage now names `dashboard/dashboard.js:1855`, the committed `SALES_API_BASE`, exact `http://127.0.0.1:8000` override, local released+shipped fixtures, exact `OVER_ALLOCATION`, 60-second TTL updates, and disabled dispatch ready controls. Cache CSS v25→26 / JS v36→37. `main.py` hash unchanged; dashboard JS syntax and diff checks clean; localhost `factory_ledger_test` suite **223 passed, 186 warnings in 5.26s**. | The fulfillment-check response omits the stored floor note, so Dispatch Queue could POST a synthesized null and erase it. Terminal orders retained misleading current-readiness pills; cancelled service-shaped lines were mislabeled Service; refresh failures looked like failed writes; the picker asserted an empty match despite a capped response; and PR 6 dropped order-list units. | Reverting can destroy saved floor notes from Dispatch Queue, misstate terminal/cancelled rows, tell operators a successful allocation failed, hide a truncated lot search, and remove units. Permanent UI rule: Dispatch Queue readiness data is read-only for Factory Ready; only All Open Orders may change the flag/note. Write outcome and follow-up refresh outcome must remain distinct. A limit-sized inventory response is incomplete, never authoritative-empty. | `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `docs/manual-tests/044-so-allocations-pr6-dashboard.md` |
| 95 | 2026-08-21 | FR-8 PR 6 allocation dashboard UI (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed/deployed** | Added a dashboard Dispatch Queue mode backed by the read-only fulfillment-check route, with Dispatch Ready/Blocked filters, dispatch-first/date-second sorting, effective remaining pounds, owning readiness state, and block/warn/info chips. Order detail now displays physical-line `ordered_lb`, `shipped_effective_lb`, `remaining_lb`, `allocated_lb`, and `shortage_lb`; computed dispatch semantics are explicitly distinct from workflow/Factory Ready and are not presented as a ship gate. Added dashboard-key-only manual SKU, manual lot, auto-FIFO, and release controls against the PR-3 routes; allocation history labels `manual`/`staged_lot`/`auto_fifo`, SKU-vs-lot level, lifecycle state, and live TTL countdown. Structured 409s render actionable copy rather than JSON. Read-only SO ship preview shows `can_ship_lb`, `reserved_others_lb`, owners, and optional structured warnings; absent V-2 flag-off warnings produce no warning artifact, while present flag-on warnings render. Copy states allocations never move physical inventory. Cache-busters CSS v24→25 / JS v35→36. Added a localhost/staging-only manual script covering display, happy-path SKU+lot allocation, over-allocation, release, auto-FIFO TTL, dispatch blockers, flag-off warning absence, and staging-dependent flag-on warning. Full localhost runner: **223 passed, 186 warnings in 5.14s**; JS syntax/diff checks clean; GPT schemas unchanged at 30/22. | PRs 1–5 exposed allocation/readiness APIs but left operators on the old recorded-shipment remainder, with no allocation controls, no blocker taxonomy display, no dispatch queue, and no way to see reservation-aware preview warnings. | Reverting restores recorded/void-blind remaining in the dashboard and removes allocation lifecycle controls, TTL/source/level visibility, dispatch filtering, blocker severities, and optional warning rendering. Keep every write on the scoped `dashboard-key-2026`; keep Dispatch Ready as computed/read-only, never a ship gate; keep absent `allocation_warning` silent under flag-off V-2 and render it only when supplied; never describe allocation/release as physical inventory movement. | `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `docs/manual-tests/044-so-allocations-pr6-dashboard.md` |
| 94 | 2026-08-21 | Accepted PR 5 review fix round — V-4 + test-infra/docs (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed/deployed** | V-4: sales-order flag-on commit preflight now sums `fifo_lot_balances(..., include_empty=False)` instead of raw net `_product_on_hand`, exactly matching the positive-lot basis consumed by `_sales_order_ship_plan`; regression probe A=100, B=-60, foreign SKU pin=60 proves ON returns atomic 409 `STOCK_ALLOCATED` (`requested=100`, `can_take=40`, `reserved_taken=60`) while OFF retains PR-4 partial ship of 40 from A and leaves B=-60. Ten test modules no longer catch broad `Exception` around `import main` and skip the module; a simulated broken import now stops collection with ERROR/exit 2. New executable `scripts/run_tests.sh` defaults `TEST_DATABASE_URL` to localhost `factory_ledger_test`, probes `pyexpat`, conditionally prepends `/opt/homebrew/opt/expat/lib` only when the pinned interpreter needs it, then runs `.venv-test/bin/python -m pytest "$@"`; conftest guidance points to it. Design V-1 now says default off/tests enable per case, and V-2 records flag-gated preview warnings as review-accepted variance dated 2026-08-21. Full runner suite: **223 passed, 186 warnings in 4.30s**. | Netting a negative lot against a positive shippable lot understated preflight physical stock (40 instead of 100), making `reserved_taken=0` and allowing a silent partial ship despite a 60-lb foreign reservation. Separately, ten broad import catches could turn a broken interpreter/application import into silent green module skips, and this Homebrew Python 3.12 cannot import `pyexpat` without the matching expat dylib path. | Reverting V-4 restores the reservation bypass whenever negative lots offset usable stock. Reintroducing module-level broad import skips can make infrastructure/application breakage look green. Bypassing the runner on this machine reproduces the `_XML_SetAllocTrackerActivationThreshold` pyexpat load failure; keep the workaround conditional and do not alter system libraries. Permanent flag rules: default OFF, enable explicitly per test/env, consume-on-ship unconditional, preview allocation warnings flag-gated per accepted V-2 variance. | `main.py`, `tests/test_sales_order_allocations.py`, ten import-hardened test modules, `tests/conftest.py`, `scripts/run_tests.sh`, `docs/designs/044-so-allocations-design.md` |
| 93 | 2026-08-21 | FR-4 PR 5 — `ALLOCATIONS_ENFORCED` steal 409s (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed/deployed** | Added dynamic env switch `ALLOCATIONS_ENFORCED`, default OFF. OFF stays on the PR-4 observe path byte-for-byte: `RESERVED_STOCK_OBSERVE_ONLY`, standalone `inventory_shipped` shrink, pack's documented observe hole, and restore `inventory_restored` shrink all remain. ON reuses PR-4 takeable arithmetic and owning-order fields to return atomic 409 `STOCK_ALLOCATED` before standalone `/ship`, pack-source, sales-order ship, or restore can persist a stock write. Sales-order own pins remain entitlement; sibling and other-order pins compete. Restore keeps addendum precedence: `RESTORE_STOCK_MISSING` → `RESTORE_SPLIT_MISSING` → PR-5 foreign-stock preflight → correction INSERT → consume → retained shrink. Flag-on previews reuse the structured allocation-warning shape and warn whenever `reserved_others_lb > 0`; an actual steal says commit will be blocked. Eight new test items cover default/off parsing, lot+SKU standalone 409s, safe/steal preview warnings, pack 409, own-allocation success, sibling/other-order blocking, restore-before-shrink, envelope fields, and before/after atomicity. Existing 214 tests were not edited; full Python 3.12.14 `.venv-test` suite with local `TEST_DATABASE_URL`: **222 passed, 185 warnings in 4.20s**. Office/Floor schemas remain 30/22; no migration, route, GPT schema, allowlist, CRUD, void, or shrink-removal changes. | PR 4 exposed reservation steals but allowed them. Without this switch, standalone ship, pack, and restore can still consume another order's exclusive stock; order ship can silently partial against competing pins. | Reverting removes the opt-in hard gate and flag-on Floor warnings. Permanent rules: default/off must preserve PR-4 response bytes and both shrink repairs; consume-on-ship stays unconditional; own-line pins never cause `STOCK_ALLOCATED`; sibling pins are foreign; restore's stock and recorded-coverage errors retain precedence; 409 payloads retain requested/can-take/reserved-taken/owner fields and must leave ledger, SOA, shipment/correction, lots, and order state unchanged. | `main.py`, `tests/test_sales_order_allocations.py`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 92 | 2026-08-20 | FR-4 PR 4 fix round — pinned-lot fidelity + owner rulings (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed/deployed** | Owner ruling 2 (defect fix): a pinned `lot_code` on standalone `/ship` preview+commit restricts the takeable deduction plan to that lot only — pinned pounds NEVER spill to other lots to avoid reserved stock; observe mode takes the pinned lot's reserved pounds with the `RESERVED_STOCK_OBSERVE_ONLY` warning and the ruling-1 `inventory_shipped` shrink; physical insufficiency on the pinned lot keeps the existing 400 even when other lots hold stock. `_takeable_deduction_plan` no longer reorders via `preferred_lot_code`; pinning is a call-site restriction of the lot list. Pinned previews keep product-wide `total_available_lb`/`total_takeable_lb`. Pack's explicit `lot_allocations` commit arithmetic gained the preview's physical clamp (symmetry by construction; `validate_lot_deduction` still guards). Owner ruling 1 (keep): the standalone-steal shrink with `release_reason='inventory_shipped'` stands and is now documented as design addendum §4.a.1; pack retains its documented observe-mode hole until PR 5. Five tests close review gaps (a)–(e): SKU-level standalone shrink, same-order sibling competition, commit-path auto-FIFO expiry persistence, pack `lot_allocations` reserved arithmetic preview==commit, and pinned-lot fidelity (all pounds from pinned lot A incl. stolen 30 with warning+shrink, 400 on pinned overdraw, sibling lot untouched). Full pinned 3.12.14 suite with `TEST_DATABASE_URL`: **214 passed, 178 warnings**; E1 re-probed green on a real uvicorn + throwaway DB clone. Schemas stay 30/22; no migration/GPT/allowlist/CRUD/restore/void changes. | The PR 4 review (accepted with variances) proved `lot_code`-pinned ships silently rerouted pounds to other physical lots to preserve reservations (probe E1: 20 from pinned A + 30 from B, no warning) — a traceability defect per owner ruling; the invented `inventory_shipped` shrink needed an owner ruling and design documentation; five behaviors had no covering test. | Reverting reintroduces silent pinned-lot spillover (operator ships lot A, product leaves from lot B) and orphans addendum §4.a.1. Permanent rules: pinned pounds come only from the pinned lot — insufficiency is a 400, never a spill; standalone steals shrink with `inventory_shipped` (ruling 1); pack keeps its hole until PR 5; commit and preview reserved arithmetic must stay identical. | `main.py`, `tests/test_sales_order_allocations.py`, `docs/designs/044-so-allocations-restore-addendum.md`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 91 | 2026-08-20 | FR-4 PR 4 standalone ship + pack takeable previews/commits (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed/deployed** | `/ship` and `/pack` preview+commit now reuse `available_lots_for_product()` and a shared takeable deduction plan: active unexpired lot pins are removed first, foreign SKU claims shadow-consume FIFO unpinned stock, and unreserved lots are chosen before any reserved fallback. Additive responses expose `reserved_others_lb`, `reserved_by_orders`, takeable capacity (`can_ship_lb` / `can_pack_lb`), and structured `RESERVED_STOCK_OBSERVE_ONLY` detail when physical stock beyond takeable is used. PR 4 remains observe-only (no `ALLOCATIONS_ENFORCED`, no `STOCK_ALLOCATED` 409): writes proceed. Standalone steals immediately run `_shrink_overallocated_products(..., release_reason='inventory_shipped')`; pack remains the documented observe-mode hole and does not convert or shrink SOA. Sales-order ship preview now proves own-line allocations are excluded while sibling/other lines still compete. Four endpoint tests cover lot+SKU pins, owning order numbers, expired-pin read exclusion/no GET write, own-allocation exclusion, standalone write+warning+shrink, pack write+warning, and target-SKU allocations not blocking production. Pinned Python 3.12.14 full suite with `TEST_DATABASE_URL`: **209 passed, 173 warnings**; office/Floor schemas remain 30/22. No migration, schema, GPT, allocate CRUD, or restore/void changes. | Standalone ship and pack previously read raw posted balances and physical FIFO directly, so previews could promise another order's reserved pounds without identifying owners, FIFO could consume a reserved early lot while unreserved stock existed later, and standalone steals left live SOA above on-hand. | Reverting hides reservation competition and restores raw-FIFO standalone/pack planning. Keep standalone shrink after the posted deduction so stolen lot/SKU claims are repaired; do not convert SOA on standalone ship or pack. Pack's flag-off over-allocation is intentional only through PR 4 and is closed by PR 5 enforcement. Expired `auto_fifo` rows remain read-excluded and are persisted released only on locked writes. | `main.py`, `tests/test_sales_order_allocations.py`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 90 | 2026-08-20 | Test-infra hardening (branch `feat/044-so-allocations`) — **LOCAL ONLY, not pushed** | Rebuilt `.venv-test` on Python **3.12.14** (was silently 3.14.5) from the captured pip-freeze set incl. `httpx==0.27.2`/`anyio==3.7.1` (latest httpx breaks this repo's TestClient). Added an interpreter guard at the very top of `tests/conftest.py`: `assert sys.version_info < (3, 13)` with a loud message — on Python 3.14 this repo's `python -m pytest` has exited 0 with ZERO tests collected (green-on-nothing). Verified: suite 205 passed/169 warnings on the rebuilt venv; a 3.14 interpreter now dies at conftest import with `AssertionError: REFUSING TO RUN ... green-on-nothing hazard` (pytest exit 4). | `.venv-test` drifted to 3.14.5 with no signal; a CI/local run on 3.14 could report success while executing nothing. | Removing the conftest assert re-permits silent zero-collection green runs on 3.13+; rebuilding `.venv-test` on newer Python or with latest `httpx` breaks TestClient-based tests (keep `httpx==0.27.2`, `anyio==3.7.1`). Companion rule to row 81's TEST_DATABASE_URL skip hazard. | `tests/conftest.py` |
| 89 | 2026-08-20 | FR-4 — migration 045 **APPLIED TO PRODUCTION 2026-08-20 ~15:05 ET** (branch `feat/044-so-allocations`; application code still NOT deployed) | Applied `migrations/045_sales_order_allocation_reactivations.sql` to prod Supabase via the session-mode pooler on port 5432 (`aws-1-us-east-1.pooler.supabase.com`, never 6543; `psql -X -v ON_ERROR_STOP=1 -f`): clean BEGIN→CREATE TABLE→3×COMMENT→COMMIT. Live read-only verification: `to_regclass` non-NULL; PK `(transaction_id, sales_order_line_id)`; CHECK `soar_quantity_lb_check (quantity_lb >= 0)`; FKs → `transactions(id)`, `sales_order_lines(id)`, `ledger_corrections(id)`; `created_at timestamptz NOT NULL DEFAULT clock_timestamp()`. Idempotence proven in prod: re-run produced `NOTICE: relation ... already exists, skipping` and a clean COMMIT. Schema re-dumped via `scripts/dump_prod_schema.sh` (3,986 lines, zero data rows); 045 objects now in the dump body and the pending `\ir 045` block is gone per the row-80 rule; only other diff is pg_dump's per-run `\restrict` token. Fresh test-DB rebuild (`setup_test_db.sh --fresh`) shows the table from the dump body. Full suite on Python 3.12.14 with the exact pinned package set: **205 passed, 169 warnings** (note: repo `.venv-test` is now Python 3.14.5 — it also passes 205; the 3.12 run used a fresh venv with the identical `pip freeze` set). | Prod lacked the reactivations table that PR 3's void-time recording and restore consume require before the application can deploy. | Dropping the prod table orphans PR 3's void UPSERTs and restore reads once deployed. Re-adding a pending `\ir 045` block to `tests/schema/schema.sql` after this re-dump double-includes the table on fresh builds (harmless via IF NOT EXISTS but stale — the row-80 rule says the block exists only between commit and prod apply). Reverting the re-dumped `schema.sql` desyncs local test DBs from prod. | `tests/schema/schema.sql`, `FACTORY_LEDGER_CHANGELOG.md`, `CHANGE_LOG.md` |
| 88 | 2026-08-20 | FR-4 PR 3 review fix round 3 — record-at-void restore (`docs/designs/044-so-allocations-restore-addendum.md`; branch `feat/044-so-allocations`) — **SUPERSEDES row 87; LOCAL ONLY, NOT deployed** | Added idempotent migration 045 `sales_order_allocation_reactivations`: PK `(transaction_id, sales_order_line_id)`, `quantity_lb numeric(14,4) >= 0` including explicit zero, void `correction_id`, timestamp. Every ship void UPSERTs one row per `sales_order_shipments` line using the SUM of SOA pounds actually returned to live rows. Restore order is transaction lock/status check → lot-level stock preflight (`RESTORE_STOCK_MISSING`) → recorded-demand load (missing = zero + warning) → product lock/auto-FIFO expiry → positive-demand live coverage preflight (`RESTORE_SPLIT_MISSING`) → correction INSERT → consume current live lot pins then SKU row via `_consume_allocation_row` → shrink with `inventory_restored`. Superseded rows are never flipped; `last_ship_transaction_id` is audit-only; ledger quantity is never the consume target. S1/S1b/S2–S5 plus explicit-zero, partial, multi-line, missing-record, stock-loss, shrink, repeat-cycle, atomicity, and four-invariant tests pass. Full pinned Python 3.12 suite: **205/205**; readiness **17/17 unmodified**. Write-foundation migrations move to 046+. | Row 87 equated ledger ship pounds with allocation pounds returned by void. That is only true for fully allocated ships: the majority unallocated path records 0, and a 50-reserved/100-shipped path records 50. Ledger-derived demand incorrectly 409ed valid restores and broke readiness. The handler also lacked the pre-insert lot stock guard and post-restore shrink. | Reverting reintroduces 409 on ordinary unallocated/partial restores, loses exact S3 40-vs-coalesced-100 attribution, permits restore to make lot stock negative, and leaves other orders overallocated after stock is re-posted. Permanent rule superseding row 87: restore demand comes only from the void-time reactivation record; missing records warn and act as zero; ledger shipped pounds are an attribution ceiling, never allocation demand. Keep stock-first guard order, positive-only coverage guard, consume-from-live mechanics, superseded immunity, and `inventory_restored` shrink. | `migrations/045_sales_order_allocation_reactivations.sql`, `main.py`, `tests/test_sales_order_allocations.py`, `docs/designs/044-so-allocations-restore-addendum.md`, `docs/designs/045-write-foundation-design.md`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 85 | 2026-08-20 | FR-4 / FR-8 PR 3 allocation writes + lifecycle hooks (branch `feat/044-so-allocations`) — **pushed to `origin/feat/044-so-allocations`, NOT deployed** | Added application-only allocation writes under the migration-044 unique-live indexes. `available_lots_for_product()` locks product lots by id then active SOA rows, persists expired `auto_fifo` rows only on product-affecting writes, subtracts all lot pins, shadow-consumes foreign SKU claims FIFO, and uses `fifo_lot_balances()` / `FIFO_LOT_ORDER_SQL` including `l.id ASC`. Dashboard/master-only routes: one `POST /sales/orders/{id}/allocations` with manual or `auto_fifo` mode, keyed `GET`, `POST .../{allocation_id}/release`, and `PATCH /lots/{lot_id}/received-at`; all four route templates added to `DASHBOARD_KEY_ALLOWLIST`, no GPT schema changes (office 30 / Floor 22). Manual SKU/lot upserts add to the sole live key; STAGED/found-inventory pins are tagged `staged_lot`; auto-FIFO pins receive a default 48-hour expiry; product, lot, line-remaining, and sibling-line competition reject with 409 `OVER_ALLOCATION`. `ship_order` now always consumes this line's lot pins first, then deterministic takeable FIFO, and converts full rows to shipped or partial rows to superseded original + sole active leftover + shipped slice. Ship void coalesces shipped slices onto the active leftover (or reactivates full consumes), retaining `last_ship_transaction_id`; restore reverses coalescing or returns 409 `RESTORE_SPLIT_MISSING`. | PRs 1–2 left allocation rows read-only: operators could see `unallocated` / `partial_allocation` but could not reserve stock, shipping could orphan active claims, and void/cancel/edit/merge operations had no reservation state machine. Found-inventory STAGED lots also had no dashboard path to record the received timestamp that clears `missing_lot_dates`. | Reverting restores the old inlined `ship_order` FIFO without the `id ASC` tie-break, so equal-date lot selection becomes nondeterministic again. More critically, any live allocation rows then become ghost reservations: ship no longer marks them shipped, partial-ship void can create/leave duplicate-key pressure instead of coalescing, restore cannot reconstruct 60 active + 40 shipped, and cancel/edit/merge leave stale exclusive claims. Keep `soa_active_sku_uniq` / `soa_active_lot_uniq`; do not replace POSTED_LINES/effective status with raw ledger rows; consume-on-ship must remain unconditional and must not be placed behind `ALLOCATIONS_ENFORCED`; GETs must never persist TTL expiry. Removing the new route templates from the dashboard allowlist 403s the allocation/date UI, while adding them to GPT schemas violates the dashboard-only boundary and risks the office 30-op cap. | `main.py`, `tests/test_sales_order_allocations.py`, `tests/test_dashboard_api_key.py` |
| 86 | 2026-08-20 | FR-4 PR 3 review fix round 1 (`docs/reviews/ef709e5-pr3-review.md`; branch `feat/044-so-allocations`) — **SUPERSEDED by row 87; NOT deployed** | Correct pieces retained by round 2: split leftovers have `last_ship_transaction_id=NULL`; release follows `POST .../release`; dashboard DELETE permission again covers notes only; allocation guard/error coverage was added. The round-1 restore selector extension was defective: it treated `superseded/split_on_ship` plus an old transaction marker as attribution and flipped the historical row shipped at full quantity without consuming live pounds. | It fixed the original leftover-marker collision and endpoint divergence, but replaced the restore error with phantom allocation accounting: a 100 lb historical row plus B's 40 lb could appear shipped from an original 100 lb reservation and a restore correction could drive ledger on-hand negative. | Do not restore the selector extension. `last_ship_transaction_id` is ambiguous after void/coalesce/re-ship and cannot determine restore quantity or attribution. Retain only the null leftover marker, POST release route, DELETE guard, and independent coverage. | `main.py`, `tests/test_sales_order_allocations.py`, `tests/test_dashboard_api_key.py`, `FACTORY_LEDGER_CHANGELOG.md` |
| 87 | 2026-08-20 | FR-4 PR 3 review fix round 2 (branch `feat/044-so-allocations`) — **LOCAL ONLY, NOT deployed** | Restore quantity is now authoritative from effective-posted `ledger_current_transaction_lines` joined to the SO shipment line. `_restore_ship_allocations` locks products, excludes/persists expired auto allocations, preflights complete active allocation coverage for every target line, returns atomic 409 `RESTORE_SPLIT_MISSING` when short, then consumes exactly the ledger pounds via `_consume_allocation_row`; it never flips superseded rows. API tests prove S1 A100→void→B40→restore A rejects with live60 unchanged/on-hand60; S2 void B then restore A consumes live100 and ends shipped100/live0/on-hand0; S3 T40→void→C30→restore T ends T40+C30 shipped/live30/on-hand30. Every checkpoint asserts live+shipped=100 and nonnegative on-hand. | Marker-based restore attribution is unsound because a split historical row can carry a transaction marker from an earlier void/coalesce. The round-1 selector extension minted phantom pounds instead of consuming currently reserved pounds. | Reverting permits restored allocations to exceed both the original reservation and ledger stock. Permanent rule: restore demand comes from effective ledger lines; all demand must be preflight-covered by live unexpired allocations; consume through the normal split helper; superseded rows are audit history only. | `main.py`, `tests/test_sales_order_allocations.py`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 84 | 2026-08-19 | FR-7 PR 2 readiness verification fix (branch `feat/044-so-allocations`) — **LOCAL ONLY, NOT deployed** | The incomplete-lot FIFO predicate now suppresses `unstaged` when the same SO line already has any live lot-level allocation on that lot. A partial pin still produces `partial_allocation` and `missing_lot_dates`; the pin exception does not clear either of those independent blockers. Eight DB tests now directly cover: partial-pin semantics, normal non-staged/non-found lots with null dates, physical `no_production` products, cross-order stock competition, non-diverged `partial_ship` queue inclusion, cancelled-line exclusion, restored shipment effectiveness, and exclusion of closed/cancelled expected receipts from `inbound_cover`. Full local suite: 172/172. | PR 2 incorrectly re-added `unstaged` from the unpinned balance of an incomplete lot even after the line explicitly pinned that lot, yielding three blockers where the design requires two. Several edge semantics were only indirectly covered. | Reverting makes a partially pinned incomplete lot report `unstaged` alongside `partial_allocation` and `missing_lot_dates`. Permanent rule: for a specific line+lot, any live lot-level pin clears only `unstaged`; it does not clear allocation completeness or missing-date checks. Keep cancelled lines and non-open receipts outside readiness, and keep restored posted ships in `shipped_effective`. | `main.py`, `tests/test_sales_order_readiness.py` |
| 83 | 2026-08-19 | FR-7 PR 2 read-only sales-order readiness (branch `feat/044-so-allocations`) — **LOCAL ONLY, NOT deployed; no allocation writes / no ship-path change** | `GET /sales/orders`, `GET /sales/orders/{id}`, and `GET /sales/orders/fulfillment-check` now share one batched page/set CTE over effective-posted `ledger_current_transaction_lines` / `ledger_current_transactions`, live `sales_order_allocations` (`active` + formula-only TTL), posted SKU/lot on-hand, and open expected receipts. Physical `shipped_effective_lb = SUM(ABS(tl.quantity_lb))` through `sales_order_shipments.transaction_id` with product match; `sales_order_shipments.quantity_lb` and the mutable counter are never readiness inputs. Physical-only formulas use ε=0.0001 for remaining / available / coverable (including sibling-line competition) / shortage / unallocated need / inventory-ready; services are excluded. Stable blockers: block shortage, unallocated, partial_allocation, unstaged, missing_lot_dates, configurable `not_floor_ready` (`FACTORY_READY_REQUIRED`, default true), fulfillment_diverged; warn no_ship_date and inbound_cover (never stock); info service_only. `dispatch_ready = inventory_ready AND no block`. List adds summary readiness fields; detail and fulfillment-check add line readiness; fulfillment open set adds partial_ship and effectively diverged shipped/invoiced orders. GETs contain no allocation UPDATE, so expired rows remain active until a future write. Office OpenAPI descriptions are additive; counts remain office 30 / Floor 22 (existing guard). Nine new DB tests cover two coverable unallocated orders, zero-allocation visibility, shortage+inbound, fully allocated inbound warn, sibling competition, partial allocation, FG+pallet service exclusion, ship-void divergence, 4-decimal ledger precision vs 2-decimal shipment linkage, staging/date/floor config, TTL no-write, and EXPLAIN validity. Full suite 164/164. Rolled-back synthetic 50-order `EXPLAIN (ANALYZE, BUFFERS)`: 50 result rows, one 25-row live-allocation scan, one 10-row shipped aggregate, planning 6.209 ms / execution 1.118 ms. | Existing fulfillment-check double-counted global inventory per line, included services, omitted partial shipments, and trusted the void-blind shipped counter; list/detail exposed no allocation readiness, so a not-ready order could have no explicit blocker. | Reverting restores counter-based remaining and per-line inventory queries, hides void/correction divergence and allocation competition, and can silently label zero-allocation demand feasible. Do not replace `shipped_effective_lb` with `quantity_shipped_lb` or `sales_order_shipments.quantity_lb`; do not count expected receipts as on-hand/coverable; do not make GET persist TTL expiry; keep service exclusion and the 30/22 operation-count guard. | `main.py`, `openapi-gpt-v3.yaml`, `tests/test_sales_order_readiness.py` |
| 82 | 2026-08-19 | FR-4 PR 1 migration 044 production schema sync (branch `feat/044-so-allocations`) — **production dump confirms 044 objects present; application behavior still unchanged** | Ran `scripts/dump_prod_schema.sh` with Homebrew PostgreSQL 17.11: `tests/schema/schema.sql` is now a 3,940-line, schema-only production dump with zero data rows. It contains `sales_order_allocations`, the identity sequence and PK, `soa_active_sku_uniq`, `soa_active_lot_uniq`, four supporting indexes, and all seven FKs directly. The temporary pending `\ir ../../migrations/044_sales_order_allocations.sql` block is gone. This cleanup observed the production schema; it did not execute migration 044 or deploy application code. Full local suite passed 155/155 against `factory_ledger_test`. | Once production contains 044, retaining the pending include makes the test fixture a hand-layered schema rather than the canonical production dump and obscures whether production/test structure has drifted. | Reverting restores a stale pre-044 dump plus an out-of-band migration include. Fresh tests may still pass because 044 is idempotent, but the fixture stops representing production directly and future dump diffs become misleading. | `tests/schema/schema.sql`, `CHANGE_LOG.md`, `FACTORY_LEDGER_CHANGELOG.md` |
| 81 | 2026-08-19 | FR-4 PR 1 review fix (branch `feat/044-so-allocations`) — **LOCAL ONLY, NOT deployed** | `tests/test_sales_order_allocations.py`: new `test_full_void_then_restore_cycle_holds_under_unique_index` = design Q2 worked example end-to-end under the partial unique index — allocate 100 lot-level → ship 40 split (superseded original + 60 active + 40 shipped) → void coalesces to ONE live 100 (`void_coalesced`; naive re-activate of the shipped row asserted to raise `UniqueViolation`) → restore: leftover 100→60, row back to `shipped` with `ship_transaction_id` set → end state {active 1×60, shipped 1×40, superseded 1×100}, `source` never overwritten, second void/restore round still clean. Suite re-run with the local test DB per `scripts/setup_test_db.sh` (`TEST_DATABASE_URL=postgresql://localhost:5432/factory_ledger_test`): 155/155, 16/16 allocation tests executed (none skipped). `created_by` stays nullable source-tag (owner approved). | Review verification ran without `TEST_DATABASE_URL` (132 skipped, allocation tests never executed); only the void half of the worked example was pinned. | Removing the test leaves the restore leg (`RESTORE_SPLIT_MISSING` precondition, leftover shrink, shipped flip) unguarded for PR 3. Running pytest without `TEST_DATABASE_URL` silently skips every DB test — not a pass. | `tests/test_sales_order_allocations.py` |
| 80 | 2026-08-19 | FR-4 PR 1 — `sales_order_allocations` schema (branch `feat/044-so-allocations`) — **LOCAL ONLY: migration 044 NOT applied to prod, NOT deployed, zero behavior change** | New idempotent `migrations/044_sales_order_allocations.sql` (PR 1 of `docs/designs/044-so-allocations-design.md`): one row per exclusive FG reservation slice against an SO line — hybrid `lot_id` NULL (SKU-level) / NOT NULL (lot-level pin); FKs sales_orders, sales_order_lines, products, lots, transactions (`ship_transaction_id`, `last_ship_transaction_id`), self (`split_from_id`); `quantity_lb numeric(14,4) > 0`; `status` active/released/shipped/superseded; `source` manual/auto_fifo/staged_lot; CHECK `allocations_shipped_has_txn` (shipped ⇔ ship_transaction_id) and `allocations_released_has_time` (released ⇒ released_at); partial unique LIVE indexes `soa_active_sku_uniq` (line) WHERE active ∧ lot NULL and `soa_active_lot_uniq` (line, lot) WHERE active ∧ lot NOT NULL, plus `soa_active_product_idx`, `soa_active_lot_idx`, `soa_order_idx`, `soa_ship_txn_idx`. No `business_date`, no session GUCs. Deviation from the design DDL: `created_by text` nullable, NO `'legacy-shared-key'` default (interim FR-15 convention — `caller_source_tag()` / `expected_receipts.created_by`). `tests/test_sales_order_allocations.py` (15): columns/types/defaults, FKs, CHECKs, unique-live semantics incl. design Q2 void-coalesce worked example and cross-line same-lot pins, idempotent re-run, file hygiene, OpenAPI 30/22 guard. `tests/schema/schema.sql` ends with a pending `\ir` 044 block (041 precedent). No `main.py` / endpoint / dashboard / GPT schema edits. Suite 154/154; office 30 ops, floor 22. | Factory Ledger has no way to reserve finished goods against a sales order: open orders double-count the same stock, staged lots (e.g. `STAGED-FEESERS-67476`) are FIFO landmines, and "ready to ship" is undefined. PR 1 lands the storage so PR 2 (readiness reads) and PR 3 (allocate + consume-on-ship) can build on it without a behavior change. | Reverting before PR 2/3 is harmless (empty table). After PR 3 lands, dropping the table or the two partial unique indexes re-permits duplicate live rows per (line) / (line, lot) — the void-coalesce and upsert logic depend on them; never drop `soa_active_sku_uniq` / `soa_active_lot_uniq`. Do not add a `'legacy-shared-key'` default to `created_by`. `schema.sql` pending `\ir` block must be removed only after prod apply + re-dump. | `migrations/044_sales_order_allocations.sql`, `tests/test_sales_order_allocations.py`, `tests/schema/schema.sql` |
| 78 | 2026-08-19 | FR-10 verification — **LOCAL / REVIEW PENDING, NOT DEPLOYED** | Rebuilt the local test database from `tests/schema/schema.sql`; verbose isolation found the prior full-run stall at `tests/test_production_warning.py::test_bom_formula_includes_verification_notes`, immediately after FR-10's auth TestClient. Restored HTTP auth coverage (401 missing / 403 wrong / 200 dashboard scoped key) with the established post-TestClient `_db_connection.rollback()` cleanup, and split the FR-10 contract into seven independent tests. Rows missing batch/case unit definitions now route to explicit Other lists, and the tile renders product names/output pounds with count unavailable. Full local suite passes **139/139** (132 baseline + 7 FR-10). | An unrolled-back session transaction retained `AccessShareLock` across the following TestClient setup; missing unit definitions could also be represented as a silent zero or omitted Other packing row. | Removing the rollback can reintroduce the sequential-suite stall. Combining the seven assertions again obscures the failing contract. Routing missing definitions back into classified totals hides uncountable activity; filtering Other only by cases hides valid pack activity. | `main.py`, `tests/test_production_today_tile.py`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
| 77 | 2026-08-19 | FR-10 Today So Far — **LOCAL / REVIEW PENDING, NOT DEPLOYED** (dashboard JS v35 / CSS v24) | Added dashboard-key-authenticated, allowlisted `GET /production/today-tile?date=YYYY-MM-DD`. Its ET day window reads only `ledger_current_transactions` / `ledger_current_transaction_lines` with `effective_status='posted'`, reuses `_production_family_from_name`, applies `default_batch_lb × yield_multiplier` for made units, and returns full-precision per-family make/pack metrics plus explicit Other product lists. Granola pack rows split 10 lb and 25 lb cases; retail is deliberately `granola_retail_cases` with `granola_retail_bag_count_available=false` because no retail bag-unit field exists. The Operations tile loads inside `refreshAll`, has isolated loading/error/zero states, and never combines cases with bags. Focused DB tests cover auth, ET date scoping, coconut yield, void exclusion, pack splits, Other, and date validation. | The calendar mixes display-level aggregation with an unauthenticated route and can silently omit unrecognized or insufficiently defined products; no authenticated canonical today contract existed for the dashboard. | Removing scoped auth/allowlist blocks the tile. Replacing current-ledger reads with raw tables reintroduces voided/superseded activity. Dividing coconut raw lb solely by `default_batch_lb` overstates pans. Do not rename retail cases to bags until a canonical sellable-unit count exists. | `main.py`, `tests/test_production_today_tile.py`, `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
| 76 | 2026-08-19 | FR-12 verification — **LOCAL / REVIEW PENDING, NOT DEPLOYED** | Local test DB workflow verified `tests/test_recent_ledger.py` 4/4 and full pytest suite 132/132 (128 baseline + 4 FR-12 tests). `node --check dashboard/dashboard.js`, Python compilation, and `git diff --check` pass. Local dashboard preview at 390px and desktop confirms the explicit error treatment for the not-yet-deployed endpoint, a 44px Refresh control, and no document horizontal overflow at 390px after the shared mobile `.header-right` wrapping rule. | The header’s calendar row previously retained intrinsic width at mobile size, making the document wider than 390px even though the new cards were responsive. The endpoint does not exist on the remote API until a future deployment, so the local static preview correctly demonstrates its visible error/retry state. | Removing the mobile header wrapping rule restores document-level horizontal overflow at 390px. Removing endpoint tests leaves global correction ordering, void/restore visibility, direction mapping, max limit, and scoped-key authorization unguarded. Do not treat the local preview’s remote 404 as a frontend empty state or deploy evidence. | `dashboard/dashboard.css`, `tests/test_recent_ledger.py` |
| 75 | 2026-08-19 | FR-12 Recent Entries — **LOCAL / REVIEW PENDING, NOT DEPLOYED** (dashboard JS v34 / CSS v23) | Added authenticated, dashboard-scoped-key allowlisted `GET /ledger/recent?limit=20` (default 20, max 50): a single read-only SELECT unions original transactions and append-only `ledger_corrections`, globally sorts each row by its own `entered_at DESC, event_id DESC`, and returns stable ID, type, calendar `business_date`, TIMESTAMPTZ `entered_at`, effective status sourced from `ledger_current_transactions`, raw status, target/correction relationship metadata, explicit server-derived direction, and effective lines (product/quantity/unit/lot). Originals remain in the feed when voided; void/amend/restore corrections are separate events and may have empty lines. Direction mapping is receive→received, make→produced, pack→packed, ship→shipped, adjust→adjusted_in/out from effective net line quantity. The dashboard adds Recent Entries immediately after Operations with automatic load, manual retry/refresh, visibly distinct loading/error/empty states, status badges from endpoint fields, business-date + ET entered-time labels, mobile cards, and 60-second visible-tab polling paused by Page Visibility. New endpoint tests cover ordering, limit/max, void/restore, direction, and scoped-key auth. No migration. | Existing history returned originals ordered by original creation and nested corrections, so a newly entered void/correction could not appear as its own globally recent audit event; unauthenticated dashboard routes also hid voided records. | Reverting the route/allowlist 403s the Recent Entries tab. Replacing `ledger_current_*` with raw transaction tables misstates corrected/voided status and lines. Omitting correction rows or sorting them by target creation hides audit events. Moving direction inference to the client risks quantity-sign mistakes. Reverting cache bumps can leave browsers on stale assets without the tab. | `main.py`, `tests/test_recent_ledger.py`, `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
|---|------|------|-------------|-------------------|-------------------|----------------|
| 74 | 2026-08-19 | FR-1 Supplies Incoming column — **LOCAL / READY FOR REVIEW, NOT DEPLOYED** (existing expected-receipts backend prerequisite live at `780a5ff`; dashboard JS v33 / CSS v22) | Supplies inventory refresh now reads the already dashboard-key-allowlisted `GET /expected-receipts?status=open&limit=500`; it filters to records with positive server-computed `remaining`, groups by `product_id`, and renders the sum as `N lb` in Incoming. An em dash means no open receipt remains; an unavailable read is explicitly labeled rather than masquerading as zero. Expanded product rows list each open record’s supplier, remaining quantity, expected date, and reference before the existing lazy FIFO-lot cards. Read-only UI only: no POST/PATCH and no backend/schema/API/allowlist change. Cache-busters v32/v21 → v33/v22. | Expected receipts (FR-2) are already the source of truth for outstanding incoming inventory, but Supplies could only show a placeholder. This exposes the live remaining quantity without duplicating or storing receipt state. | Reverting restores the intentional em-dash placeholder. Do not sum `expected_qty`: partially received receipts must display the backend’s posted-ledger-derived `remaining`; do not include closed/cancelled records. If the expected-receipts GET is unavailable, retain its explicit unavailable state—an em dash means no incoming records, not a failed read. | `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
| 73 | 2026-08-18 | Supplies dashboard frontend — **LOCAL / READY FOR REVIEW, NOT DEPLOYED** (backend prerequisite live at `f563100`; dashboard JS v32 / CSS v21) | New top-level Supplies tab. Inventory: Ingredients / Packaging / All sub-tabs (Packaging combines packaging + consumable; All de-duplicates all three supply categories), per-tab low-stock counts, alphabetical product-name search in a sticky scroll panel, every zero-on-hand product, Name / On Hand / Unit / Incoming / Status columns (Incoming is always em dash with one future-PO TODO), amber low rows + Low Stock only when `is_low=true`. Rows are click/Enter/Space expandable; multiple remain open; `GET /supplies/inventory/{id}/lots` fires only on first expand and is session-cached, then defensively sorted by lot date; extensible field-list cards show lot code/date/remaining with loading, empty, and error states. Request Supply modal uses the inventory list plus Product not listed (free-text item + required unit encoded in `item_text`), optional positive qty/note, required Arturo/Luz/MG/Other requester and name for Other, API validation/error feedback, and disabled submit while saving. Requests load newest-first from `status=all`, default to open only with open-count badge, optionally show done, PATCH open→done, and refresh after create/status changes. Responsive table/modal behavior and keyboard access verified locally; cache-busters v32/v21. No backend/schema/GPT files changed. Existing suite: 128 passed; no dashboard JS test runner exists, so none was added. | Staff had no dashboard surface for packaging/consumable inventory, configured low-stock alerts, FIFO supply-lot visibility, or floor-to-office supply requests even though the five backend routes were live. | Reverting removes the Supplies tab and request workflow. Removing the category merge drops consumables from Packaging; counting raw concatenated arrays risks duplicate badges; replacing Incoming em dash with 0 fabricates nonexistent PO data; fetching lots during page load breaks lazy/cached behavior; showing a success label for `is_low=false` violates threshold semantics; removing the scoped `fetchSalesAPI` helper usage makes cross-origin calls unauthenticated. Deploying without the v32/v21 cache bump can strand returning browsers on the prior dashboard. | `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
| 72 | 2026-08-18 | Supplies backend (packaging/consumables inventory, low-stock thresholds, supply requests) — **migration 043 APPLIED to prod 2026-08-18 15:17 ET** (verified read-only: type CHECK admits `consumable`, `low_stock_threshold` present, `supply_requests` 0 rows + 7 constraints + 2 indexes; `tests/schema/schema.sql` re-dumped, pending `\ir` removed); backend **DEPLOYED 2026-08-18** on main `f563100` (Railway successful; all five live endpoints verified) | Migration `043_supplies.sql`: `products.type` CHECK widened to add `consumable` (`packaging` already existed; category = `products.type`, text+CHECK, no enum); `products.low_stock_threshold numeric` NULL (≥ 0, all types incl. ingredients, NULL = no alerting); new `supply_requests` (product_id FK xor item_text, qty > 0 nullable, note, requested_by, status open|done, created_at, done_at; CHECK done_at ⇔ status=done). `main.py`: `fifo_lot_balances()` / `FIFO_LOT_ORDER_SQL` beside `lot_on_hand()` = the inline ship/make/pack allocation query (posted-only per-lot SUM, HAVING > 0, ORDER BY COALESCE(received_at, created_at)) extracted for read paths; new dashboard-only endpoints `GET /supplies/inventory` (ALL products incl. zero-inventory, on_hand = posted-only ledger SUM via LEFT JOIN, is_low = on_hand < threshold, `?category=`), `GET /supplies/inventory/{id}/lots` (FIFO breakdown via the helper, `?include_empty`), `POST/GET /supply-requests`, `PATCH /supply-requests/{id}` (open→done only) — all on `DASHBOARD_KEY_ALLOWLIST`; `openapi-gpt-v3.yaml` untouched (30 ops). `tests/test_supplies.py` (12) incl. lots order == `/ship` preview multi-lot FIFO; suite 128/128. `tests/schema/schema.sql` is the post-043 production schema dump. | No inventory view for packaging/consumables (existing views hide zero-balance items and only alert ingredients < 100 via `low_stock_alerts`), no per-product threshold, no way for the floor to ask the office for supplies. | Reverting main.py while 043 is applied leaves harmless unused objects. Reverting 043 after `supply_requests` rows exist loses them; narrowing the type CHECK back fails if any `consumable` product exists. **Never store on_hand / is_low** — both are pure ledger reads by design. Removing the five routes from `DASHBOARD_KEY_ALLOWLIST` 403s the future Supplies tab. Re-inlining the FIFO query instead of calling `fifo_lot_balances()` risks the lots view drifting from what ship/make actually consume. `low_stock_alerts` view (pre-existing) still reads raw `transaction_lines` (not posted-only) — untouched here. | `migrations/043_supplies.sql`, `main.py` (`fifo_lot_balances`, SUPPLIES section, `DASHBOARD_KEY_ALLOWLIST`, `SupplyRequestCreate/Update`), `tests/test_supplies.py`, `tests/schema/schema.sql` |
| 71 | 2026-08-18 | FR-2 supplier activity + process hard rules (**DEPLOYED 2026-08-18 14:48 ET** — push 1b94553..ade1487 approved per-action; Railway deployment 61efe81a SUCCESS, one 502 during cutover then /health 200, `GET /expected-receipts` dashboard key 200; **migration 042 APPLIED to prod 2026-08-18 14:55 ET** — `UPDATE 8`, verified: 51 suppliers = 43 active + 8 inactive (FOUND, FOUND INVENTORY, INITIAL INVENTORY, Inventory Correction, INVENTORY FOUND, Inventory Intake, PHYSICAL COUNT, UNKNOWN); live `POST /expected-receipts` supplier_name "PHYSICAL COUNT" → 422 SUPPLIER_INACTIVE with 5 candidates, nothing created; `GET /suppliers` → 43, sentinels absent) | Receiving auto-match and preview lookahead now skip INACTIVE suppliers (`receive()` commit, `preview_expected_receipt_match`); resolution/candidates/`GET /suppliers` were already active-only. Verified 041 backfill re-run semantics: deactivated rows stay inactive, deleted rows come back active → deactivate, never delete. New `migrations/042_deactivate_sentinel_suppliers.sql` deactivates the 8 ledger pseudo-suppliers (found / found inventory / inventory found / physical count / initial inventory / inventory correction / inventory intake / unknown) by normalised name. `CLAUDE.md` `## Hard rules`: no push/merge to main without explicit per-action approval this session; deploy steps confirmed one at a time; hunk-level staging on the shared checkout. Suite 116/116. | An open expected receipt for a since-deactivated supplier could still be auto-linked; sentinel names from the backfill would pollute supplier pick-lists; main 778e0f9 was pushed without the agreed step-by-step gating and FR-2/B-2 hunks got cross-committed. | Reverting the active check lets receipts link to deactivated suppliers again. Deleting (instead of deactivating) sentinel suppliers resurrects them active on any 041 re-run. Removing the CLAUDE.md hard rules re-permits ungated pushes to main. | `main.py` (`receive()`, `preview_expected_receipt_match`), `tests/test_expected_receipts.py`, `migrations/042_deactivate_sentinel_suppliers.sql`, `CLAUDE.md` |
| 70 | 2026-08-18 | dashboard B-2 (cases / batches / workers / search / units) | **DEPLOYED 2026-08-18 14:25 ET** (main fast-forwarded c22c180 → 778e0f9; Railway 4e24da3e SUCCESS, Netlify 6a84a363 ready). Live checks all PASS: `/dashboard/api/inventory/batches` lists 4 coconut batches, `yield_multiplier` 1.11 / `made_unit_size_lbs` 399.6, lot 26-05-06-COCO-003 4,795.2 lb → `batch_count` 12.0 (product totals match a read-only DB recompute, e.g. Sweetened Flake 25,551.6 lb → 63.9); floor confirmed on live data — shipment line Graham Cracker Crumbs – 50 LB 1,690 lb / 50 = 33.8 → `unit_count` 33 (round would be 34), 164/164 shipment lines + 19/19 finished-goods lots == floor; ingredients endpoint returns `uom` on 59/59 items and 91/91 lots (54 lb, 4 unit, 1 "50 lb bag"); Netlify index.html serves dashboard.js?v=31 / dashboard.css?v=20, search placeholder "Search SKU, lot, SO, customer..." (no "supplier"), "Workers on Floor" absent from index + process-flow. (Landed as 778e0f9; renumbered 69 → 70 on landing — row 69 was already taken by the FR-2 GPT follow-up, row-28 precedent). Prod verified 2026-08-18: all 25 active batch products are `type='batch'`; `yield_multiplier`=1.11 is entered data on the three sweetened-coconut batches (column default 1.0). Floor lbs→cases on the dashboard (`Math.floor` in `fmtQty`/`productionUnitCount`/process-flow; `_floor_unit_count` on dashboard `unit_count` fields). Batch panel no longer filters by the granola-only `batch_skus` config list — it selects all active `type='batch'` products in coconut/granola/graham/chips/sprinkles and applies `yield_multiplier` so coconut pans are `on_hand / (default_batch_lb * yield)` (4795.2 / (360×1.11) = 12, not 13.3). Removed the hardcoded "Workers on Floor 8/10" card (no worker/headcount table). Search placeholder dropped "supplier" to match `/dashboard/api/search` (products, lots, orders, customers). Ingredient panel reads `products.uom` for headers/values. Inventory still ledger SUMs only. Suite 113/113 at authoring, 115/115 at landing. Cache-bust CSS v20 / JS v31. | Case counts could round up and overstate available cases; coconut (and any future graham/chips/sprinkles) batches were missing or counted without hydration yield; a fake 8/10 labor widget sat on Production Lines; search promised supplier lookup it does not do; packaging/`50 lb bag`/`container` items were labeled as lb. | Reverting restores rounded-up cases, a granola-only batch list that ignores yield, the fake workers card, a supplier search placeholder, and lb-only ingredient headers. Reverting only the cache-bust leaves returning browsers on stale JS/CSS. | `main.py` (`dashboard_api_batches`, `dashboard_api_ingredients`, dashboard `unit_count`), `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `dashboard/process-flow.html`, `tests/test_dashboard_b2.py` |
| 69 | 2026-08-18 | office GPT schema + instructions (FR-2), **NOT PASTED** into the GPT editor (repo files DEPLOYED with main 778e0f9 2026-08-18 14:25 ET — backend now serves `POST /expected-receipts`, so the paste is unblocked) | `openapi-gpt-v3.yaml` 3.4.0 → **3.5.0**: `listProducts` (GET /bom/products) removed, `createExpectedReceipt` (POST /expected-receipts, `ExpectedReceiptCreate` schema, created_by default `gpt-sales-admin`) added — **exactly 30 ops**, all descriptions ≤300 chars. `gpt-instructions-v3.md` v3.7.0 → **v3.8.0** (7,982 chars): `## EXPECTED RECEIPTS` routing rule added, `listProducts` dropped from QUERIES, `createSalesOrder`→`createOrder` repo drift fixed, redundant phrasing trimmed to fit the cap (details in CHANGE_LOG 14:32; all 10 Permanent Rules intact). Floor GPT untouched. Backend: `caller_source_tag()` fills `expected_receipts.created_by` ('dashboard' for the scoped key, caller tag for master key, else NULL). | 30-op cap left no room for the FR-2 create op; office GPT needs a way to record incoming deliveries; created_by needed a non-fake attribution until FR-15. | Re-adding `listProducts` without removing another op breaks the 30-op HARD RULE (editor rejects the paste). Pasting this schema before the FR-2 backend is live makes `createExpectedReceipt` 404 in the GPT. Instructions edits that push the file over 8,000 chars are rejected by the editor — measure chars (not bytes) before every paste. Reverting `caller_source_tag()` to a placeholder reintroduces fake user ids in `created_by`. | `openapi-gpt-v3.yaml`, `gpt-instructions-v3.md`, `main.py` (`caller_source_tag`, `create_expected_receipt`), `tests/test_expected_receipts.py` |
| 68 | 2026-08-18 | FR-2 expected receipts (backend/dashboard **DEPLOYED 2026-08-18 14:25 ET** with main 778e0f9 — Railway 4e24da3e, Netlify 6a84a363; live `GET /expected-receipts` with dashboard key → 200; migration 041 **APPLIED to prod 2026-08-18 ~14:40 ET** — 51 suppliers backfilled, expected_receipts + transactions.expected_receipt_id live, verified read-only; `tests/schema/schema.sql` re-dumped from prod) | New `suppliers` (backfilled from ledger shipper names via `supplier_name_norm()`, unique on normalised name), `expected_receipts` (open/closed/cancelled, qty lb, date, reference, notes), nullable `transactions.expected_receipt_id` set at receive-commit INSERT (transactions is append-only). API: `POST/GET /expected-receipts`, `GET/PATCH /expected-receipts/{id}`, `GET/POST /suppliers` — all on `DASHBOARD_KEY_ALLOWLIST`; supplier_name resolves case/whitespace-insensitively, no match → 422 with ≤5 candidates, never auto-created. `receive()` commit auto-links the FIFO open record for (product, supplier), recomputes received as the posted-only ledger SUM and auto-closes at ≥ expected (over-receipt closes; remaining floors at 0 in responses). Dashboard "Expected Receipts" tab (v30/v19). `tests/test_expected_receipts.py` (15); suite 110/110. | Office had no record of what was coming in, so receiving could not be reconciled against expectations and nobody could see overdue deliveries. | Reverting main.py while 041 is applied leaves harmless unused tables (nothing reads them for balances). Reverting 041 after rows exist orphans `transactions.expected_receipt_id` (drop the FK column first). **Never add a stored remaining/received column** — remaining is a pure SUM by design (mirrors the on-hand invariant); any cache would drift on voids. Removing the routes from `DASHBOARD_KEY_ALLOWLIST` 403s the new tab. `tests/schema/schema.sql` is now the post-041 prod dump (pending `\ir` block removed). | `migrations/041_expected_receipts.sql`, `main.py` (FR-2 section before SHIP ENDPOINTS; `receive()`; `DASHBOARD_KEY_ALLOWLIST`), `tests/test_expected_receipts.py`, `tests/schema/schema.sql`, `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
| 67 | 2026-08-18 | API auth — DASHBOARD_API_KEY env-only | **DEPLOYED 2026-08-18 13:29 ET** (commit 3d8d3b8; Railway deployment 133c4bf4 SUCCESS; live smoke: health 200, dashboard key → 200 on `GET /sales/orders`, 403 on `POST /make/preview`; bogus key 403; no key 401). Removed the literal `dashboard-key-2026` fallback from `DASHBOARD_API_KEY` in main.py — the scoped dashboard key now comes ONLY from the environment (same pattern as master `API_KEY`, which never had a fallback), and `startup()` raises `RuntimeError("DASHBOARD_API_KEY env var required …")` if it is unset, right after the existing `API_KEY` check. `tests/conftest.py` sets `DASHBOARD_API_KEY=test-dashboard-key` next to its `API_KEY` default so `with TestClient(main.app)` startup still succeeds; suite 95/95. Railway env verified to already hold `DASHBOARD_API_KEY` before push (`railway variables`). | The fallback meant a Railway env misconfiguration would silently keep accepting the well-known literal instead of failing; the row-62 TODO to drop it was left open. | Reverting re-introduces the silent literal fallback. If `DASHBOARD_API_KEY` is ever removed from Railway the app now refuses to start (fail-loud by design) — set the env var, do not re-add a literal. Any new test harness that boots the app must set `DASHBOARD_API_KEY` (or rely on conftest). | `main.py` (`DASHBOARD_API_KEY`, `startup()`), `tests/conftest.py` |
| 66 | 2026-08-17 | inventory recon (prod data) | Posted the reviewed inventory-variance plan: standalone QB-tagged ships (netted against `SUNSHINE-RECON-2026`, which was **not** voided), FG under-packs, Classic #9 / #1 bulk pack+ships, generic batch consumes to the Aug 14 count, FG residuals, 67476 Feesers staged lot, and new WIP product `15999` (banana/PB-chip mix, 1.00 lb token). Mini 100, 70088, and coconut batches excluded. Open SOs SO-260629-003 / SO-260814-002 / SO-260817-001 not shipped. Txns 1950–2026, shipments 333–340. In-transaction on-hand verify passed before commit. | Ledger on-hand for Sunshine, Blue Stripes, counted FG, and counted granola batches did not match QB billing + Arturo’s 2026-08-14 floor count. | Reverting or voiding 1950–2026 without a paired restore puts those SKUs back to the pre-recon overstated balances and removes `15999` / `STAGED-FEESERS-67476`. Do not void 1325–1342 as a “cleanup” of this row — the recon dump is the first 4,639/900/142/140 cs of billed Sunshine volume. | `docs/audits/inventory-variance-execution-2026-08-17.md`, `scripts/inv_recon_post_2026_08_17.py` (data write; no schema/API change) |
| 65 | 2026-08-13 | Floor GPT editor config (manual paste) | Second and final pending manual step from the 2026-08-12 deploy (rows 57/58) completed: Floor GPT (Factory Ledger - Floor 2.0) received `gpt-configs/dist/GPT_FLOOR_INSTRUCTIONS.md` (7,833 chars, under 8,000 cap; includes the MAKE production_warning relay rule) and `gpt-configs/schemas/openapi-floor.yaml` (22 ops, v4.1.0; all descriptions ≤300 chars per row 58). With row 64 (office GPT), both pending pastes from the Aug 12 checklist are now done — both GPTs match the repo schema/instruction sources. GPT-editor-side change only — no repo files modified. | Floor GPT was still running pre-row-57 instructions/schema, so the Kosher Ignition production_warning relay rule and updated op descriptions were not yet live in the editor. | Reverting the Floor GPT to older instructions drops the MAKE relay rule (operators could commit SS Classic #9 production without the verification note being relayed). Any future paste must stay under 8,000 instruction chars and 300-char descriptions, and go to the correct GPT (row 64 mixup precedent). | `gpt-configs/dist/GPT_FLOOR_INSTRUCTIONS.md`, `gpt-configs/schemas/openapi-floor.yaml` (pasted into GPT editor; no repo change) |
| 64 | 2026-08-13 | office GPT Actions config (manual paste) | Completed pending manual paste from 2026-08-12 deploy (rows 57/58): office GPT (Factory-Ledger 2.0) Actions restored to `openapi-gpt-v3.yaml` (30 ops, v3.4.0) — the floor schema had been pasted there by mistake, killing customer/order-entry ops. Instructions updated createSalesOrder→createOrder (v3 rename; op lives at POST /sales/orders). Verified end-to-end: Dingman's Dairy PO 26791 → SO-260813-001 created via searchCustomers → resolveProducts → createOrder with batched disambiguation. GPT-editor-side change only — no repo files modified. Remaining from Aug 12 checklist: Floor GPT paste (GPT_FLOOR_INSTRUCTIONS.md + openapi-floor.yaml) if not yet done. | Office GPT was running the 22-op floor schema, so all customer and sales-order operations (searchCustomers, resolveProducts, createOrder, order updates) were unavailable; instructions still referenced createSalesOrder, which no longer exists in v3. | Pasting any schema other than openapi-gpt-v3.yaml (30 ops, v3.4.0) into the office GPT kills customer/order-entry ops again; reverting instructions to createSalesOrder references an operationId that no longer exists. Any future schema paste must respect the 300-char description limit (row 58) and 30-op cap (row 44). | `openapi-gpt-v3.yaml` (pasted into GPT editor; no repo change) |
| 63 | 2026-08-18 | dashboard front-end auth key | **DEPLOYED 2026-08-18 13:11 ET** (feature 0082d7e; merge to main 063e6eb; Netlify deploy `6a84923d70e2240008b12e51` state ready; served `dashboard.js?v=29` / `mini-calendar.js?v=3`, master-key string absent from all served files; live dashboard page load + Sales Orders tab → all Railway calls 200 with the dashboard key). All five front-end consumers (`dashboard.js`, `mini-calendar.js`, `sankey.html`, `process-flow.html`, `traceability.html`) now send `dashboard-key-2026` (the scoped `DASHBOARD_API_KEY`, row 62) instead of the master key; cache-bust dashboard.js v28→29, mini-calendar.js v2→3. Master key string no longer appears under `dashboard/`. Verified against a local run of the row-62 backend using only the dashboard key: every request each page makes → 200; master-only routes → 403. | The Netlify bundle exposed the master API key (full write + `/admin/sql` access) to anyone viewing source. | Reverting re-exposes the master key in the browser bundle. **Deploy ordering hazard:** if this front-end goes live on Netlify before the row-62 backend is on Railway, every keyed dashboard call (Sales Orders tab, mini-calendar, notes CRUD, matrix export, sankey/process-flow/traceability) returns 403 `Invalid API key` — deploy backend first. Any new dashboard call to a route not on `DASHBOARD_KEY_ALLOWLIST` will 403 until the allowlist is extended. | `dashboard/dashboard.js`, `dashboard/mini-calendar.js`, `dashboard/sankey.html`, `dashboard/process-flow.html`, `dashboard/traceability.html`, `dashboard/index.html` |
| 62 | 2026-08-18 | API auth — scoped dashboard key | **DEPLOYED 2026-08-18 13:10 ET** (feature fa69b7b; merge to main 258baae/237c3aa; Railway deployment `ed8adc82-89b4-442a-b48b-1613ba626758` SUCCESS; gate verified live: dashboard key `/reason-codes`→200, `/admin/lots/duplicates`→403 not-authorized, master key→200). Added `DASHBOARD_API_KEY` (env; fallback `dashboard-key-2026`) as a second accepted key. `verify_api_key`/`verify_api_key_flexible` route through `_authorize_api_key()`: master `API_KEY` = full access unchanged; dashboard key allowed only on the 44 `(METHOD, route-template)` pairs in `DASHBOARD_KEY_ALLOWLIST` (dashboard/read-only GETs, SO header/status/line edits + `/ready`, notes CRUD, `POST /sales/orders/{id}/ship/preview|commit`, `POST /receive/preview|commit`); everything else (all `/admin/*` incl. `POST /admin/sql`, make/pack/adjust/void, raw `/ship` `/receive`, deletes, lot reassign/rename, product/customer creates) → 403 `API key not authorized for this endpoint`. Match uses the route template so path params cannot smuggle. 401/403 semantics for missing/unknown keys unchanged. `tests/test_dashboard_api_key.py` (11 tests); suite 90/90. | The Netlify dashboard (and sankey/process-flow/traceability pages) hardcode the master key in browser-visible JS, exposing `/admin/sql` and every write to anyone who reads the source. | Reverting re-collapses to a single all-powerful key; once the front-end is switched to `dashboard-key-2026` (row 63), reverting the backend breaks every dashboard call (403/401). Adding a route to the allowlist grants it to the browser key — keep `/admin/*` and raw ship/receive off it. | `main.py` (`DASHBOARD_API_KEY`, `DASHBOARD_KEY_ALLOWLIST`, `_authorize_api_key`), `tests/test_dashboard_api_key.py` |
| 61 | 2026-08-13 | Sales Orders matrix XLSX export | **DEPLOYED 2026-08-13 15:11 ET** (commit `1cdd9cb`; Railway deployment `68da81ca-0279-4cf8-b8a8-78dec6e817cf` SUCCESS). Restricted `GET /export/orders-matrix.xlsx` to finished-goods products at the SQL population boundary (`p.type = 'finished'`), retaining the existing exclusions for service and `no_production` lines and the hard failure for invalid case data on included finished goods. Added a route regression test that asserts this production-matrix boundary and verifies the XLSX remains loadable with its existing Cases/Pounds format. Production acceptance: HTTP 200, correct XLSX media type/attachment/CORS headers, valid 16,024-byte ZIP workbook, populated Cases/Pounds sheets (19 rows × 17 columns each), 12 product columns, raw SKU `11033` absent, and `/health` connected. No frontend, schema, migration, product data, order data, or GPT operation changed. | Production returned HTTP 422 for every matrix click because open order `SO-260811-002` contains raw ingredient SKU `11033` (Oats – Gluten Free), sold by pound with `case_size_lb = NULL`; the query admitted ingredients before attempting mandatory case conversion. | Reverting lets any open ingredient/packaging/batch sale with no case size block the entire matrix export again. Removing the existing invalid-case check instead would silently permit bad finished-goods conversion, so that guard must remain. | `main.py`, `tests/test_orders_matrix_export.py` |
| 60 | 2026-08-12 | dashboard Activity tab layout | **DEPLOYED 2026-08-12 14:49 ET** (feature 559dc6f, merge 413441c; Netlify serving v28/v18 within ~30s, verified: section order daily-entries→shipments→receipts, new JS/CSS live). Frontend-only: Activity tab sections reordered to Daily Entries → Shipping → Receiving; Shipping and Receiving tables truncated to the first 4 transactions with a "Show all (N more)"/"Show less" footer toggle (display-only — API calls unchanged at limit=100; hidden rows get `overflow-row overflow-hidden`, detail rows collapse with parents). Cache-bust dashboard.js v27→28, dashboard.css v17→18. No backend changes. | Daily Entries buried below two long logs; Shipping/Receiving pushed everything off-screen | Section order reverts and full 100-row logs render again; stale-cache users get old JS/CSS if version strings revert | `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css` |
| 59 | 2026-08-12 | Daily Entries view (timely-data-entry scoring) | **DEPLOYED 2026-08-12 14:36 ET** (feature 0886394, merge 42b5802; Railway live 14:36, Netlify v27/v17 confirmed, live endpoint verified against 2026-08-11 prod data — 9 txns, IDs 1892–1900). New dashboard read endpoint `GET /dashboard/api/activity/daily-entries?date=&date_mode=event\|entered`: every posted transaction for one plant-local day with per-line product/SKU (odoo_code)/signed quantity_lb, occurred_at, and created_at (DB entry time). Reads effective views only (`ledger_current_transactions` + `ledger_current_transaction_lines`, `effective_status='posted'`) so voids/amendments resolve. `late_entry` = created_at on a later America/New_York calendar day than business_date; only trusted when `created_at_source='database'` — backfilled rows (migration_backfill_039) report `entry_time_reliable=false` because their created_at is the migration run time, not real entry time. Event filter uses `business_date` (same naive-UTC→NY convention as dashboard_api_production, per migration 039). Deliberately NOT in either GPT Action schema (v3 at its 30-op cap; ops unchanged 30/22). Dashboard: "Daily Entries" section on Activity tab (date picker + mode select), late rows amber-highlighted with lag text; dashboard.js v27, dashboard.css v17. Tests: tests/test_daily_entries.py (3), suite 79/79. | No way to see when ledger entries were actually keyed in vs. when the event happened, so timely-entry scoring (entries made on a later day than the event) was impossible from the dashboard. | Removing the endpoint 404s the Daily Entries panel (dashboard.js v27 calls it on every refresh). Re-adding it to a GPT schema would breach the 30-op cap (row 44/58 constraints). If the late flag is ever computed from raw created_at without the created_at_source guard, every pre-039 row shows as "late" on 2026-08-11 (backfill timestamp). | `main.py`, `dashboard/index.html`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `tests/test_daily_entries.py` |
| 58 | 2026-08-12 | GPT schema description limits (editor constraint) | **NEW HARD CONSTRAINT DISCOVERED: the ChatGPT GPT editor rejects any OpenAPI description longer than 300 characters** (row 57's /make description hit it at 409). Audited BOTH schemas for all description/summary strings over 300 chars and trimmed the 3 offenders with no semantic loss: floor `/make` op description 409→281 (keeps sku_confirmation_required/sibling behavior AND the production_warning → relay verification_notes EN+ES verbatim + operator acknowledgment rule), floor `OrderStatus` schema description 326→267 (pre-existing; keeps all status transition semantics), v3 `/pack` 200 response 303→279 (pre-existing; keeps add-in preview/commit fields and warning behavior). Text-only changes — no operationIds, paths, or structure touched; op counts unchanged (30 v3 hard cap / 22 floor); both files valid YAML. | The GPT editor rejected openapi-floor.yaml at paste time, blocking the row-57 GPT rollout. | Re-expanding any description past 300 chars makes the editor reject the whole schema paste again. Trimming further must preserve the /make relay rule and OrderStatus transition semantics — they are behavioral instructions to the GPT, not just documentation. | `gpt-configs/schemas/openapi-floor.yaml`, `openapi-gpt-v3.yaml` |
| 57 | 2026-08-12 | production warnings (verification_notes visibility) | **DEPLOYED 2026-08-12 ~12:45 ET** (feature `193a4b2`; merges `147bf6d` + `dc2ed6a` pushed to main; Railway auto-deploy verified live: `/health` healthy, `GET /bom/batches/283/formula` returns verification_notes + production_warning, `POST /make` preview for 90025 returns production_warning with the full Kosher Ignition text, batch 107 formula correctly has NO warning; post-merge suite 76/76). GPT editor updates (Floor instructions paste + both schema refreshes) still pending — see deploy plan. Row-56 follow-up (rows renumbered 51→56, 52→57 on merge: origin/main had taken 51–55 for the Phase 1 / calendar work). `resolve_product_full` now also selects `verification_notes`/`verification_notes_es`; new `build_production_warning()` helper returns a warning block when the product's `verification_notes` is non-empty. `/make` (preview AND commit) adds `production_warning` {verification_notes, verification_notes_es?, message} to the response and appends the note to `preview_message`; `GET /bom/batches/{id}/formula` adds `verification_notes`(+`_es`) and the same `production_warning` block. Both GPT schemas updated on EXISTING operations only (response/description text for `getBatchFormula` and `make`): op counts unchanged at 30 (v3, at hard cap) and 22 (floor). Floor instructions source gained a MAKE rule — relay production_warning/verification_notes verbatim (EN + ES) BEFORE commit, operator must confirm — and dropped the redundant "For order dispatch, also quote new order_status." line (fully covered by the commitShipOrder receipt rule); dist rebuilt at 7,833 chars (< 8,000 hard limit). New `tests/test_production_warning.py` (6 tests); full suite 68/68 on local test DB. Local-env repairs (not committed/behavioral): `.venv-test` rebuilt on Python 3.14 with psycopg2-binary 2.9.12 (pin 2.9.9 has no cp314 wheel). CORRECTED ON MERGE: the "stray" append-only triggers found in the local test DB were actually Phase 1 migration 039 (row 51) applied by another session; origin/main's updated `tests/schema/schema.sql` includes them — the local `--fresh` rebuild from the then-stale schema.sql predated that pull. Post-merge suite re-run against the updated schema before deploy. | Row 56 recorded that the Kosher Ignition note on 90025/90026 (and codes 70013–70018) was invisible to floor operators: no Floor GPT endpoint or dashboard surface returned `verification_notes`. | Reverting main.py hides the Kosher Ignition (and any future) production requirement from every make/formula response — operators could start SS Classic #9 production without the owner/messenger lighting the oven. Reverting the instructions/schema text stops the Floor GPT from relaying the note even though the API sends it. Restoring the removed dispatch line costs headroom against the 8,000-char instruction limit. | `main.py`, `tests/test_production_warning.py`, `openapi-gpt-v3.yaml`, `gpt-configs/schemas/openapi-floor.yaml`, `gpt-configs/sources/floor-specific.md`, `gpt-configs/dist/GPT_FLOOR_INSTRUCTIONS.md` |
| 56 | 2026-08-12 | products catalog (prod DB data only, no code) | **LIVE IN PROD DB 2026-08-12 (direct SQL, single transaction; no migration file).** Created the kosher-supervised SS Classic #9 product family: batches `90025` "Batch SS Classic Granola #9 (Kosher Ignition)" (323 lb, product id 283) and `90026` "Batch SS Classic Chocolate Chip Granola #9 (Kosher Ignition)" (348 lb, id 284), with `batch_formulas` copied from `90002` (7 rows) and `90001` (8 rows) respectively — verified identical ingredient-for-ingredient. Created 6 finished goods parented to the new batches: `70013` SS Classic #9 Bulk per/lb, `70014` 25 LB, `70015` 10 LB, `70016` SS Classic Chocolate Chip #9 Bulk per/lb, `70017` 25 LB, `70018` 10 LB (`pack_format` NULL/`25lb`/`10lb` per migration-040 convention; brand Sunshine, label_type house). All 8 rows carry `verification_notes`: "Kosher Ignition: oven must be turned on by owner (Blubber) or his designated messenger. Do not start production without this." **Known gap:** `verification_notes` is NOT returned by any Floor GPT endpoint (`/products/search`, `/bom/products`, `/bom/batches/{id}/formula`, `/make`) nor shown on the dashboard, so floor operators will not see the Kosher Ignition requirement in-system — surfacing it is an open follow-up. | Kosher supervision requires distinct SKUs for SS runs of the Classic #9 recipes, with the owner/messenger oven-ignition requirement recorded on the products themselves. | Deleting or deactivating these products breaks any lots/transactions/sales-order lines that reference ids 283–290 and removes the only in-system record of the Kosher Ignition requirement. Reusing codes 90025/90026/70013–70018 for other products would corrupt Odoo mapping. | none (data-only; no schema or code change) |
| 55 | 2026-08-11 | Production Calendar category colors | **DEPLOYED 2026-08-11 15:44 ET** — feature commit `01f36a0`; merge commit `89a0219`; Railway deployment `c8163fd1-896d-45f6-b10c-b3414fb33437` succeeded; Netlify deployment `6a7b7b9dd6c8d60008a6930f` published successfully. Live dashboard verification confirmed `dashboard.css?v=16` and `dashboard.js?v=26`. Added theme variables for Coconut blue (`#60a5fa`), Granola amber (`#fbbf24`), and Graham green (`#4ade80`) in dark mode, with darker accessible light-mode companions. Layer 1 Made/Packed row labels use their product-family color while numeric values remain the existing near-white; Layer 2 family card headers use the same mapping. CSS cache advances v15→v16 and JS v25→v26 because the rendered labels now carry stable family classes. No API, schema, database, count, or layout behavior changed. JavaScript syntax and diff checks passed. Browser QA verified all eight visible row labels and three family headers, unchanged numeric colors, and dark-card contrast ratios of 5.75:1 (Coconut), 8.76:1 (Granola), and 8.40:1 (Graham). | The redesigned calendar removed the earlier blue/amber/green family cues, making mixed production days slower to scan even though their grouping and numbers were correct. | Reverting removes the family color cues from both calendar layers. Reverting only the cache bumps can leave returning browsers on stale CSS or JS, preventing the classes and styles from matching. | `dashboard/dashboard.css`, `dashboard/dashboard.js`, `dashboard/index.html` |
| 54 | 2026-08-11 | Pack-format classification and Production Calendar detail | **DEPLOYED 2026-08-11 15:29 ET** (feature `66a6c5e`; merge `02d8119`; Railway deployment `8a9b0fdf-d30d-482b-b6ed-203be1e1db6b`; Netlify deployment `6a7b778d5c4c1a0008476b40`). Migration 040 adds nullable `products.pack_format` constrained to `10lb`, `25lb`, `bagged`, or NULL. The guarded backfill applied the approved 58-SKU mapping: 2 `10lb`, 13 `25lb`, 9 `bagged`, and 34 NULL; bulk per-lb SKU 70004 intentionally remains NULL and is excluded from case counts. The existing `GET /dashboard/api/production` response now includes transaction type, SKU, and pack format. Made-unit counts use finished-output weight; coconut pans are `total_lbs / (default_batch_lb * yield_multiplier)`, so 4,795.2 lb / (360 lb × 1.11) = 12 pans. The dashboard calendar now has compact day tiles for nonzero coconut pans, granola/graham batches, and packed cases split into Granola 10 lb/25 lb/bagged, Coconut, and Graham. Clicking a tile opens one populated card per Coconut, Granola, or Graham family, with only populated Made/Packed subsections, full product names, SKU labels, and no placeholder columns. No other endpoint/report consumes the calendar coconut `batch_count`; unrelated inventory/lot counts retain their prior calculations. GPT schemas are unchanged (30 main / 22 Floor operations). Full DB-backed suite passes 70/70; static checks and browser QA passed. Live read-only Aug 10 verification passed: Batch Coconut Sweetened Flake = 12 pans, Batch Classic Granola #9 = 16 batches, CQ Coconut Sweetened Flake 10 LB = 440 packed cases, and Graham Cracker Crumbs – 10 LB = 420 packed cases. | Finished goods had no durable pack-format classification, preventing reliable granola case splits, and the calendar compressed production into abbreviated family columns without full per-product/SKU detail. Coconut’s hydration gain also required finished-output pan math to avoid overstating Aug 10 as 13 instead of 12 pans. | Reverting migration 040 removes the classification required for granola case splits; deploying the application without the migrated column breaks the calendar query. Reverting the API/UI restores unsplit packed granola, abbreviated details/placeholders, and the legacy coconut base-weight count. Reverting only the cache bumps can leave browsers on stale calendar assets. | `migrations/040_product_pack_format.sql`, `scripts/propose_pack_format_backfill.py`, `main.py`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `tests/schema/schema.sql`, `tests/test_dashboard_production_calendar.py` |
| 53 | 2026-08-11 | Graham dashboard visibility and correction-aware production reads | **DEPLOYED 2026-08-11 13:26 ET via PR #14 (feature `078575f`; merge `1359df5`; Railway deployment `cbfa943e-4d2e-4b7d-8ec2-995e7e79a1f7`; Netlify CSS v14 / JS v24 live).** Production calendar classification now recognizes `graham` case-insensitively and renders a dedicated green GRAHAM column; uncategorized products render in an optional OTHER section and log a console warning instead of being dropped. Rolling view keeps three category columns while seven-column month view stacks category sections for readability. Dashboard config adds exact DB products `Graham Cracker Crumbs – 10 LB` to 10 LB Cases and `Graham Cracker Crumbs – 50 LB` to a new Bulk Crumbs ingredient panel; cache versions advance to CSS v14 / JS v24. `get_daily_production_summary`, `GET /dashboard/api/production`, and all make/pack/adjust queries in `GET /production/day-summary` now read `ledger_current_transactions` / `ledger_current_transaction_lines` and filter `effective_status='posted'`. Regression coverage amends a production line 10→25 lb, verifies all three reads show 25 lb, then append-only voids it and verifies all three omit it. Full suite 69/69. Post-deploy verification passed: the 2026-08-10 API and visible Monday calendar show Graham 10 LB at 4,200 lb / 420 units; 10 LB Cases shows 4,200 lb / 420 cases with no missing SKU; Bulk Crumbs shows Graham 50 LB at 29,990 lb with no missing SKU. | Graham crumb makes/packs were classified as `other`, then silently discarded by a two-category summary/renderer; its 10 lb and 50 lb products were absent from configured inventory panels. The three production summary paths also read raw transaction state, so Phase 1 line corrections and append-only voids could be misreported. | Reverting hides Graham production and its configured on-hand inventory again, resumes silent loss of unknown production categories, can restore unreadable three-way month cells if only the responsive rules are removed, and makes the three production summaries ignore corrected line values/effective void status. Reverting only the cache bumps can leave browsers on stale two-category assets. | `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/dashboard_config.json`, `dashboard/index.html`, `main.py`, `tests/test_phase1_ledger_integrity.py` |
| 52 | 2026-08-11 | Dashboard entry timestamps | Shipping, Receiving, and lot transaction timelines now return and visibly render database `created_at` beside operational time, with `migration_backfill_039` shown as `backfilled` and `legacy_unverified` shown as `legacy`. The dashboard APIs now read Phase 1 effective transaction/line views so append-only voids are excluded. No database or Floor OpenAPI schema changed; full suite 68/68. | Operators previously had to query the evidence endpoints to see entry time, and these dashboard reads still used raw pre-Phase-1 status semantics. | Reverting hides entry time in the daily dashboard and can make append-only-voided shipments/receipts appear active there. | `main.py`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `tests/test_phase1_ledger_integrity.py` |
| 51 | 2026-08-11 | Trial v8.1 Phase 1 timestamp/cutoff integrity | **DEPLOYED 2026-08-11 10:34 ET** (migration 039; application commit `d17836f`; Railway deployment `8a005c7b-6205-4046-a5de-8b04d89d3f47`). A restore-verified backup and all preflights passed; the guarded migration asserted the approved 12,526-row scope. Database triggers force and protect `created_at` plus provenance on core transactional tables; transaction headers/lines are append-only and separate operational/business time from creation time. Voids, amendments, and effective line moves append `ledger_corrections`; originals remain unchanged while current balances/history resolve corrections. Daily certifications have an append-only correction path. JSON/CSV cutoff endpoints apply `created_at > certified_at`. Production smoke passed with Floor adjustment transaction `1891` preserved as effective-voided by correction `549ba2f1-d50b-43f6-9f6f-aa5f6ae44ff0`; past dummy certification `0d9af99b-975f-4962-b8f2-311b4f1fd558` was corrected by `ce5d1b11-e5c1-4baf-b4d9-4a0d815db853`. All later-phase sentinel tables remain absent and the Floor OpenAPI is byte-unchanged. | The ledger could not prove original creation time, distinguish corrections from originals, preserve voided originals, or classify late entries against the evening certification. | Reverting the application alone breaks effective-state queries; reversing the migration requires an explicitly approved data-restoration plan. Removing the triggers or correction views restores mutable/hide-on-void evidence and removes certification/cutoff proof. | `migrations/039_trial_timestamp_integrity.sql`, `main.py`, `tests/test_phase1_ledger_integrity.py`, `tests/test_void_semantics.py`, `docs/deployments/phase-1-ledger-trial.md` |
| 50 | 2026-08-05 | Floor GPT dispatch configuration sync | **LIVE IN GPT EDITOR 2026-08-05.** Back-ported the final 7,605-character Floor instructions into canonical `shared-rules.md` + `floor-specific.md` and regenerated the dist artifact (7,735 chars including generated header; body is an exact match). Added `ALL 22 ACTIONS ARE LIVE`, separated single-endpoint mode workflows from two-action sales-order dispatch, routed exact SO dispatch intent to preview-only `shipOrder`, and required explicit approval before mode-less `commitShipOrder`. Canonical schema is v4.1.0 / 22 unique operations and byte-matches the editor artifact. Full live chain subsequently shipped `SO-260723-001`: shipment header 319, transactions 1847/1848; order is now shipped. Incident record documents the three-layer root cause, consequential-flag trade-off, client quirks, and follow-ups. This row supersedes row 49's pre-operator-test statement that SO-260723-001 was unchanged. | The server endpoint alone did not fix model behavior: stale instructions could still claim no access, search an SO as a product, or send `mode` to the dedicated commit action. Repository status also lagged the editor configuration. | Reverting the sources/dist restores the old one-action mode protocol and false no-access behavior even though the endpoint remains live. Reverting the schema removes the dedicated GPT action. Weakening the preview→explicit approval→commit rule removes the principal safety barrier because both GPT operations intentionally use `x-openai-isConsequential: false`. | `gpt-configs/sources/shared-rules.md`, `gpt-configs/sources/floor-specific.md`, `gpt-configs/dist/GPT_FLOOR_INSTRUCTIONS.md`, `gpt-configs/schemas/openapi-floor.yaml`, `docs/incidents/2026-08-05-gpt-actions-dispatch.md`, `STATUS.md`, `gpt-configs/README.md` |
| 49 | 2026-08-05 | Floor GPT sales-order dispatch | **DEPLOYED 2026-08-05 11:23 ET (commit `5b1ae75`; Railway deployment `cbfb0169-157c-460a-b69b-773eb5e8b907`).** Added first-class `POST /sales/orders/{order_id}/ship/commit` (`commitShipOrder`) with a required mode-less `CommitShipOrderRequest`; it constructs a legacy `ShipOrderRequest(mode='commit')` and delegates to the existing `ship_order` implementation, so FIFO, locking, shipment records, status transitions, readonly handling, and error receipts remain single-sourced. The combined `/sales/orders/{order_id}/ship` remains backward compatible. Floor OpenAPI advanced to v4.1.0 with 22 unique operations, preview-only `shipOrder`, dedicated `commitShipOrder`, and `x-openai-isConsequential: false` on both. Redocly valid (pre-existing warnings only); full suite 62/62 and focused shipment suite 6/6 passed. Production disposable order `SO-260805-001`: preview 1 lb, new commit route returned `partial_ship`, shipment 318, transaction 1846 / `TXN-3811F7`; transaction 1846 then voided and order cancelled, inventory returned to 2,300 lb, voided transaction absent from history/dashboard. Protected `SO-260723-001` verified unchanged (`confirmed`, 0 lb shipped, no shipments). | ChatGPT's consequential-action dialog failed to render for combined `shipOrder` commits, so approved dispatch calls died client-side even though the API commit logic was healthy. A separate non-consequential action gives the GPT a reliable second-stage call after explicit operator approval. | Reverting removes the callable `commitShipOrder` action and restores the broken Custom GPT path where preview succeeds but approved commits are never sent. The legacy combined endpoint/dashboard remain operational, but the Floor GPT cannot reliably dispatch until its schema is also rolled back or the endpoint is restored. | `main.py`, `tests/test_ship_order_service_line.py`, `gpt-configs/schemas/openapi-floor.yaml` |
| 48 | 2026-07-22 | PACKED production-calendar timezone bucketing | **DEPLOYED 2026-07-22 11:22 ET (commit `25e0ad23`; Railway deployment `a654797b-60b1-40a0-a267-6c3b2fe2ce2a`).** The `GET /dashboard/api/production` query now converts the UTC wall value in `transactions.timestamp` with `(t.timestamp AT TIME ZONE 'UTC') AT TIME ZONE 'America/New_York'` before applying `DATE(...)` in the selected/grouped production date and both month/rolling date filters. Scope is this endpoint only; no timestamp writes, schema, or other dashboard displays changed. Live production verification with `month=2026-07` returns product 138 (`Granola Wheat Free 25 LB`), 2,000 lb / 80 units, under 2026-07-20 and absent from 2026-07-21. | Late-evening ET make/pack transactions were stored after midnight as UTC wall values and the raw `DATE(t.timestamp)` query displayed them on the following production day; transaction 1681 was entered 2026-07-20 20:01 ET but appeared on 2026-07-21. | Reverting restores raw UTC date bucketing, so production entered after 20:00 EDT (or 19:00 EST) appears on the next calendar day and month-boundary activity can land in the wrong dashboard period. | `main.py` |
| 47 | 2026-07-20 | production board Phase 4+5 visual hierarchy and Order Book | **DEPLOYED 2026-07-20 via Netlify (commit `edcbf84`, merged to `main`).** Reallocated the three-column layout from 305/center/340px to 240/center/340px, enlarged board totals/details/day/station typography with taller cells and protected worker-badge space, compacted and muted rail typography/spacing while retaining readable controls, and rebuilt active Order Book lines as no-wrap tabular rows using the shared `shortName()` with full-name tooltips and fixed-width exclude/delete icon slots. Customer/SO and Ship/Ready/status headers are more compact. Active cards now sort Late/Won't finish, then Tight, then On time/Early/From stock while retaining due-date order within each group; excluded placement and include behavior are unchanged. Board version is `v2.1`. No scheduler engine, cell construction, metrics, totals, text builders, recalculation triggers, or print-mode logic changed. | The small grid competed visually with oversized settings controls, and long Order Book product names wrapped action links onto separate lines while urgent late orders could appear below lower-priority cards. | Reverting restores the narrow/smaller center board, larger/higher-contrast rails, wrapping Order Book actions and full product wording, due-date-only card order, and `v2.0-dev`; scheduling output and stored data remain unchanged. | `dashboard/scheduler/seven-wells-production-board.html` |
| 46 | 2026-07-20 | production board Phase 3 day schedule panel | **NOT YET DEPLOYED — branch `feat/day-schedule-panel`.** Day-header copy now computes `dailyScheduleText(day)` once, uses that exact string for the clipboard and a new center-column panel, and keeps the existing day-button Copied confirmation. The panel replaces its title/body when another day is selected and provides Copy, isolated schedule-only Print, and Close controls. The top-bar Print explicitly remains whole-board mode and excludes the schedule panel. No storage, scheduler engine, order book, settings rail, metrics, or totals changed. Board version is `v2.0-dev`. | Operators can now read and print the daily floor schedule without pasting clipboard text into another application, while displayed and copied schedules cannot drift. | Reverting returns day schedules to clipboard-only access and removes schedule-only printing; scheduling output and whole-board printing remain otherwise unchanged. | `dashboard/scheduler/seven-wells-production-board.html` |
| 45 | 2026-07-20 | production board Phase 2 station scope | **NOT YET DEPLOYED — branch `feat/schedule-text-v2`.** Limited board-cell pan aggregation from row 43 to Granola Bake and Coconut (sweet), leaving Coconut (toasted) cells on their existing SKU renderer. The daily Coconut clipboard section still applies the requested pan-product/full-name format to sweet and toasted runs. | Keeps the visual board change within the two explicitly requested station-cell scopes without losing the required toasted clipboard naming. | Reverting this refinement also changes Coconut (toasted) board cells to pan-group presentation, expanding the visual scope beyond the request. | `dashboard/scheduler/seven-wells-production-board.html` |
| 44 | 2026-07-20 | production board Phase 2 clipboard precision | **NOT YET DEPLOYED — branch `feat/schedule-text-v2`.** Refined row 43 so single-SKU bake pan groups retain their existing clipboard text and coconut destinations use the `cases` values already stored in station details rather than deriving them again during rendering. | Preserves Vanilla Crisp and other single-SKU floor wording while guaranteeing the displayed coconut case counts match the scheduling output exactly. | Reverting this refinement can rename a single-SKU bake run to its pan label and can make presentation independently recalculate coconut cases instead of displaying the scheduler's canonical value. | `dashboard/scheduler/seven-wells-production-board.html` |
| 43 | 2026-07-20 | production board Phase 2 pan-product presentation | **NOT YET DEPLOYED — branch `feat/schedule-text-v2`.** Bake and sweet/toasted coconut station details now group existing cell details by `panOf(p)`, with SKU or pack-destination sub-lines and existing customer allocations retained at member level. Copied daily schedules group Baking by pan product, retain the legacy single-SKU/unshared text (including `floorName` overrides), and describe coconut pan products with full mix names plus one or more brand/case-size destinations and unchanged case counts. Packing and Repack clipboard sections remain SKU-level. Board version is `v1.9-dev`. Scheduling engine, `out.cells.*` construction, changeovers, order rendering, layout/CSS, and order book are unchanged. | Operators previously saw separate bake/mix lines for SKUs produced from one shared pan product and coconut clipboard lines named for a destination SKU rather than the mix and pack routing. | Reverting restores SKU-level Bake/Coconut presentation and the old coconut clipboard wording; schedule dates, capacity, pan/case totals, changeovers, and order readiness remain numerically unchanged. | `dashboard/scheduler/seven-wells-production-board.html` |
| 42 | 2026-07-20 | production board Phase 1 pan-product mapping | **NOT YET DEPLOYED — branch `feat/pan-product-mapping`.** Added only the user-specified pan-product names to catalog entries, with unmapped SKUs falling back through `panOf(p)` to their existing short names and `prodOv` able to override `pan`. Granola Bake changeover capacity and plan counts now use distinct pan products; pouch, 10-lb, 25-lb, coconut, and repack SKU changeovers remain unchanged. Board version is `v1.8-dev`; schedule/detail/order rendering functions were not changed. | Multiple SKUs produced from the same baked/mixed batch were falsely counted as separate Granola Bake changeovers. | Reverting restores per-SKU bake changeovers, inflating the plan changeover count and, when changeover minutes are nonzero, unnecessarily reducing bake capacity and potentially delaying completion. | `dashboard/scheduler/seven-wells-production-board.html` |
| 42 | 2026-07-20 | dashboard sales order list fetching | **NOT YET DEPLOYED — branch `fix/dashboard-order-fetch-limit`.** Sales Orders now sends the selected dropdown status to the existing `GET /sales/orders` status filter while retaining `limit=200`: default load uses `status=open`, individual lifecycle selections use their matching status, and All Orders omits the status parameter. Status changes refetch instead of filtering the previously loaded response. Dashboard JS cache-buster bumped to v23. No API or schema change; the endpoint already supported `status`, including the special `open` value. | The unfiltered endpoint sorted all 210 orders by ship date and capped at 200 before the browser applied its open filter, so two open 08/31 orders fell beyond the cutoff after 200 earlier rows. | Reverting restores client-only status filtering over a ship-date-truncated 200-row response; later-dated open orders can disappear whenever total history exceeds the cap, and changing status can show only the subset present in that truncated response. | `dashboard/dashboard.js`, `dashboard/index.html` |
| 41 | 2026-07-14 | dashboard sales order detail layout | **NOT YET DEPLOYED — branch `feat/so-detail-section-order`.** Reordered existing sales order detail markup only: header and Edit Order remain first, product line items now precede the unchanged TOTAL ORDERED / SHIPPED / REMAINING and PALLETS summary, and NOTES remains last. No data binding, calculation, API, or styling changes. | Operators see the product lines immediately beneath the order controls, with totals following the products and notes retained at the bottom. | Reverting restores the prior header → summary → product table → notes order; order data and calculations remain unchanged. | `dashboard/dashboard.js` |
| 40 | 2026-07-09 | dashboard sales order editing | **READY TO DEPLOY — branch `feat/dashboard-order-editing`.** Added dashboard-only order detail editing for eligible sales orders. Detail view now shows `Edit Order` only for `new`/`confirmed` orders, mirrors the server header-edit gate, sends writes through the existing authenticated Railway helper, waits for 2xx before showing success, displays API rejection text, and re-fetches detail after successful saves. Header save wires `requested_ship_date`/`notes` to `PATCH /sales/orders/{order_id}`; line save wires quantity/price to `PATCH /sales/orders/{order_id}/lines/{line_id}/update` query params; status dropdown wires to `PATCH /sales/orders/{order_id}/status` with confirmation. No add/remove/cancel-line UI, no API changes, no OpenAPI changes. Manual prod tests passed: cancelled `SO-260709-003` rejected header PATCH with `ORDER_HEADER_LOCKED`; local dashboard edited confirmed `SO-260710-001` to ship-by `2026-08-05`, notes `TEST ORDER 2 — edited via dashboard`, line 584 `quantity_lb=30`, `case_price=2.50`, with derived `cases=3` and `line_value=7.5`. Backlog: `GET /sales/orders` ignores `?search=`; document full status transition graph, including whether `shipped` → `cancelled` is blocked. | Dashboard users could view order details but had no safe inline way to edit ship-by date, notes, or line quantity/price; manual tests also needed proof that rejected writes surface server errors and successful writes re-fetch canonical data. | Reverting removes dashboard order-detail editing and returns users to API/manual edits for ship date, notes, quantity, and price. Reverting only cache-busters can leave browsers on stale JS/CSS without the tested controls. Server-side gates remain API-owned; reverting dashboard code does not change API status enforcement. | `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html` |
| 39 | 2026-07-08 | orders matrix Excel pan notes, deployment | **DEPLOYED 2026-07-08 16:18 ET (feature commit `7127468`; Railway deployment `cc49789b-3e4b-4607-a7e3-21e4c2e5d328`).** Railway GitHub status reported success for `fastapi-production-b73a.up.railway.app`; `/health` returned HTTP 200 with database connected and pool active. The deployed export adds 260×80 Factory Ledger comments to all nonzero product cells on Cases and Pounds, calculation comments on BATCHES / PANS cells, documented batch identifiers, special 70073 and 31012 routing text, and `<0.1 pans` for positive values that would round to 0.0. Existing input-row source comments remain intact. Focused matrix tests: 2/2 passed. | Production planners now get cell-level case/pound/pan math without misleading tiny positive requirements as zero. | Reverting removes production notes and summary calculations, restores manual conversion work, and makes sub-0.05 pan requirements display as `0.0 pans`; source input comments and the base matrix export otherwise remain. | `main.py`, `tests/test_orders_matrix_export.py` |
| 38 | 2026-07-08 | orders matrix pan-note formatting, tests | **NOT YET DEPLOYED — branch `feat/matrix-cell-pan-notes`.** Positive pan calculations that format to zero at one decimal now display `<0.1 pans` instead of `0.0 pans` on both Cases and Pounds comments. Regression assertions cover the fractional 70073 test line on both sheets. | Small but real production requirements appeared as zero pans, which was mathematically misleading. | Reverting makes positive pan requirements below 0.05 display as `0.0 pans` again. | `main.py`, `tests/test_orders_matrix_export.py` |
| 37 | 2026-07-08 | orders matrix Excel comments, tests | **NOT YET DEPLOYED — branch `feat/matrix-cell-pan-notes`.** Added Factory Ledger comments to every nonzero product quantity cell on both Cases and Pounds sheets, showing cases, pounds, calculated pans, documented batch identifiers where known, the special 70073 finished-weight/oven-load detail, and the 31012 repack exception. Added calculation comments to product cells in the BATCHES / PANS row while preserving source-citation comments on the input row. Comment boxes are 260×80. Extended export tests cover both sheets, computed pans, repack wording, comment metadata/size, preserved citations, and summary calculation text. | Planners had to manually convert each order cell from cases/pounds into pans and could not see routing exceptions at the point of use. | Reverting removes cell-level pan math and routing context from the workbook; the matrix still exports, but planners must calculate pans manually and may miss the 70073 finished-weight distinction or 31012 repack routing. | `main.py`, `tests/test_orders_matrix_export.py` |
| 1 | 2026-03-19 | main.py | Exclude service/charge lines from SO weight totals | Service lines (pallets, freight) inflated weight totals on sales orders | SO weight summaries become inaccurate | `main.py` |
| 2 | 2026-03-19 | main.py | Auto-deduct add-in ingredients during pack | PB Chips, Banana Bites weren't consumed from inventory at hopper | Inventory ghost stock for add-in ingredients | `main.py` |
| 3 | 2026-03-19 | dashboard | Add 8oz BS panel and fix case weight rounding | 8oz products missing from dashboard; case weights showed too many decimals | 8oz products disappear from dashboard | `main.py`, `dashboard/` |
| 4 | 2026-03-19 | main.py | Fix day summary for pack consumption from prior-day batch lots | Pack runs using batches made on previous days didn't show in day summary | Day summary under-reports consumption | `main.py` |
| 5 | 2026-03-19 | dashboard | Fix dashboard API calls to use absolute Railway URL | Relative paths failed when dashboard hosted on Netlify | Dashboard API calls return 404 | `dashboard/` |
| 6 | 2026-03-19 | main.py | Replace `is_ingredient` with `type != 'ingredient'` in /make commit pack-prompt query | /make commit crashed with "column is_ingredient does not exist" | /make commit will crash again on auto-prompt /pack query | `main.py` |
| 7 | 2026-03-23 | main.py, dashboard, docs | "Ready to Ship" display label + ready→in_production reverse transition | "Ready" label was unclear; no way to move order back if production falls short | Dashboard shows "Ready" instead of "Ready to Ship"; can't reverse from ready status | `main.py`, `dashboard.js`, `index.html`, `gpt-instructions-v3.md`, `GUIDE.md`, `CONTEXT.md` |
| 8 | 2026-03-23 | main.py, dashboard | Backward trace handles ingredient lots (supplier trace + downstream batches) | Backward trace returned "Batch not found" for ingredient lots | Ingredient lots will 404 on backward trace again | `main.py`, `dashboard/traceability.html` |
| 9 | 2026-03-23 | main.py, dashboard | Lot code collision disambiguation (product_id param on 5 endpoints) + trace type validation on /trace/ingredient | Wrong lot returned when lot codes collide across products; /trace/ingredient returned false traces for finished goods lots | Lot collisions return wrong data; /trace/ingredient silently returns empty traces for batch lots; PATCH supplier-lot corrupts wrong lot | `main.py`, `dashboard/traceability.html` |
| 10 | 2026-03-23 | main.py, dashboard | Extend collision disambiguation to main dashboard — add product_id to shipment/receipt/search API responses, pass it through all lot links, handle 409 with disambiguation picker | Main dashboard showed raw 409 error when clicking colliding lot codes in shipping, receiving, search, or product panel | Dashboard lot links break for any colliding lot code; users see raw JSON error instead of picker | `main.py`, `dashboard/dashboard.js` |
| 11 | 2026-03-24 | GPT instructions | Order entry behavior fix — add ORDER ENTRY FROM CONFIRMATIONS section, strengthen BE CONCISE and ACT DON'T LOOP, tighten disambiguation for order entry, compress verbose sections | GPT acted as consultant during order entry: dumped SOPs, over-confirmed, took 6-7 exchanges instead of 1-2 | GPT reverts to verbose consultant mode during order entry; offers unprompted next steps; shows API payloads | `gpt-instructions-v3.md` |
| 12 | 2026-03-24 | main.py | Direct-ship trace + supplier-lot exposure: added direct_shipments to /trace/ingredient and _trace_ingredient_backward, new /trace/supplier-lot endpoint, normalized entry_source enums | Ingredient/resale lots shipped directly to customers invisible to trace (FDA recall gap) | Trace endpoints lose direct-ship visibility; /trace/supplier-lot endpoint disappears; entry_source checks may miss lots with new enum values | `main.py` |
| 13 | 2026-03-25 | main.py, migration | Service item handling: is_service detection in create_sales_order and add_order_lines; quantity_lb constraint relaxed from > 0 to >= 0 | Service items (Pallets, freight) rejected by CHECK constraint requiring quantity_lb > 0 | Service items cannot be added to sales orders; quantity_lb > 0 constraint rejects them | `main.py`, `migrations/031_relax_quantity_lb_check_for_service_items.sql` |
| 13 | 2026-03-24 | OpenAPI + GPT instructions | Added format:date + description to requested_ship_date in both schemas; strengthened ORDER EDITING section with "CALL API IMMEDIATELY", date conversion rules, "NEVER show curl/docs" | GPT showed curl commands instead of calling updateOrderHeader for ship date changes | GPT reverts to showing curl commands for ship date edits; date format guidance lost | `openapi-v3.yaml`, `openapi-schema-gpt.yaml`, `gpt-instructions-v3.md` |
| 13 | 2026-03-24 | main.py | Option C trace: /trace/ingredient soft-handles output lots (returns upstream_ingredients, lot_origin, origin_note instead of 400 error); /trace/batch adds customer_shipments, total_shipped_lb, on_hand_lb | Output lots (e.g. 601141 Graham Cracker Crumbs NTF) blocked from /trace/ingredient despite shipping directly to customers | /trace/ingredient reverts to hard-rejecting output lots (shipment data invisible); /trace/batch loses customer shipment visibility | `main.py` |
| 14 | 2026-03-24 | main.py, migrations | Rename lot 324 from UNKNOWN to 25216 + new PATCH /lots/{lot_id}/rename endpoint | Chocolate Sprinkles lot stuck with lot_code='UNKNOWN'; no API to rename lots | Lot 324 reverts to UNKNOWN (trace by code fails); rename endpoint disappears, future UNKNOWN lots require raw SQL | `main.py`, `migrations/029_rename_unknown_lot_to_25216.sql` |
| 15 | 2026-03-24 | main.py, migrations | GAP-3: Standalone /ship writes shipments + shipment_lines; backfill migration 030 | Standalone shipments invisible to shipment tables (packing slips, integrity checker, reports) | Standalone ships stop creating shipment records; existing backfilled records remain but new ones won't be created; integrity checker flags all standalone ships | `main.py`, `migrations/030_backfill_standalone_shipment_records.sql` |
| 16 | 2026-03-25 | main.py, OpenAPI, GPT instructions | resolve_order_id dependency: all /sales/orders/{order_id} endpoints accept order_number strings (e.g. SO-260323-001) in addition to integer DB ids | GPT only knows order_number, not DB id — FastAPI rejected strings with validation error, GPT fell back to curl commands | All order endpoints revert to integer-only; GPT can't call any order update/ship/status endpoints by order_number | `main.py`, `openapi-v3.yaml`, `openapi-schema-gpt.yaml`, `gpt-instructions-v3.md` |
| 17 | 2026-03-25 | main.py, dashboard, OpenAPI, GPT | Unit Display: all qty endpoints return unit_count/batch_count derived from case_size_lb/default_batch_lb; dashboard shows dual "X lb · Y units" format; packing slip uses dual format; new /products/missing-case-size endpoint | Operators saw lb-only everywhere, had to mentally convert to units/cases | All unit counts disappear from API responses and dashboard; packing slip reverts to "N cs" format; /products/missing-case-size endpoint removed | `main.py`, `dashboard/dashboard.js`, `dashboard/traceability.html`, `openapi-v3.yaml`, `openapi-schema-gpt.yaml`, `GPT_INSTRUCTIONS.md`, `gpt-instructions-v3.md` |
| 19 | 2026-03-26 | main.py, OpenAPI | /inventory/lookup unified endpoint: fuzzy search + lot-level inventory in single call; /inventory/{item_name} fuzzy fallback (tiered search if no LIKE match, 404/300 for zero/multiple) | GPT needed two API calls (searchProducts → getInventoryItem) for every inventory question | /inventory/lookup endpoint disappears; getInventoryItem loses fuzzy fallback (returns empty instead of searching) | `main.py`, `openapi-gpt-v3.yaml`, `openapi-v3.yaml`, `openapi-schema-gpt.yaml` |
| 18 | 2026-03-25 | main.py, OpenAPI, GPT | Add product_id disambiguation to all three trace endpoints + add /trace/supplier-lot to OpenAPI schemas | GPT couldn't pass product_id to disambiguate any trace endpoint; /trace/supplier-lot was missing from OpenAPI entirely | Trace endpoints lose product_id in OpenAPI (GPT can't disambiguate); /trace/supplier-lot disappears from GPT actions | `main.py`, `openapi-schema-gpt.yaml`, `openapi-v3.yaml`, `gpt-instructions-v3.md` |
| 20 | 2026-04-14 | main.py, OpenAPI | /receive supplier_lot_code fallback: replace hard 400 with default to lot_code or 'N/A'; add description to OpenAPI schemas | GPT couldn't complete receives — schema said field was optional but API hard-rejected when omitted | Receives will hard-reject again when supplier_lot_code is omitted; GPT loses fallback description guidance | `main.py`, `openapi-v3.yaml`, `openapi-gpt-v3.yaml` |
| 21 | 2026-04-14 | GPT instructions, OpenAPI | Restore SEARCH FIRST + NEVER INSTRUCT rules; add lot-code/supplier-lot/customer/day-summary routing; fix 4 phantom endpoints; swap getCurrentInventory+checkFulfillment for searchCustomers+getDaySummary in schema | GPT refused to call API — told users to manually run GET requests and paste results; lot lookups returned "no results" without calling API | GPT reverts to manual-instruction mode; lot-code routing lost; searchCustomers and getDaySummary disappear from Actions; phantom endpoints re-confuse GPT | `GPT_INSTRUCTIONS.md`, `gpt-instructions-v3.md`, `openapi-gpt-v3.yaml` |
| 22 | 2026-04-16 | products table, migration | Product catalog cleanup: backfill odoo_code 70089 on 'Classic Granola 25 LB' (id=171); merge SKU-less duplicates 'BS Cocoa Chips' (id=167) and 'BS Cocoa Liquor' (id=168) into canonical 15008 'BS Cocoa Liquor – Chips' (id=61), repointing 9 FK refs across lots / transaction_lines / inventory_adjustments / product_verification_history; assert label_type='private_label' on 70082 Setton FV. Product count 203 → 201. | Two SKU-less duplicate ingredient rows bloated the catalog and split inventory/transaction history across three products when they were the same item; Classic Granola 25 LB had no Odoo code; 70082 needed an explicit private-label guard so restores can't drift. | Duplicate BS Cocoa rows reappear (167, 168 with NULL odoo_code); split lot/transaction history across three product ids; Classic Granola 25 LB reverts to NULL odoo_code (breaks SKU-based integrations); 70082 may silently flip to 'house' label_type if a pre-migration backup is restored. | `migrations/032_backfill_skus_and_merge_bs_cocoa.sql` |
| 23 | 2026-04-16 | sales_orders, migration | Force-close SO-260326-002 (Ace Endico) to status='shipped' by bypassing the manual-'shipped' guard (main.py:5375); append paper-trail note (Ace Endico ship-to, 04/13/2026 customer pickup, invoice 28159-I, PO 624249, lot breakdown for 140 Fancy + 20 Medium coconut cases). **No transaction_lines, shipment_lines, or sales_order_shipments rows written** — this is a paper-only close. | Order was physically shipped and invoiced but never entered as shipment/transactions in the ledger; business decision to close the order without backfilling real records. | Reverting would flip SO-260326-002 back to 'confirmed' and strip the paper-trail note from `sales_orders.notes`. NOTE: dashboard `Shipped` column already shows 0 lb for this order because no transaction rows exist — reverting the status flip would not restore any underlying shipment data (there never was any). | `migrations/033_force_close_so260326002_ace_endico.sql` |
| 24 | 2026-04-16 | sales_orders, migration | Force-close SO-260414-003 (Hannas Gourmet) to status='shipped' using the same paper-only pattern as #23; append paper-trail note (Hannas Gourmet ship-to 1330-14 Lincoln Ave Holbrook NY, 03/11/2026 customer pickup, invoice 28123-I dated 03/02/2026, PO 2026-0099-SW, lot JAN 20 2026 on 10 units Granola Vanilla Almond 25 LB + 1 pallet charge). **No ledger transactions written.** | Order was physically shipped/invoiced in March but entered retroactively on 04/14/2026 without ledger transactions; same business decision as #23 to close on paper rather than backfill real records. | Reverting flips SO-260414-003 back to 'confirmed' and strips the paper-trail note; same caveat as #23 — dashboard `Shipped` already reads 0 for this order so there's nothing underlying to restore. | `migrations/034_force_close_so260414003_hannas.sql` |
| 25 | 2026-04-20 | main.py, OpenAPI, GPT instructions, tests | Pass 1: customer address tiebreaker + 4xx error shape normalization. (a) `resolve_customer_id` accepts optional `address`; trigram tiebreaker on `customers.address` (≥0.6 abs, ≥0.2 gap) collapses ambiguous name matches to one row. New helper `_pick_by_address`. Additive — no-address calls unchanged. (b) `customer_address` field added to OrderCreate and ShipRequest and threaded through both resolve_customer_id call sites. (c) `resolve_product_id` / `resolve_product_full` / `resolve_order_id` and all 4xx raise sites in createOrder, getOrder, updateOrderHeader, addOrderLines, updateOrderLine normalized to `{error_code, message, input, suggestions}` dict shape. Product-ambiguous/uncertain status **400 → 409**. New codes: ORDER_NOT_FOUND, CUSTOMER_NOT_FOUND, ORDER_HEADER_LOCKED, NO_FIELDS_TO_UPDATE, CASE_WEIGHT_REQUIRED, ORDER_LINES_LOCKED, LINE_NOT_FOUND, PRODUCT_AMBIGUOUS, PRODUCT_UNCERTAIN, PRODUCT_NOT_FOUND. (d) GPT instructions: new PRE-FLIGHT — CUSTOMER section, removed duplicate DAY SUMMARY routing, ERRORS section now tells GPT to surface `detail.suggestions` on 4xx. 7,987 chars. (e) OpenAPI: `components.schemas.ErrorResponse` added; 400/404/409 response blocks referencing it added to the 5 sales-order ops only for status codes they actually raise. 30/30 ops held. (f) `tests/` scaffolding (pytest.ini, conftest.py with db_cursor SAVEPOINT+ROLLBACK fixture, test_resolve_customer.py with 4 tests). | GPT was asking operators to disambiguate customers even when a PO address uniquely pointed to the right row; plain-string 4xx detail forced GPT into a generic "something went wrong" fallback instead of showing the structured suggestions list. | Reverting: (a) address tiebreaker disappears — every name-ambiguous POST /sales/orders goes back to asking for operator disambiguation; (b) product/order/customer resolver errors revert to plain-string `detail`, GPT loses error_code parsing and surfaces generic failure messages; (c) PRODUCT_AMBIGUOUS/UNCERTAIN flips back to 400 (caller spot-check: no in-repo code catches 400 from these resolvers, but any external consumer of the old 400 contract would break); (d) OpenAPI `ErrorResponse` schema and the 4xx response blocks disappear, so GPT falls back to unknown-response handling on the 5 sales-order ops. | `main.py`, `gpt-instructions-v3.md`, `openapi-gpt-v3.yaml`, `tests/`, `pytest.ini`, `FOLLOWUPS.md` |
| 26 | 2026-04-20 | main.py, tests | F05-04: ship_order auto-fulfills is_service lines (detect at select, skip FIFO, set line_status='fulfilled' + bump quantity_shipped_lb); preview path also skips stock lookup for service lines (returns `is_service: true, on_hand_lb: null, short: 0`); zero-shipment guard now only counts physical shipped_lb so a pallet-only auto-fulfill can't bypass it; response `results[]` carries `is_service: true` on service rows. Matches existing is_service handling in create_sales_order and add_order_lines (rows 1 and 13). | Orders with a Pallet Charge stuck at partial_ship forever because FIFO found no lots for service products, tripping `actual_ship <= BALANCE_EPSILON` and leaving `all_fully_shipped=False`; workarounds were migrations 022 and 024. Per audit 17-migration-integrity.md F17-06, this was one of the last remaining root causes of the recurring close-order migration pattern. | ship_order reverts to leaving pallet-charge orders at partial_ship; operators return to writing one-off close-order SQL per stuck order; preview resumes misleading "no stock / short: N" warnings for service lines; ZERO_SHIPMENT guard could be bypassed by a service-only "shipment". | `main.py`, `tests/test_ship_order_service_line.py` |
| 27 | 2026-05-18 | main.py | Read-only transaction tripwire on `update_order_header`: module-level `READONLY_PROBE_SQL` constant + `_is_readonly_error(exc)` + `_capture_readonly_diagnostics()` helpers near `get_transaction()`. The `except Exception` branch of `PATCH /sales/orders/{order_id}` detects `"read-only transaction"` in `str(e)` (case-insensitive), grabs a fresh pooled connection (not the failing request's poisoned one) to run the probe (`default_transaction_read_only`, `transaction_read_only`, `inet_server_addr()`, `current_database()`, `current_user`, `pg_is_in_recovery()`, `version()`), attaches the result under a `diagnostics` key in the 500 response, and emits a single-line ERROR log prefixed `READONLY_TRIPWIRE:` followed by JSON (grep-friendly in Railway). Probe wrapped in its own try/except so probe failure can't mask the original error (falls back to `{"probe_error": "..."}`). Non-read-only errors keep the original `{"error": str(e)}` shape unchanged; status stays 500. No retry, no new deps. Healthy-state baseline saved at `audits/2026-05/readonly-baseline-20260518T210745Z.json` (default_ro=off, txn_ro=off, is_replica=false, PG 17.6). Smoke-tested post-deploy: PATCH SO-260415-004 with its existing `requested_ship_date` → HTTP 200, happy path intact. | Prod intermittently failed UPDATEs with `cannot execute UPDATE in a read-only transaction` (Supabase failover/promotion window) and the window passed before session state could be captured — root cause unnameable. The tripwire guarantees the next occurrence leaves a receipt in both the response body AND Railway logs so the failing replica/user/setting can be named. | Reverting strips `_is_readonly_error`, `_capture_readonly_diagnostics`, `READONLY_PROBE_SQL`, and the diagnostics branch from `update_order_header` — the next read-only window goes unobserved again and the in-repo healthy-state baseline is lost. **Scope:** only `PATCH /sales/orders/{order_id}` is instrumented; other UPDATE endpoints (`/ship/commit`, `/make/commit`, `/adjust`) use the same `except Exception` pattern but are not yet wired — a real `@app.exception_handler(Exception)` would cover them and is the follow-up. | `main.py`, `audits/2026-05/readonly-baseline-20260518T210745Z.json`, PR #6 (merge `49a22e3`) |
| 28 | 2026-05-19 | products table, migration | Backfilled migration 034a (`migrations/034a_fruit_nut_bom_bake_line_copack.sql`) capturing verbatim the BEGIN…COMMIT transaction applied via Supabase SQL Editor on 2026-05-18 15:23 ET: (1) UPDATE products id=179 (Granola Fruit Nut Batch — name='Granola Fruit Nut Batch', brand='CNS', default_batch_lb 25→384.52, yield_25lb_cases=15, yield_10lb_cases=38, verification_status='verified', verification_notes=NULL, verified_at=NOW()); (2) DELETE FROM product_bom WHERE finished_product_id=179, then INSERT 5 BOM rows (Classic batch 90002 @ 323 lb + 4 inclusions @ 15.38 lb: sliced almonds, walnuts, raisins, dried cranberries — total 384.52 lb); (3) ALTER TABLE products ADD COLUMN bake_line text, then UPDATE bake_line='SS' on 7 Sunshine SKUs (70002/70003/70004/70006/70010/70011/70070); (4) ALTER TABLE products ADD COLUMN is_copack boolean NOT NULL DEFAULT false, then UPDATE is_copack=true on 9 co-pack SKUs (10301/10302/10303/10304/10305/10306/25013/25014/31012). All four blocks pre-commit-verified on 2026-05-18 by the operator; one explicit transaction. File is marked HISTORICAL — header forbids running against live DB (already applied). | Dropped `products.bake_line` column (migration 036, `migrations/036_drop_bake_line.sql`, applied 2026-05-19 11:47 ET). Single `ALTER TABLE products DROP COLUMN bake_line` inside a BEGIN/COMMIT with `SET TRANSACTION READ WRITE` (Supabase pooler safety net). Pre-drop verified: 201 rows total, 7 with `bake_line='SS'`. Post-commit `information_schema.columns` lookup confirms column is gone; row count unchanged. Zero code references to `bake_line` existed prior to drop (grep across Python/SQL/YAML/JSON/JS/HTML/MD); no views/functions/constraints/indexes referenced the column. | Migration 035 slot conflict resolved: two migrations independently claimed `035_*.sql` — `feat/planner-v2`'s `035_backfill_parent_batch_product_id_and_archive_retired_fgs.sql` (committed 2026-04-30, applied to prod 2026-04-30 ~11:22 ET) and sibling worktree `optimistic-dijkstra-b5d5ae`'s `035_drop_bake_line.sql` (created and applied to prod 2026-05-19 11:47 ET). Resolved by **chronological-replay rule: production apply order is canonical, regardless of which branch a migration was committed to first.** Earlier-applied migration keeps the contested slot; later-applied migration renumbers to next free slot. `backfill_parent_batch` (applied 19 days earlier) keeps 035; `drop_bake_line` renumbered to 036. **Precedent for future conflicts: neither commit date nor first-merged-to-main wins — production apply order wins.** | Reverting 034a: replay against a fresh schema loses the Fruit Nut BOM (id=179 BOM stays empty / wrong) and loses the `is_copack` column entirely (9 co-pack SKUs revert to indistinguishable from house SKUs). The `bake_line` column re-add is harmless on its own (036 drops it again on replay), but if 036 is reverted alongside, the column comes back with all NULLs — the 7 SS-tagged rows from the original 2026-05-18 session are not restored. Reverting 036: `bake_line` column reappears NULL across 201 rows; brand + parent_batch_product_id continue to carry routing info, so no functional break, but a redundant nullable column lingers and any code that later begins reading `bake_line` would silently see NULL. Reverting the renumber (034a→ different slot, or 036→035): breaks numeric-sort replay ordering and re-opens the slot conflict; do not revert without resolving the underlying chronology question first. | `migrations/034a_fruit_nut_bom_bake_line_copack.sql`, `migrations/036_drop_bake_line.sql` |
| 28a | 2026-05-28 | main.py, tests | **DEPLOYED 2026-06-10 13:24 ET (merge 0777df7, branch fix/readonly-tripwire-global-handler; app version bumped 3.0.0→3.1.0 as deploy probe — /openapi.json served 3.1.0 at 13:24:34). Post-deploy smoke all green: PATCH /sales/orders/999999/status → 404 `{detail, success:false, error_detail:{code:HTTP_404}}` with NO error_code/diagnostics (normal HTTPException path unaffected by the tripwire handlers); POST /lots/473/reassign {to_product_id:2,reason_code:"correction"} → 400 "Lot is already assigned to Almonds – Diced" + success:false + error_detail{HTTP_400}, no error_code/diagnostics; notes POST→id 20/success:true, DELETE→{deleted:true,success:true}, GET confirms gone; Railway deployment logs show clean startup and ZERO READONLY_TRIPWIRE / Unhandled-exception / safety-net lines.** Completes the global-handler follow-up flagged in #27: replaces the per-route diagnostics branch in `update_order_header` with a single `@app.exception_handler(Exception)` covering every route. The handler detects readonly via the existing `_is_readonly_error(exc)` helper, runs `_capture_readonly_diagnostics()` on a fresh pooled connection, and returns HTTP **503** with `{"success": false, "error_code": "READONLY_TRANSACTION", "retryable": true, "error": <str(exc)>, "diagnostics": {...}, "message": "..."}`. Emits a single ERROR log line prefixed `READONLY_TRIPWIRE:` followed by JSON (grep-friendly in Railway). Added 33 `if _is_readonly_error(e): raise` lines across per-route `except Exception` blocks (ship_commit, `PATCH /lots/{lot_id}/rename`, `POST /admin/bom/{product_id}/lines`, `POST /dashboard/api/notes`, pack, make, adjust, receive, et al.) so the readonly case bubbles out of the route's swallowing except into the global handler. Non-readonly exceptions keep the existing per-route `{"error": str(e)}` shape at status 500 — guarded by a regression test that asserts `RuntimeError("boom")` still returns 500 with body `{"error": "boom"}` and emits NO `READONLY_TRIPWIRE` log. New test file `tests/test_readonly_tripwire.py`: 6 tests, 6 pass — (a) positive ship_commit 503 + log + diagnostics shape, (b) negative regression guard, (c) parametrized 4-route coverage (ship/lots-rename/admin-bom/dashboard-notes) each verifying 503 envelope + per-route `path`/`method` in the log payload. Tests use `httpx.AsyncClient(transport=httpx.ASGITransport(app=main.app, raise_app_exceptions=False))` + `@pytest.mark.anyio` (anyio backend `asyncio`) — replaces starlette TestClient, which is incompatible with the container's httpx 0.28.1 (`Client.__init__()` no longer accepts `app=`). Existing 7 tests (`test_resolve_customer`, `test_ship_order_service_line`) unchanged. **Updated 2026-06-10 (rebase onto post-envelope main ddf2a32):** bare-`Exception` handlers are hoisted into ServerErrorMiddleware (outermost), which would bypass the deployed `write_response_envelope`; the handler logic (`_exception_receipt_response`) is now ALSO registered for `psycopg2.Error` so real readonly DB errors are handled in ExceptionMiddleware (innermost) and the envelope post-processes the 503 (adds `error_detail`). The bare-Exception registration remains as a safety net and replicates `error_detail` itself. Coexistence test added (psycopg2 ReadOnlySqlTransaction → 503 + diagnostics + success:false + error_detail); tripwire suite now 9 tests, full suite 43/43 vs local TEST_DATABASE_URL. | SO-260514-001 ship_order incident: PR #6's tripwire (#27) only covered `update_order_header`; when `POST /sales/orders/{id}/ship` (commit) hit a readonly error, the per-route `except Exception` swallowed it into a 500 with no diagnostic receipt and no `READONLY_TRIPWIRE` log line, leaving the next Supabase failover window unobservable on the most important mutation paths. | Reverting strips the `@app.exception_handler(Exception)` and the 33 `if _is_readonly_error(e): raise` lines — readonly errors on ship_commit, lots/rename, admin/bom, dashboard notes, pack, make, adjust, et al. revert to silent per-route 500 with no diagnostic receipt. Recurrence of SO-260514-001 failure mode: ship silently fails, no `error_code` in response, no `READONLY_TRIPWIRE` log line in Railway, failure window passes before session state can be captured. #27's per-route diagnostics branch in `update_order_header` is also subsumed (and removed on revert) by the global handler. The new `httpx.AsyncClient` test harness disappears with the test file. | `main.py`, `tests/test_readonly_tripwire.py` |
| 28b | 2026-05-28 | main.py, tests | **DEPLOYED 2026-06-10 13:24 ET (merge 0777df7, same deploy as row 28a).** Readonly connection discard + safe idempotent-write recovery path: `get_db_connection()` now calls `putconn(conn, close=True)` on readonly errors to evict poisoned connections from psycopg2 pool; new helpers `_discard_readonly_connection`, `_verify_connection_writable`, `_run_db_write`, `run_idempotent_write_with_readonly_retry`; `PATCH /customers/{customer_id}` rewired through retry helper as proof-of-concept (only explicitly safe/idempotent routes get retry — ship/make/pack/adjust do NOT retry, they fail loud with 503). Two new tests: connection-discard closes the bad connection, idempotent retry fires once on a fresh writable connection. | Poisoned readonly connections stayed in the psycopg2 pool after a Supabase failover window, causing subsequent requests to fail even after the primary was back. No safe retry path existed for idempotent writes. | Reverting: poisoned connections stay in pool after failover (requests fail until pool cycles); `PATCH /customers/{customer_id}` loses retry capability; two new tests disappear. Ship/make/pack/adjust are unaffected (they already fail loud via #29's global handler). | `main.py`, `tests/test_readonly_tripwire.py` |
| 29 | 2026-06-01 | dashboard | Cleaned the `bulk_25lb` ("25 LB Bulk Cases") SKU manifest in `dashboard/dashboard_config.json`: renamed `Granola Vanilla Crisp 25 LB` → `Granola Vanilla Crisp 25 LB (French Vanilla)` to match active product id=134 (odoo 70052, 1,100 lb on hand); removed `Granola Setton Morning Latte Crunch 25 LB` (retired SKU — product id=130 archived `active=false` in migration 035); removed `Classic Granola 25 LB` (dead duplicate — product id=171 deactivated; the live equivalent `Granola Classic 25 LB` id=136/odoo 70050 remains). List goes 15→13. Server-side manifest only, read by `main.py` `_load_dashboard_config` (~main.py:6764); no code or schema change. | The Operations tab rendered a red "Missing SKUs" alert on the 25 LB Bulk Cases group flagging three SKUs the backend match (`LOWER(p.name) = LOWER(sku) AND active=true`, main.py:6878-6879) could never satisfy: one name-format mismatch (French Vanilla suffix) and two retired/duplicate product rows. The flagged names were config staleness, not absent inventory. | Reverting restores the three stale entries; the red alert "Missing SKUs: Granola Vanilla Crisp 25 LB, Granola Setton Morning Latte Crunch 25 LB, Classic Granola 25 LB" returns and the panel header reverts from "(13 SKUs)" to "(12 SKUs)". No inventory or data impact — manifest only. | `dashboard/dashboard_config.json` |
| 30 | 2026-06-09 | main.py, tests, scripts, GPT configs | **DEPLOYED 2026-06-10 11:21 ET (merge c2d8017); historical cleanup COMPLETE 2026-06-10 (all 12 reversals voided; 3-lot verification mismatch resolved benign — real 6/9 evening makes #1295/#1299, see CHANGE_LOG.md 2026-06-10 11:31). The do-not-revert warning in "Breaks If Reverted" is now ACTIVE.** Unified void semantics: `status='posted'` is the single source of truth for ALL balance math. New module-level `POSTED_LINES` subquery constant + `lot_on_hand(cur, lot_id)` helper (main.py ~120); every balance/on-hand/availability query (~50 sites: validate_lot_deduction, /inventory/*, /lots/*, ship/make/pack/adjust FIFO availability, sales-order ship, dashboard inventory panels, /production/requirements, day-summary on-hand, planner loaders, /audit/integrity negative-lot+dust+case-size checks, trace on-hand scalars, lot timeline) now reads lines through the posted-only filter. `POST /void/{id}` reworked: flips status to 'voided', appends optional `{"reason"}` body to notes, posts NO reversal transaction; response keeps `reversal_transaction_id`/`reversal_lines` keys, now always null/empty. Floor GPT YAML + instructions prose updated (op counts: floor 21, gpt-v3 30 — unchanged). Tests: tests/test_void_semantics.py (4 tests). Post-deploy cleanup: scripts/cleanup_void_reversals.sh voids the 12 historical reversal txns (470, 864, 932–941) and verifies 16 lots; see VOID_SEMANTICS_RUNBOOK.md for deploy-timing warnings (deploy after floor close; run script immediately — interim window shows lots 610/617 negative). No schema migration. | Old void posted a reversal while some balance queries counted voided lines and others filtered posted — views disagreed by exactly the voided amounts (lot 582: 0 vs −2,907; lot 293: 0 vs +10; lot 612: 0 vs −3,800). | Reverting re-splits balance math into voided-counting and posted-only camps; any void issued under the new code then silently inflates/deflates the unfiltered views (no reversal exists to cancel it), and the 12 cleaned-up historical reversals (cleanup RAN 2026-06-10) stay voided, corrupting unfiltered balances by the reversal amounts. Do NOT revert after cleanup runs without re-posting reversals. | `main.py`, `tests/test_void_semantics.py`, `scripts/cleanup_void_reversals.sh`, `VOID_SEMANTICS_RUNBOOK.md`, `gpt-configs/schemas/openapi-floor.yaml`, `gpt-configs/sources/floor-specific.md`, `gpt-configs/dist/GPT_FLOOR_INSTRUCTIONS.md` |
| 31 | 2026-06-10 | main.py, tests, scripts, OpenAPI | **DEPLOYED 2026-06-10 12:34 ET (merge f5423b8, branch fix/write-response-contract e9e7482).** Uniform write-response contract: new `write_response_envelope` HTTP middleware (outermost, after CORS) adds `success: bool` to every JSON response on POST/PUT/PATCH/DELETE (setdefault — endpoints already returning it untouched) and, on ≥400, `error_detail: {code, message}` derived from existing `detail`/`error` keys (preserved verbatim). GETs untouched. Record ids added where missing: `order_id` on PATCH status / POST lines / PATCH cancel; nullable `reassignment_id` on POST /lots/{lot_id}/reassign. **BUG FIX: reassign's lot query used `GROUP BY ... FOR UPDATE OF l`, which Postgres rejects — the endpoint 500'd on EVERY call in prod** (same bug class as the ship-path fix in 89e15ae); split into row-lock then separate POSTED_LINES aggregate. Test infra: tests now run ONLY against a local DB (TEST_DATABASE_URL; conftest hard-refuses prod hosts, scrubs DATABASE_URL); schema-only prod dump committed as tests/schema/schema.sql; 23 new contract tests, suite 34/34 green. Post-deploy smoke 2026-06-10 ~12:35 ET all green: same-product reassign → 400 "already assigned" + envelope (no 500); notes POST/DELETE round-trip success:true and gone on GET; invalid transition on shipped order 3 → 400 with success:false + error_detail + original detail intact; GET /inventory/current has no success key. | June 9 audit: 12 write endpoints lacked the success+id contract the 9 core inventory ops have, so GPT callers couldn't reliably tell success from failure; POST /lots/{lot_id}/reassign was hard-broken (500 on every call); test suite previously pointed at the production DATABASE_URL. | **Reverting re-breaks POST /lots/{lot_id}/reassign entirely — the GROUP BY + FOR UPDATE query 500s on every call, making lot reassignment impossible via API.** Also: all write responses lose the uniform `success`/`error_detail` envelope (GPT callers parsing it fail back to guessing), the added `order_id`/`reassignment_id` keys disappear, and tests resume targeting whatever DATABASE_URL is set — including prod. | `main.py`, `tests/test_write_response_contract.py`, `tests/conftest.py`, `tests/schema/schema.sql`, `tests/requirements-test.txt`, `scripts/setup_test_db.sh`, `scripts/dump_prod_schema.sh`, `openapi-gpt-v3.yaml`, `gpt-configs/schemas/openapi-floor.yaml` |
| 32 | 2026-06-10 | main.py, dashboard, tests | **DEPLOYED 2026-06-10 14:33 ET (merge 60c6f62, branch fix/auth-unauthenticated-writes da9ad73; app version bumped 3.1.0→3.1.1 as deploy probe — /openapi.json served 3.1.1 at 14:33:10). CD race resolved benignly: Netlify (dashboard.js?v=9 with X-API-Key) was live at 14:32:29, BEFORE Railway — new JS sent the key to the old API for ~40s, harmless; the notes UI had zero downtime. Post-deploy smoke all green: POST notes no key → 401 {detail:"API key required", success:false, error_detail:{HTTP_401}}; DELETE notes/999999 no key → 401 (auth fires before domain 404); POST with key "auth smoke - delete me" → id 21 success:true, DELETE → {deleted:true,id:21,success:true}, GET confirms gone (0 matches); PATCH /sales/orders/999999/status → 404 envelope byte-identical to row 31's record (no error_code/diagnostics); Railway logs since deploy: clean startup, 0 READONLY_TRIPWIRE, 0 errors.** Closed the unauthenticated-write surface from the June 9 audit: added the standard `Depends(verify_api_key)` to the only four mutating routes that lacked it — `POST /dashboard/api/notes`, `PUT /dashboard/api/notes/{id}`, `DELETE /dashboard/api/notes/{id}`, `PUT /dashboard/api/notes/{id}/toggle` (missing key → 401, wrong key → 403; same dependency as every other write route, no new auth scheme). Companion dashboard change: the four notes fetches in `dashboard/dashboard.js` now send `X-API-Key` via the existing in-file `SALES_API_KEY` const (no new exposure — that const was already published in the same file); `dashboard/index.html` cache-buster bumped `dashboard.js?v=8`→`v=9` (NOT v=7→v=8: commit 1ff7efa had already taken v=8). Read endpoints (incl. `GET /dashboard/api/notes`) deliberately untouched. No OpenAPI change; gpt-v3 op count stays 30. Tests: new `tests/test_notes_auth.py` (8 tests: 401-without-key on all four, 403-wrong-key, no-row-on-rejected-create, with-key happy path per endpoint, with-key 404 domain errors preserved); existing tripwire + contract tests pass unmodified (they already send the key); suite 51/51 green vs local TEST_DATABASE_URL. **Honesty note: this adds CONSISTENCY, not security — the API key remains published in dashboard.js, served to anyone by Netlify. Its purpose is to make a future key rotation actually cover the full write surface: after this, rotating the key locks every mutating route, with no keyless side door left behind.** **CD race on deploy:** one merge to main triggers BOTH the Railway (API) and Netlify (dashboard) deploys; until both finish there is a brief window where the notes UI can hit the new auth requirement without sending the key yet (401s in the notes panel). Expected, self-healing once Netlify finishes, no action needed. | June 9 audit: unauthenticated-write exposure — the notes CRUD endpoints were the only mutating routes callable with no key at all (anyone could create/edit/delete notes, and a future key rotation would not have protected them). Caller analysis confirmed the only callers are the four dashboard.js notes fetches (no GPT schema, no scripts, no other dashboard pages), so the paired JS change is the complete blast radius. | Reverting main.py reopens the four keyless write endpoints — the only unauthenticated mutating surface returns, and a future key rotation silently fails to cover notes again. Reverting only dashboard.js (keeping main.py) breaks the dashboard notes UI outright: every create/edit/delete/toggle 401s. Reverting the v=9 cache-buster lets browsers keep stale v=8 JS that omits the key → same 401 breakage for returning visitors. | `main.py`, `dashboard/dashboard.js`, `dashboard/index.html`, `tests/test_notes_auth.py` |
| 33 | 2026-06-23 | main.py, dashboard, tests | **NOT YET DEPLOYED — branch `feat/sales-order-inline-expand` (local commit only). Add deploy/smoke detail + version-probe here at merge time.** Sales Orders list now expands line items inline. (a) `main.py` `get_sales_order`: line query gains `p.odoo_code` + `p.uom`; each line dict gains `sku` (odoo_code, may be null) and `uom` (coalesced to `'lb'`). Purely additive response fields — no new operation, gpt-v3 op count stays 30, no OpenAPI/GPT-schema change, no balance/auth/void-semantics impact. (b) `dashboard/dashboard.js`: new leading expand column on `.orders-table` with a per-row caret toggle (`.order-expand-toggle`); click handler calls `stopPropagation()` so it does NOT trigger the row click that opens the full detail page (SO-number click behavior unchanged). Toggling shows/hides a sibling `.order-lines-row` and lazily fetches `/sales/orders/{id}` once, caching the detail in `state.orderLinesCache` (survives filter/search re-renders → no refetch). New `renderOrderLinesContent` renders a SKU / Product / Ordered / UoM / Remaining sub-table; new `bindOrderExpandToggles` wires the controls. Multiple rows can be expanded simultaneously; expansion state resets on re-render (filters/refresh) by design, cache makes re-expand instant. (c) `dashboard/dashboard.css`: dark-theme styles for the toggle, 90° caret rotation on expand, and the inline `.order-lines-table` (mirrors existing `.orders-table`/`.order-inventory-table` patterns). (d) `dashboard/index.html`: cache-busters `dashboard.css?v=6→v=7`, `dashboard.js?v=9→v=10`. Tests: new `tests/test_sales_order_line_fields.py` (2 tests: lines expose correct `sku`/`uom`; `uom` defaults to `'lb'` when product.uom is NULL); full suite 53/53 green vs local TEST_DATABASE_URL. | Operators had to open the full order-detail page to see any line item; they wanted SKU/product/ordered/UoM/remaining inline in the list while keeping the detail page for the SO-number click. | Reverting `main.py` strips `sku`/`uom` from the line response → the inline panel's SKU and UoM columns go blank (and any other consumer of those keys loses them). Reverting `dashboard.js`/`dashboard.css` removes the expand column and inline rendering; the table reverts to row-click-only navigation. Reverting the v=10/v=7 cache-busters leaves browsers on stale v=9 JS / v=6 CSS that lack the expand control and its styles. No data/schema impact either way. | `main.py`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `tests/test_sales_order_line_fields.py` |
| 34 | 2026-06-23 | sales order annotations, dashboard, tests | **NOT YET DEPLOYED — branch `feat/so-floor-ready`; migration NOT RUN.** Adds a UI-only Factory Ready annotation layer for open Sales Orders. New standalone migration `migrations/037_sales_order_flags.sql` creates `sales_order_flags(so_number primary key, ready, ready_at, ready_by, note, updated_at)`. `POST /sales-orders/{so_number}/ready` upserts only that annotation row after checking the SO exists and is open; it is modeled on `POST /dashboard/api/notes` and uses the same `Depends(verify_api_key)` auth, `get_transaction()` write path, global `write_response_envelope`, and `_is_readonly_error(e): raise` tripwire escape. `GET /sales/orders` LEFT JOINs `sales_order_flags` and adds only `ready`, `ready_at`, `ready_by`, `note` to each order row. Dashboard Sales Orders table only: checkbox immediately after the expand caret with optimistic POST, faded `.so-ready` row + green left border, READY pill near status, drawer note input/display, and "Hide ready" filter. Cache-busters `dashboard.css?v=7→v=8`, `dashboard.js?v=10→v=11`. Tests/schema updated for the annotation table and endpoint/list contract. | Operators need a floor-facing way to mark an open SO as factory-ready without changing the order lifecycle status or touching any operational ledger records. | Reverting removes only the annotation layer: ready checkboxes/pills/notes/filter disappear and `/sales/orders` stops returning the four annotation fields. Existing sales order, inventory, FIFO, lot, production, and shipment logic/data remain unchanged because this change never writes those tables. Reverting only the migration while keeping code breaks the list JOIN and ready POST at runtime (`sales_order_flags` missing). Reverting only dashboard code leaves an unused API/table with no UI. Reverting cache-busters can leave browsers on stale JS/CSS without the annotation controls. | `migrations/037_sales_order_flags.sql`, `main.py`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `tests/schema/schema.sql`, `tests/test_write_response_contract.py` |
| 35 | 2026-06-29 | sales order pallet display, dashboard, tests | **DEPLOYED 2026-06-29 13:15 ET (commit `fa091e6`; Railway deployment `d0c13a45-9ec6-4ff6-8388-0994716afa20`; Netlify deploy `6a42a80a626e1c0008f39d53`).** Added shared `pallet-calculations.js`: 10-lb cases use 140 cases/pallet, 25-lb cases use 60, calculations consume case/unit counts, mixed fractional totals include rounded physical pallet counts, and unknown mappings return a quiet em dash. `/sales/orders` adds read-only `pallet_lines` summaries so the list can calculate from cases without extra per-order requests. List, inline expansion, and detail views show compact total/per-line ordered and remaining pallets. No inventory, shipment, lot, order, or database data writes. Cache-busters: CSS v11, dashboard JS v14, helper v1. Tests: 56 pytest + 4 Node tests green; syntax/diff checks green. Production smoke: Juliette SO-260615-002 shows seven 24-case lines at 0.4 pallet and `2.8 pallets / 3 physical mixed pallets`; Restaurant Depot SO-260629-001 and SO-260526-001 show `26 pallets`; unknown Blue Stripes mapping shows `—`; expand/detail/back and existing Factory Ready controls render; browser console and Railway error scan clean. | Operators had to mentally convert case quantities into pallets, especially on mixed 25-lb orders, and could not see physical rounded pallet requirements at a glance. | Reverting the helper or its script tag breaks pallet rendering; reverting only `main.py` makes list totals fall back to `—` while detail calculations still work; reverting dashboard JS/CSS removes the pallet columns/KPI/secondary text. Removing the httpx test pin makes the repo's Starlette TestClient suite fail under httpx 0.28+. Operational order/inventory/shipment logic remains unaffected because this feature is read/display-only. | `main.py`, `dashboard/pallet-calculations.js`, `dashboard/dashboard.js`, `dashboard/dashboard.css`, `dashboard/index.html`, `tests/test_pallet_calculations.js`, `tests/test_sales_order_line_fields.py`, `tests/requirements-test.txt` |
| 36 | 2026-07-08 | open-orders matrix Excel export, dashboard, tests | **DEPLOYED 2026-07-08 14:44 ET (commit `5aec509`; Railway deployment `369dfadb-2fc5-43d7-a51a-12a06f6f423e`).** Added authenticated dashboard-only `GET /export/orders-matrix.xlsx` (excluded from OpenAPI) with styled Cases/Pounds sheets, live formulas, UoM validation, PAN_YIELD planning rows/comments, week banding/separators, past-due styling, and fractional-case visibility. Added the Sales Orders matrix-download button and openpyxl dependency. Production smoke returned HTTP 200 with the XLSX media type; Netlify served dashboard JS v17, the button, and handler. Matrix tests 2/2 passed; GPT schema remained untouched at 30 operations. | Production planners needed a case/pound matrix with product-family layout and batch/pan planning, while unknown UoMs had to fail loudly rather than silently produce wrong weights. | Reverting removes the XLSX route and dashboard download, loses UoM safety and fractional-case visibility, and removes live planning formulas/comments. Reverting only the dependency breaks application import/startup because `main.py` imports openpyxl. Reverting only dashboard code leaves the API inaccessible from the UI. | `main.py`, `requirements.txt`, `dashboard/dashboard.js`, `dashboard/index.html`, `tests/test_orders_matrix_export.py` |

## Known Root Causes

- **Weight calculation** — service/charge lines on SOs must always be filtered out (`line_type` or product category check)
- **Inventory accuracy** — add-in ingredients must be deducted at pack time, not just primary ingredients
- **Cross-origin hosting** — dashboard on Netlify, API on Railway; all API calls must use absolute URLs
- **Timezone** — all timestamps must use `America/New_York`; mixing UTC causes off-by-one on day boundaries
- **6543 pooler session readonly** — `set_session(readonly=True)`, `SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY`, and `default_transaction_read_only=on` on the transaction-mode pooler (port 6543) survive disconnect and poison the next client, including the app (2026-08-17 SO-260811-002 READONLY_TRIPWIRE). Use transaction-scoped `BEGIN TRANSACTION READ ONLY` / `SET TRANSACTION READ ONLY` after each `BEGIN`, or connect via the session pooler on port 5432. Never apply session-level readonly to the 6543 URL.

## Permanent Rules (GPT Instructions)

When editing GPT instructions (`GPT_INSTRUCTIONS.md` or `gpt-instructions-v3.md`), all of these rules must survive:

1. Always use ET timezone for all dates/times
2. Lot numbers follow format: `YY-MM-DD-XXXX-NNN`
3. Pack transactions auto-deduct add-in ingredients
4. Service/charge lines excluded from weight totals
5. `case_size_lb` must be set before packing (hard fail if missing)
6. Ship endpoint validates SO line quantities before committing
7. Receive transactions require supplier lot when available
8. Batch/make transactions consume ingredients proportionally
9. Dashboard API uses absolute URLs (Railway base)
10. All migrations are idempotent (IF NOT EXISTS / ON CONFLICT DO NOTHING)
