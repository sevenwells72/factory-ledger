# Local Netlify folder
.netlify

# Claude Code worktrees
.claude/worktrees/

# Environment variables (secrets)
.env

# Local test venv and Python caches
.venv-test/
__pycache__/

# macOS Finder metadata
.DS_Store

# floor form exports (local only, never commit)
data/forms/*.csv

# Node tooling for tests/visual (Playwright harness)
node_modules/
package-lock.json

# Visual-audit output: screenshots + raw results are regenerated by
# `npm run test:visual`, never committed (docs/design/audit/06-browser-check.md is)
docs/design/audit/screenshots/

# Generated by scripts/mint_actor_keys.py — hashes only, but it is operational
# output tied to one minting run, not source. Paste it into the Supabase SQL
# editor and delete it.
actors_insert.sql
